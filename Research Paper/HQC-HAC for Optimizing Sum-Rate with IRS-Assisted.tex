\documentclass[12pt,onecolumn]{IEEEtran}

\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{caption}
\usepackage{cite}
\usepackage{xcolor}
\usepackage{tabularx}
\usepackage{array}
\usepackage{bm}
\usepackage{tikz}
\usepackage{quantikz}
\usepackage{amsmath}
\usepackage{makecell}
\usepackage{booktabs}
\newcolumntype{Y}{>{\raggedright\arraybackslash}X}

\usepackage{setspace}
\renewcommand{\baselinestretch}{1.75}





\title{\LARGE 
Hybrid Quantum-Classical Hierarchical Actor-Critic for Sum-Rate Maximization in IRS-Assisted Grouped RSMA Systems
}

\author{Thinh Duong Tan Hung and Wonjong Noh}

\begin{document}

\maketitle

\begin{abstract}
Integrated satellite-terrestrial networks (ISTNs) have emerged as a key architecture for next-generation wireless systems, offering wide-area coverage and connectivity by combining satellite and terrestrial infrastructure.
However, ISTNs face fundamental challenges including severe path loss, atmospheric fading, building blockage, and the worst-user bottleneck caused by users with poor channel conditions.
To address these challenges, this paper considers an IRS-assisted grouped rate-splitting multiple access (RSMA) downlink system in ISTN, where multiple intelligent reflecting surfaces (IRSs) improve link reliability for users under blockage, and grouped RSMA enables flexible interference management across heterogeneous user groups.

We propose a hybrid quantum-classical hierarchical actor-critic (HQC-HAC) framework to jointly optimize four coupled decision variables: IRS-assisted link assignment, IRS phase-shift configuration, transmit power allocation, and common-rate distribution.
These variables form a sequential dependency chain --- the user grouping from link assignment constrains phase-shift design, which determines effective channels for power allocation, and the resulting power levels set the rate budget for common-rate splitting.
This inherent ordering motivates a hierarchical decomposition.
At the top level, a quantum actor based on variational quantum circuits (VQCs) resolves the combinatorial IRS assignment bottleneck, whose search space scales as $(M+1)^K$.
An autoencoder (AE) compresses the high-dimensional system state into a compact latent representation before quantum encoding to improve scalability.
At the lower level, three classical sub-actors sequentially handle phase-shift, power, and common-rate decisions.
Entropy regularization with an annealing schedule is applied across all actors to maintain sufficient exploration during training.

Simulation results show that the proposed HQC-HAC achieves significant sum-rate gains over baseline methods while satisfying per-user quality-of-service (QoS) constraints under imperfect channel state information (CSI) and dynamic user mobility.
The quantum assignment actor attains sum-rate performance comparable to its classical deep-network counterpart while using two orders of magnitude fewer assignment-actor parameters, and the learned policy amortizes the per-state optimization cost of model-based methods into a single offline training run.
\end{abstract}

\begin{IEEEkeywords}
Integrated satellite-terrestrial networks (ISTN), intelligent reflecting surface (IRS), rate-splitting multiple access (RSMA), hierarchical actor-critic (HAC), variational quantum circuit (VQC), hybrid quantum-classical reinforcement learning, sum-rate maximization, quality of service (QoS), resource allocation.
\end{IEEEkeywords}


\section{Introduction}

\subsection{Background}

The proliferation of wireless services and the escalating demand for pervasive global connectivity have exposed the fundamental limitations of conventional terrestrial networks, whose coverage is inherently circumscribed by the deployment of fixed ground-based infrastructure. Satellite systems offer a compelling remedy: operating across a diverse spectrum of orbital regimes --- from low Earth orbit (LEO) mega-constellations such as Starlink and OneWeb that deliver low-latency broadband, through medium Earth orbit (MEO) platforms underpinning global navigation systems, to geostationary (GEO) satellites that provide continental-scale coverage from a fixed vantage point --- they maintain line-of-sight access over vast geographical expanses irrespective of terrain~\cite{wang2019convergence}. The recent emergence of small-form-factor CubeSats has further diversified this landscape by substantially reducing launch costs and enabling rapid constellation scaling. Integrating these orbital tiers with terrestrial infrastructure into a unified integrated satellite-terrestrial network (ISTN) has consequently attracted significant research interest as a pathway to delivering pervasive connectivity to maritime, rural, and geographically isolated environments where ground-based base stations are neither practical nor economically viable~\cite{zhu2021integrated,kodheli2020satellite}.

Despite this coverage advantage, satellite-based communications are inherently susceptible to physical impairments that intensify with the propagation distance involved. The extended path between an orbiting satellite and a ground receiver exposes the signal to substantial free-space path loss, rain attenuation, and tropospheric scintillation~\cite{3gpp38811}. In densely built environments, buildings and urban structures further compound these impairments by shadowing the direct satellite-to-user link, leaving the affected terminal without a reliable primary propagation path. Furthermore, when multiple users concurrently access the satellite downlink, inter-user interference degrades individual link quality, and terminals operating under the most adverse channel conditions --- commonly referred to as the worst-user bottleneck --- impose a disproportionate constraint on the aggregate achievable rate of the entire served population~\cite{liu2011multicast}.

The worst-user bottleneck is a well-recognized impediment in multi-user downlink design. Rate-splitting multiple access (RSMA) addresses it by introducing a structured degree of flexibility into interference management~\cite{mao2018rate}. Rather than fully decoding interference, as in non-orthogonal multiple access (NOMA), or treating it entirely as noise, as in space-division multiple access (SDMA), RSMA decomposes each user message into a common component --- decoded cooperatively by a designated set of receivers via successive interference cancellation (SIC)~\cite{cover1972broadcast} --- and a private component decoded independently at each terminal. This partial interference treatment effectively decouples overall system throughput from the performance of the weakest user. A grouped extension of RSMA refines this mechanism further by partitioning users into subgroups according to their channel conditions or propagation characteristics~\cite{tan2025flexible}: within each subgroup, the decodable common rate is bounded by the least-capable member of that group rather than by the globally worst-performing user, affording considerably finer granularity in rate allocation across the network.

Partitioning users by propagation characteristics inevitably raises the question of how to adequately serve those whose direct satellite link is geometrically obstructed. Rate-splitting alone is insufficient to compensate for a physically blocked propagation path, necessitating an alternative transmission route. Intelligent reflecting surfaces (IRSs) furnish precisely such a mechanism~\cite{pan2022overview}. An IRS comprises a planar array of passive reflecting elements, each capable of independently imposing a controllable phase shift on the incident electromagnetic wave. Through judicious configuration of these phase shifts, the reflected wavefront can be coherently steered toward an intended receiver, effectively establishing a virtual line-of-sight link that circumvents the obstruction without incurring additional transmit power expenditure. In the context of grouped RSMA, each active IRS can serve as the reflective backbone of a designated user group, providing reliable alternative propagation paths for terminals that are unreachable via the direct satellite link, as illustrated in Fig.~\ref{fig:istn_system}: Group~$0$ users with unobstructed satellite links receive the direct signal $x_0$, while users in Group~$g$ and Group~$g'$ behind buildings are served through dedicated IRS panels carrying the reflected signals $x_g$ and $x_{g'}$, respectively.

\begin{figure}[t]
    \centering
    \includegraphics[width=0.65\linewidth]{images/ISTN_Comms}
    \caption{Proposed IRS-assisted grouped RSMA system in an ISTN downlink.
    The satellite simultaneously serves $G+1$ groups: Group~$0$ users with unobstructed satellite links receive the direct signal $x_0$, while Group~$g$ and Group~$g'$ users blocked by buildings are served via dedicated IRS panels carrying reflected signals $x_g$ and $x_{g'}$.
    Each IRS steers its reflected signal toward the assigned group through coherent phase-shift configuration $\boldsymbol{\Phi}_m$.}
    \label{fig:istn_system}
\end{figure}

The joint optimization of IRS-user link assignment, phase-shift configuration, transmit power allocation, and common-rate distribution constitutes a mixed-integer non-convex problem that admits no tractable closed-form solution. Reinforcement learning (RL) is well-suited to this class of problems by virtue of its model-free nature, acquiring effective resource allocation policies through iterative environment interaction without requiring an explicit analytical characterization of the system~\cite{sutton2018reinforcement}. A salient structural feature of the present problem is that the four optimization variables are not independent but form a strict sequential dependency: the IRS-user assignment defines the grouping structure, which in turn governs the effective channel conditions underlying the phase-shift design, which subsequently shapes the power allocation landscape, and the resulting power budget finally delimits the feasible common-rate split. This inherent ordering provides a principled motivation for a hierarchical actor-critic (HAC) decomposition~\cite{levy2019learning}, wherein a high-level actor commits to the discrete IRS assignment and dedicated low-level actors resolve the remaining continuous variables in sequence --- an architectural paradigm demonstrated to be effective in managing the expansive joint action spaces encountered in IRS-assisted wireless resource management~\cite{gong2022hierarchical}, \cite{gong2023hierarchical}. Entropy regularization is further incorporated across all hierarchical levels to sustain adequate policy diversity and forestall premature convergence during training~\cite{haarnoja2018soft}.

Even with a hierarchical decomposition in place, the top-level IRS assignment remains the principal source of discrete complexity: as the number of deployed IRS panels and served users scales up, the space of feasible user-to-link associations expands at a rate that rapidly renders classical discrete optimization and flat RL policies intractable for realistic system dimensions. Variational quantum circuits (VQCs) offer a fundamentally different computational pathway by harnessing quantum superposition and entanglement to represent rich probability distributions over large discrete action spaces with a compact set of trainable parameters~\cite{jerbi2021parametrized}. A further practical impediment arises from the high dimensionality of the wireless system state, which encompasses complex channel coefficients, blockage indicators, per-user traffic demands, and current allocation variables --- a representation far too unwieldy to be encoded directly into the constrained register of an available quantum device. An autoencoder (AE)~\cite{hinton2006reducing} bridges this dimensionality gap by learning a compact latent embedding of the system state that preserves the information most pertinent to the assignment decision, following the hybrid quantum-classical paradigm established in~\cite{nagy2025hybrid}. This AE-VQC architecture constitutes the quantum actor at the apex of the proposed HQC-HAC framework, resolving the combinatorial bottleneck at the highest level of the hierarchy while remaining fully interoperable with the classical sub-actors governing the lower-level continuous decisions. Motivated by the complementary strengths of these components, this paper proposes the hybrid quantum-classical hierarchical actor-critic (HQC-HAC) framework for sum-rate maximization in IRS-assisted grouped RSMA systems within ISTN communications.

\begin{table*}[t]
\centering
\caption{Comparison of existing works on IRS/RIS-assisted communication systems}
\label{tab:comparison_existing_works}
\begin{tabular}{c|c|c|c|c|c|c|c|c|c|c|c}
\hline
\textbf{Ref.} 
& \textbf{Access} 
& \textbf{Blockage}
& \textbf{CSI} 
& \textbf{Steer. Vec.} 
& \textbf{Prop. Path}
& \multicolumn{3}{c|}{\textbf{IRS/RIS}}
& \textbf{RL} 
& \textbf{QC} 
& \textbf{Obj.} \\
\cline{7-9}
& 
& 
& 
& 
& 
& \textbf{Scale}
& \textbf{Type}
& \textbf{Phase}
& 
& 
& \\
\hline

2022 {\cite{cao2022robust}} 
& SDMA 
& \checkmark 
& Imperf. 
& -- 
& Multi 
& Single 
& Passive 
& Cont. 
& -- 
& -- 
& Sum-rate \\

2022 {\cite{dong2022intelligent}} 
& TDMA 
& \checkmark 
& Perf. 
& \checkmark
& Multi 
& Single 
& Passive 
& Cont. 
& -- 
& -- 
& WSR \\

2024 {\cite{asif2024transmissive}} 
& H-NOMA 
& --
& Perf. 
& \checkmark
& Single 
& Single 
& Passive 
& Cont. 
& -- 
& -- 
& TA-SR \\

2025 {\cite{peng2025robust}} 
& RSMA 
& -- 
& Imperf. 
& -- 
& Multi 
& Single 
& Passive 
& Disc. 
& -- 
& -- 
& WSR \\

2025 {\cite{han2025weighted}} 
& RSMA 
& -- 
& Perf. 
& -- 
& Single 
& Single 
& Passive 
& Disc. 
& -- 
& -- 
& WSR \\

2025 {\cite{lv2025weighted}} 
& MU-BF 
& -- 
& Perf. 
& -- 
& Multi 
& Single 
& Passive 
& Cont. 
& -- 
& -- 
& WSR \\

2025 {\cite{fu2025average}} 
& NOMA 
& -- 
& Perf. 
& \checkmark
& Multi
& Single 
& Passive 
& Cont. 
& \checkmark
& -- 
& ASR \\

2025 {\cite{li2025sum}} 
& RSMA 
& \checkmark 
& Perf. 
& -- 
& Single 
& Multi 
& Passive 
& Cont. 
& -- 
& -- 
& Sum-rate \\

2026 {\cite{he2026weighted}} 
& RSMA 
& -- 
& Imperf. 
& \checkmark 
& Multi 
& Multi 
& Passive 
& Cont. 
& -- 
& -- 
& WSR \\

2026 {\cite{zhang2026satellite}} 
& RSMA 
& -- 
& Imperf.
& -- 
& Multi
& Single
& Active
& Cont.
& -- 
& -- 
& Sum-rate \\

2026 {\cite{tan2025flexible}} 
& RSMA 
& -- 
& Perf. 
& -- 
& Single 
& Single 
& Passive 
& Cont. 
& -- 
& -- 
& Sum-rate \\

\textbf{Proposed} 
& G\_RSMA
& \checkmark 
& Imperf. 
& \checkmark 
& Multi 
& Multi 
& Passive 
& Disc.
& \checkmark 
& \checkmark 
& Sum-rate \\
\hline
\end{tabular}
\end{table*}

Based on the comparison in Table~\ref{tab:comparison_existing_works} and to the best of our knowledge, no existing study jointly addresses IRS-assisted grouped RSMA, blockage-aware transmission, imperfect CSI, multi-IRS deployment, discrete phase shifts, RL-based optimization, and quantum-enhanced policy learning within a single framework.
Motivated by this collective gap, this paper proposes the hybrid quantum-classical hierarchical actor-critic (HQC-HAC) framework for sum-rate maximization in IRS-assisted grouped RSMA-enabled ISTN systems.

\subsection{Contributions}

Motivated by the identified gaps, this paper proposes the HQC-HAC framework for sum-rate maximization in IRS-assisted grouped RSMA systems over ISTN downlinks.
The framework jointly optimizes four sequentially coupled decision variables --- IRS-user link assignment, IRS phase-shift configuration, transmit power allocation, and common-rate distribution --- under per-user QoS constraints, discrete phase quantization, imperfect CSI, and building blockage.
The main contributions of this paper are summarized as follows:

\begin{itemize}
    \item We formulate a sum-rate maximization problem for a multi-IRS-assisted grouped RSMA downlink system in ISTN, in which IRS-user link assignment, IRS phase-shift configuration, transmit power allocation, and common-rate distribution are jointly optimized as a sequential dependency chain under per-user QoS, discrete phase quantization, building blockage, and imperfect CSI constraints.

    \item We propose the HQC-HAC framework to solve the resulting mixed-integer non-convex problem by exploiting the inherent sequential dependency of the decision variables. A quantum actor based on a variational quantum circuit (VQC) resolves the combinatorial IRS-user assignment at the high level, while three classical sub-actors sequentially handle phase-shift, power, and common-rate decisions at the low level. Entropy regularization with an annealing schedule is incorporated across all hierarchical levels to sustain policy diversity and forestall premature convergence.

    \item We design a hybrid AE-VQC quantum actor architecture in which an autoencoder (AE) compresses the high-dimensional wireless system state --- comprising channel coefficients, blockage indicators, user demands, and allocation variables --- into a compact latent representation before quantum encoding, enabling the VQC to operate within the constrained register of near-term quantum devices while preserving the information critical to the assignment decision.
\end{itemize}

\subsection{Organization}

The remainder of this paper is organized as follows.
Section~\ref{sec:related} reviews related works on IRS-assisted satellite communications, grouped RSMA, and hybrid quantum-classical reinforcement learning.
Section~\ref{sec:model} presents the system model and formulates the sum-rate maximization problem.
Section~\ref{sec:framework} details the proposed HQC-HAC framework, covering the hierarchical actor-critic decomposition, AE-based latent state compression, VQC-based quantum actor design, and entropy regularization schedule.
Section~\ref{sec:results} presents simulation results and performance comparisons against baseline methods.
Section~\ref{sec:discussion} discusses the implications and limitations of the proposed framework.
Section~\ref{sec:conclusion} concludes the paper.

\section{Related Works}
\label{sec:related}

\subsection{IRS-Assisted Grouped RSMA for Sum-Rate Optimization in ISTNs}

The foundational framework for IRS-assisted wireless networks was established by Wu and Zhang~\cite{wu2019intelligent}, who studied an IRS-aided multiuser MISO downlink in which a multi-antenna access point serves multiple single-antenna users via a passive reflective array.
Their work demonstrated that jointly optimizing active transmit beamforming at the access point and passive phase shifts at the IRS yields substantial reductions in total transmit power relative to IRS-free baselines.
By aligning the phase shifts so that the IRS-reflected signal combines coherently with the direct path at the intended receiver, the IRS effectively establishes a virtual line-of-sight link that circumvents physical obstructions and augments received signal power without additional transmit energy expenditure.
Reflected signals can simultaneously be steered destructively at non-intended receivers, furnishing an additional spatial degree of freedom for interference suppression beyond what is attainable through active beamforming alone.
The resulting non-convex problem was addressed via semidefinite relaxation (SDR) and alternating optimization, with analysis confirming that the required transmit power decreases on the order of $N^2$ as the number of reflecting elements $N$ grows --- a favorable scaling law that motivates large-element IRS deployments in practical systems.

Building upon this beamforming paradigm, Tan et al.~\cite{tan2025flexible} extended IRS-assisted transmission to satellite downlink communications by proposing a flexible grouped RSMA scheme for sum-rate maximization in ISTNs.
In their system, the $K$ ground users are dynamically partitioned into two disjoint groups based on real-time channel conditions and potential line-of-sight obstructions: group $\mathcal{K}_1$ users are exclusively served via the satellite-IRS-user reflected link, while group $\mathcal{K}_2$ users communicate through the direct satellite-user link.
Within each group, RSMA decomposes every user's message into a common sub-message --- decoded cooperatively by all group members via successive interference cancellation (SIC) using a shared codebook --- and an independent private sub-message decoded solely at the intended terminal.
Critically, the decodable common rate within each group is bounded by the weakest member of that subgroup rather than by the globally worst-performing user across the entire population, substantially alleviating the worst-user bottleneck inherent in conventional flat RSMA designs.
The joint optimization problem encompasses satellite precoding beamforming, IRS passive phase-shift configuration, rate-splitting allocation, and binary IRS-user group selection, and was decomposed into four subproblems solved respectively via SDR with polyblock outer approximation (POA), Riemannian manifold conjugate gradient (RMCG) descent on the IRS phase manifold, alternating optimization for rate allocation, and simulated annealing for the binary group assignment, with the joint solution recovered iteratively.

Despite reporting approximately 29\% sum-rate gain over the IRS-assisted RSMA baseline, the framework of~\cite{tan2025flexible} carries several limitations pertinent to realistic deployment.
The optimization presupposes perfect and instantaneous CSI at the satellite, whereas practical satellite links are susceptible to estimation errors arising from channel aging, Doppler effects, feedback delay, and limited feedback bandwidth.
Only a single IRS panel is considered, which is insufficient to extend reflected coverage across spatially dispersed user groups.
Furthermore, the iterative model-based solver incurs computational complexity that scales prohibitively as the numbers of users, IRS elements, and control variables grow, and the framework provides no mechanism for adaptive policy adjustment under dynamic channel conditions or user mobility.
The proposed HQC-HAC framework addresses these deficiencies by optimizing IRS-user link assignment across multiple IRS panels under imperfect CSI, building blockage, and dynamic user mobility, replacing model-based sub-problem decomposition with a hierarchical reinforcement learning approach whose variational quantum policy representation manages the combinatorial complexity of multi-IRS user assignment.

\subsection{Entropy-Regularized Hierarchical Reinforcement Learning and Hybrid Quantum-Classical Policies}

Hierarchical actor-critic (HAC) methods decompose complex decision problems into multiple levels of abstraction, with high-level actors committing to sub-goals that low-level actors subsequently fulfill~\cite{levy2019learning, bacon2017option}.
This temporal and functional decomposition has been demonstrated to accelerate learning in environments with large composite action spaces, including IRS-assisted wireless resource management, where jointly optimizing discrete and continuous variables over extended interaction horizons is otherwise intractable~\cite{gong2022hierarchical}.
Entropy regularization is commonly incorporated into actor-critic frameworks to encourage sufficient policy diversity during training: the maximum-entropy reinforcement learning objective of the soft actor-critic algorithm~\cite{haarnoja2018soft} penalizes premature collapse to deterministic policies and has been applied extensively in continuous-action wireless optimization problems.

Hybrid quantum-classical reinforcement learning extends this paradigm by replacing or augmenting classical policy networks with variational quantum circuits (VQCs)~\cite{jerbi2021parametrized}.
VQCs leverage parameterized quantum state transformations to model complex nonlinear policy mappings using comparatively compact trainable representations, thereby motivating their investigation for high-dimensional combinatorial decision problems.
However, directly encoding high-dimensional wireless system states into a quantum register is impractical given current hardware constraints.
Nagy et al.~\cite{nagy2025hybrid} addressed this by jointly training an autoencoder (AE) with a VQC-based policy, where the AE compresses the original observation into a low-dimensional latent space suitable for quantum encoding.
Motivated by these complementary contributions, this work embeds an AE-VQC quantum actor within an entropy-regularized HAC structure, applying it to the combinatorial IRS-user assignment problem in IRS-assisted grouped RSMA over ISTN downlinks.

\section{System Model and Problem Formulation}
\label{sec:model}

\subsection{Problem Formulation}

\subsubsection{Signal Model}

\begin{table*}[t]
\centering
\caption{Summary of Main Notations}
\label{tab:variables}
\renewcommand{\arraystretch}{1.18}
\small
\singlespacing
\begin{tabularx}{\textwidth}{
>{\centering\arraybackslash}p{1.6cm}
>{\raggedright\arraybackslash}X
>{\centering\arraybackslash}p{1.6cm}
>{\raggedright\arraybackslash}X
}
\toprule
\textbf{Symbol} & \textbf{Description} 
& \textbf{Symbol} & \textbf{Description} \\
\midrule

$\mathcal{K}$ 
& Set of ground users
& $\mathcal{M}$ 
& Set of IRSs \\

$K$ 
& Number of ground users
& $M$ 
& Number of IRSs \\

$\mathcal{N}$ 
& Set of IRS reflecting elements
& $N$ 
& Number of reflecting elements \\

$\varphi_k$ 
& IRS-selection variable of user $k$
& $\mathcal{G}$ 
& Set of active transmission groups \\

$\mathcal{K}_g$ 
& Set of users assigned to group $g$
& $P_S$ 
& Maximum transmit power of the satellite \\

$s_k$ 
& Original message intended for user $k$
& $s_{c,g}$ 
& Common stream of group $g$ \\

$s_{k,p,g}$
& Private stream of user $k$ in group $g$
& $\boldsymbol{\Phi}_m$
& Phase-shift matrix of IRS $m$ \\

$w_{c,g}$
& Scalar precoding coefficient for $s_{c,g}$
& $w_{k,p,g}$
& Scalar precoding coefficient for $s_{k,p,g}$ \\

$\mathbf{w}$ 
& Set of scalar precoding coefficients
& $h$ 
& Effective channel coefficient \\

$\theta_{m,n}$ 
& Phase shift of the $n$-th IRS element
& $\mathcal{Q}_{\theta}$ 
& Quantized phase-angle set \\

$B$ 
& Number of phase quantization bits
& $g$ 
& True channel coefficient \\

$\hat{g}$ 
& Estimated channel coefficient
& $\Delta g$ 
& CSI estimation error \\

$x$ 
& Transmitted signal at the satellite
& $y_k$ 
& Received signal at user $k$ \\

$n_0$ 
& Additive white Gaussian noise
& $\sigma^2$ 
& Noise variance \\

$\gamma$ 
& SINR for decoding the corresponding stream
& $R_{k,c,g}$ 
& Common-stream decoding rate of user $k$ \\

$R_{c,g}$ 
& Decodable common rate of group $g$
& $C_{k,g}$ 
& Allocated common rate for user $k$ \\

$R_{k,p,g}$
& Achievable private rate of user $k$ in group $g$
& $\epsilon$
& Atmospheric attenuation coefficient \\

$R_k$ 
& Total achievable rate of user $k$
& $R_{\mathrm{tot}}$ 
& Achievable system sum-rate \\

$R_k^{\min}$ 
& Minimum QoS requirement of user $k$
& 
& \\

$\gamma_{\mathrm d}$
& RL discount factor
& $\lambda_{\mathrm{GAE}}$
& GAE bias--variance parameter \\

$\epsilon_{\mathrm c}$
& PPO clipping range
& $\epsilon_{\mathrm q}$
& QoS-penalty guard constant \\

$\lambda_{D}$
& QoS-penalty weight
& $\beta$
& Entropy regularization coefficient \\

$\alpha_{AE}$
& Autoencoder loss weight
& $G_t$
& GAE $\lambda$-return regression target \\

$\rho_t$
& PPO importance-sampling ratio
& $\hat{A}_t$
& Generalized advantage estimate \\

$V(\cdot)$
& State-value function (critic)
& $\boldsymbol{\eta}$
& VQC trainable parameters \\

$c_{\mathrm s}$
& SPSA perturbation step
& $\tau$
& SoftmaxPQC inverse temperature (head) \\

$\mathbf{s}_t$
& State: affinity feature vector
& $\mathbf{a}_t$
& Action $(\boldsymbol{\varphi},\boldsymbol{\Phi},\mathbf{w},\mathbf{C})$ \\

$\mathbf{z}_t$
& AE latent vector ($\in\mathbb{R}^{2n_q}$)
& $d_{\mathrm{aff}}$
& Affinity dimension $K(M{+}2)+2M$ \\

$n_q$
& Number of qubits
& $L$
& Number of VQC variational layers \\

$\hat{\mathbf{o}}_t$
& VQC measurement (readout) vector
& $N_Q$
& Number of readout observables \\

$\boldsymbol{\lambda}_y, \boldsymbol{\lambda}_z$
& Trainable encoding scales
& $\boldsymbol{\gamma}, \mathbf{b}_{\mathrm{LN}}$
& Encoding LayerNorm learnable affine \\

\bottomrule
\end{tabularx}
\end{table*}

We consider an IRS-assisted grouped rate-splitting multiple access (RSMA) downlink communication system in an integrated satellite-terrestrial network (ISTN). 
In the considered system, a satellite serves a set of ground users with the assistance of intelligent reflecting surfaces (IRSs), which are deployed on high buildings to enhance the wireless propagation environment and support users experiencing weak or blocked direct satellite-to-user links. 
The satellite is assumed to act as the central controller, which determines the transmission strategy, resource allocation, and IRS-assisted communication mode according to the observed channel conditions and user requirements.

Let $\mathcal{K}=\{1,2,\ldots,K\}$ denote the set of downlink ground users, where $K$ is the total number of users. 
The satellite intends to transmit one independent message to each user. 
Specifically, the message intended for user $k \in \mathcal{K}$ is denoted by $s_k$. 
Therefore, the set of scheduled user messages can be written as
\begin{equation}
    s = \{s_1, s_2, \ldots, s_K\}.
\end{equation}

We consider $M$ IRSs deployed on the rooftops of buildings to assist the satellite downlink transmission. 
Let $\mathcal{M}=\{1,2,\ldots,M\}$ denote the set of IRSs. 
Each IRS is equipped with $N$ passive reflecting elements, which can independently adjust the phase shift of the incident signal. 
The IRSs are assumed to be connected to the central controller, and their reflection coefficients are configured according to the channel conditions and user association decisions. For IRS $m \in \mathcal{M}$, the reflection matrix is defined as
\begin{equation}
    \boldsymbol{\Phi}_m
    =
    \operatorname{diag}
    \left(
    e^{j\theta_{m,1}},
    e^{j\theta_{m,2}},
    \ldots,
    e^{j\theta_{m,N}}
    \right),
\end{equation}
where $\theta_{m,n}$ denotes the phase shift of the $n$-th reflecting element at IRS $m$. Let $B$ denote the number of quantization bits. 
Then, each IRS reflecting element can select one phase shift from $2^B$ discrete phase angles. 
The quantized phase-angle set is defined as
\begin{equation}
    \mathcal{Q}_{\theta}
    =
    \left\{
    \theta^{(0)}, \theta^{(1)}, \ldots, \theta^{(2^B-1)}
    \right\},
\end{equation}
where the $q$-th discrete phase angle is given by
\begin{equation}
    \theta^{(q)}
    =
    \frac{2\pi q}{2^B},
    \quad
    q = 0,1,\ldots,2^B-1.
\end{equation}

To determine the transmission mode of each user, we define an IRS-selection variable for every user $k \in \mathcal{K}$. 
Since the considered system includes $M$ IRSs, each user can either be served through the direct satellite-to-user link or through one of the IRS-assisted reflected links. 
Therefore, the IRS-selection variable is defined as
\begin{equation}
    \varphi_k \in \{0,1,\ldots,M\},
    \quad \forall k \in \mathcal{K},
\end{equation}
where
\begin{equation}
    \varphi_k =
    \begin{cases}
        0, & \text{if user } k \text{ is served through the direct satellite-to-user link},\\
        m, & \text{if user } k \text{ is served through the satellite-IRS } m\text{-user link}.
    \end{cases}
\end{equation}

Following the RSMA principle, each user message $s_k$ of user $k \in \mathcal{K}_g$ is split into a common sub-message $s_{c,g}$ and a private sub-message $s_{k,p,g}$.
However, not all IRSs are necessarily selected by the agent at each decision step. 
Therefore, the number of active groups is denoted by $G$, where $G \leq M+1$.
We define the set of scheduled user messages after splitting as

\[
\tilde{\mathbf{s}} = [\,s_{c,1}, s_{c,2}, \ldots, s_{c,G},\; \{s_{k,p,g}\}_{g \in \mathcal{G},\, k \in \mathcal{K}_g}\,].
\]

Due to the long propagation distance between the satellite and ground nodes, the satellite-to-user (SU) and satellite-to-IRS (SR) links are dominated by large-scale path loss and rain attenuation following the ITU-R model~\cite{itu618}.
The IRS-to-user (RU) link additionally exhibits small-scale Rayleigh fading and distance-dependent path loss.
The true channel coefficients of the three links are modeled as
\begin{equation}
\begin{aligned}
    g^{SU}_k
    &=
    \frac{c\sqrt{G_S G_U \epsilon^{SU}_k}}{4\pi f_{SU} d^{SU}_k},
    && k \in \mathcal{K},
    \\[2mm]
    g^{SR}_{m}
    &=
    \frac{c\sqrt{G_S \epsilon^{SR}_{m}}}{4\pi f_{SR} d^{SR}_{m}},
    && m \in \mathcal{M},
    \\[2mm]
    g^{RU}_{m,k}
    &=
    \sqrt{
        G_U \cdot \Omega_{sf}
        \cdot
        \!\left(\frac{d_0}{d^{RU}_{m,k}}\right)^{\!\alpha}
        \cdot\,
        \epsilon^{RU}_{m,k}
    }
    \;\tilde{g}^{sf}_{m,k},
    && m \in \mathcal{M},\ k \in \mathcal{K},
\end{aligned}
\end{equation}
where $c$ is the speed of light; $G_S$ and $G_U$ are the satellite transmit and user receive antenna gains; $f_{SU}$ and $f_{SR}$ are the corresponding carrier frequencies; $d^{SU}_k$, $d^{SR}_{m}$, and $d^{RU}_{m,k}$ denote the satellite-to-user, satellite-to-IRS, and IRS-to-user link distances; $\epsilon^{SU}_k$, $\epsilon^{SR}_{m}$, and $\epsilon^{RU}_{m,k}$ are the rain-attenuation coefficients modeled as lognormal random variables per the ITU-R standard; $\Omega_{sf}$ is the average shadow-fading power gain of the IRS-to-user link; $\alpha$ is the path-loss exponent; $d_0$ is the reference distance; and $\tilde{g}^{sf}_{m,k} \sim \mathcal{CN}(0,1)$ represents the small-scale Rayleigh fading component.
Each channel coefficient is complex, with a random propagation phase uniformly distributed on $[0, 2\pi)$, which is tracked via Doppler compensation at the satellite.

Since the satellite acquires only an imperfect estimate of each channel coefficient, the estimated channel $\hat{g}$ and the true channel $g$ are related by a multiplicative error model:
\begin{equation}
    \hat{g} = g + \Delta g,
    \qquad
    \Delta g = \kappa \lvert g \rvert \varepsilon,
    \qquad
    \varepsilon \sim \mathcal{CN}(0,1),
\end{equation}
where $\kappa \geq 0$ is the relative error coefficient applied uniformly to all three links.
This yields the estimated channels $\hat{g}^{SU}_k$, $\hat{g}^{SR}_{m}$, and $\hat{g}^{RU}_{m,k}$, which are used for all precoding and resource allocation design.
The policy observes only the estimated channel magnitudes $|\hat{g}|$ and designs the assignment and precoding on them, whereas the environment scores the achieved rate on the true channels $g$.
Imperfect CSI therefore manifests as a design-versus-reality gap: the $\hat{g}$-optimal decisions are evaluated against the physical channel $g$.

Since a single-antenna satellite is considered, spatial beamforming is not performed. 
Instead, each RSMA stream is assigned a scalar precoding coefficient. 
For each active group $g \in \mathcal{G}$, let $w_{c,g} \in \mathbb{C}$ denote the scalar precoding coefficient for the group-specific common stream $s_{c,g}$. 
Similarly, let $w_{k,p,g} \in \mathbb{C}$ denote the scalar precoding coefficient for the private stream $s_{k,p,g}$ of user $k \in \mathcal{K}_g$.
Based on the grouped RSMA transmission structure, the transmitted signal at the satellite is expressed as
\begin{equation}
    x
    =
    \sum_{g \in \mathcal{G}} w_{c,g}s_{c,g}
    +
    \sum_{g \in \mathcal{G}}\sum_{k \in \mathcal{K}_g} w_{k,p,g}s_{k,p,g},
\end{equation}
where $s_{c,g}$ is the common stream intended for all users in group $\mathcal{K}_g$, and $s_{k,p,g}$ is the private stream intended only for user $k$ in group $g$.
The scalar precoding coefficients are constrained by the maximum transmit power of the satellite, i.e.,
$\sum_{g \in \mathcal{G}} |w_{c,g}|^2 + \sum_{g \in \mathcal{G}}\sum_{k \in \mathcal{K}_g} |w_{k,p,g}|^2 \leq P_S$,
where $P_S$ denotes the maximum transmit power of the satellite.

Fig.~\ref{fig:rsma_encoder} illustrates the RSMA Encoder structure for a representative group $g \in \mathcal{G}$, which consists of three sequential stages.
In the \emph{Rate Splitting} stage, each user message $s_{k,g}$ is decomposed into a common sub-message $s_{k,c,g}$ and a private sub-message $s_{k,p,g}$.
The common sub-messages of all $|\mathcal{K}_g|$ users are jointly encoded into a single group codeword $s_{c,g}$, while each private sub-message remains exclusive to its intended user.
In the \emph{Scalar Precoding} stage, the common codeword $s_{c,g}$ is scaled by the shared coefficient $w_{c,g}$, and each private stream $s_{k,p,g}$ is independently scaled by the user-specific coefficient $w_{k,p,g}$.
Finally, all precoded streams are superimposed in the \emph{Precoded Streams} stage to form the group signal $x_g$.
The same encoder structure is applied independently to every active group $g \in \mathcal{G}$, and the total transmitted signal aggregates all group contributions: $x = \sum_{g \in \mathcal{G}} x_g$.

\begin{figure}[t]
    \centering
    \includegraphics[width=0.65\linewidth]{images/RSMA_Encoder}
    \caption{RSMA Encoder structure for a representative group $g \in \mathcal{G}$, comprising three stages:
    \emph{Rate Splitting} --- each user message $s_{k,g}$ is decomposed into a common sub-message $s_{k,c,g}$ (jointly encoded into the shared codeword $s_{c,g}$) and a private sub-message $s_{k,p,g}$;
    \emph{Scalar Precoding} --- $s_{c,g}$ and each $s_{k,p,g}$ are independently scaled by $w_{c,g}$ and $w_{k,p,g}$, respectively;
    \emph{Precoded Streams} --- all weighted streams are superimposed to form the group signal $x_g$.
    The overall transmitted signal is $x = \sum_{g \in \mathcal{G}} x_g$.}
    \label{fig:rsma_encoder}
\end{figure}

After receiving $y_k$, each user first decodes its group-specific common stream and then decodes its private stream by applying SIC.
For user $k \in \mathcal{K}_g$, the SINRs for decoding the common stream $s_{c,g}$ and the private stream $s_{k,p,g}$ are respectively given by
\begin{equation}
\begin{aligned}
    \gamma_{k,c,g}
    &=
    \frac{
        |h_k w_{c,g}|^2
    }{
        \sum_{\substack{g' \in \mathcal{G} \\ g' \neq g}}
        |h_k w_{c,g'}|^2
        +
        \sum_{g' \in \mathcal{G}}\sum_{j \in \mathcal{K}_{g'}}
        |h_k w_{j,p,g'}|^2
        +
        \sigma^2
    },
    \\[2mm]
    \gamma_{k,p,g}
    &=
    \frac{
        |h_k w_{k,p,g}|^2
    }{
        \sum_{g' \in \mathcal{G}}\sum_{\substack{j \in \mathcal{K}_{g'} \\ j \neq k}}
        |h_k w_{j,p,g'}|^2
        +
        \sum_{\substack{g' \in \mathcal{G} \\ g' \neq g}}
        |h_k w_{c,g'}|^2
        +
        \sigma^2
    }.
\end{aligned}
\end{equation}
Since the common stream $s_{c,g}$ is intended for all users in group $\mathcal{K}_g$, it must be successfully decoded by every user in that group. 
Therefore, the achievable common rate of group $g$ is limited by the user with the lowest common-stream decoding rate. 
For user $k \in \mathcal{K}_g$, the common-stream decoding rate and the resulting group common rate are given by
\begin{equation}
    R_{k,c,g}
    =
    \log_2\!\left(1 + \gamma_{k,c,g}\right),
    \qquad
    R_{c,g}
    =
    \min_{k \in \mathcal{K}_g} R_{k,c,g}.
\end{equation}

Let $C_{k,g}$ denote the portion of the group common rate allocated to user $k \in \mathcal{K}_g$. 
The common-rate allocation must satisfy
\begin{equation}
    \sum_{k \in \mathcal{K}_g} C_{k,g}
    =
    R_{c,g},
    \quad
    C_{k,g} \geq 0.
\end{equation}

After decoding and removing the common stream, user $k$ decodes its private stream, achieving rate $R_{k,p,g} = \log_2(1 + \gamma_{k,p,g})$.
The total achievable rate of user $k$ is thus $R_k = R_{k,p,g} + C_{k,g}$, and the system sum-rate is
\begin{equation}
    R_{\mathrm{tot}}
    =
    \sum_{k \in \mathcal{K}}
    R_k
    =
    \sum_{g \in \mathcal{G}} R_{c,g}
    +
    \sum_{g \in \mathcal{G}}\sum_{k \in \mathcal{K}_g} R_{k,p,g}.
\end{equation}

\subsubsection{Optimization Problem}

Based on the above system model, the objective of this work is to maximize the achievable system sum-rate by jointly optimizing the IRS-selection variable, scalar precoding coefficients, IRS phase-shift matrices, and common-rate allocation. 
Specifically, the optimization variables are given by
$\boldsymbol{\varphi}=\{\varphi_k\}_{k\in\mathcal{K}}$, 
$\mathbf{w}=\{w_{c,g},w_{k,p,g}\}_{g\in\mathcal{G},k\in\mathcal{K}_g}$,
$\boldsymbol{\Phi}=\{\boldsymbol{\Phi}_m\}_{m\in\mathcal{M}}$, 
and $\mathbf{C}=\{C_{k,g}\}_{k\in\mathcal{K}_g,g\in\mathcal{G}}$. 
The sum-rate maximization problem is formulated as
\begin{subequations}\label{eq:P0}
\begin{align}
    \text{(P0):} \quad
    \max_{\boldsymbol{\varphi},\mathbf{w},\boldsymbol{\Phi},\mathbf{C}}
    \quad
    &
    R_{\mathrm{tot}}
    \label{eq:P0_objective}
    \\
    \text{s.t.} \quad
    &
    \varphi_k \in \{0,1,\ldots,M\},
    \quad \forall k \in \mathcal{K},
    \label{eq:P0_selection}
    \\[1mm]
    &
    \sum_{g \in \mathcal{G}} |w_{c,g}|^2
    +
    \sum_{g \in \mathcal{G}}\sum_{k \in \mathcal{K}_g} |w_{k,p,g}|^2
    \leq P_S,
    \label{eq:P0_power}
    \\[1mm]
    &
    \theta_{m,n} \in \mathcal{Q}_{\theta},
    \quad \forall m \in \mathcal{M}, \forall n \in \mathcal{N},
    \label{eq:P0_phase}
    \\[1mm]
    &
    \boldsymbol{\Phi}_m
    =
    \operatorname{diag}
    \left(
    e^{j\theta_{m,1}},
    e^{j\theta_{m,2}},
    \ldots,
    e^{j\theta_{m,N}}
    \right),
    \quad \forall m \in \mathcal{M},
    \label{eq:P0_phi}
    \\[1mm]
    &
    \sum_{k \in \mathcal{K}_g} C_{k,g}
    = R_{c,g},
    \quad \forall g \in \mathcal{G},
    \label{eq:P0_common_rate}
    \\[1mm]
    &
    C_{k,g} \geq 0,
    \quad \forall k \in \mathcal{K}_g,\ \forall g \in \mathcal{G},
    \label{eq:P0_nonnegative}
    \\[1mm]
    &
    R_k \geq R_k^{\min},
    \quad \forall k \in \mathcal{K}.
    \label{eq:P0_qos}
\end{align}
\end{subequations}

In the above problem (P0),~\eqref{eq:P0_selection} limits the values of the IRS-selection variables, which determine whether each user is served through the direct satellite-to-user link or one of the available IRS-assisted links.~\eqref{eq:P0_power} guarantees that the total transmit power of the satellite does not exceed the maximum power budget.~\eqref{eq:P0_phase} restricts the phase shift of each IRS reflecting element to the discrete quantized phase-angle set, while constraint~\eqref{eq:P0_phi} defines the corresponding passive IRS phase-shift matrix. 
Constraint~\eqref{eq:P0_common_rate} requires that the allocated common rates fully exhaust the group common rate, i.e., the entire decodable common rate of each group is distributed among its members with no waste. Constraint~\eqref{eq:P0_nonnegative} ensures non-negativity of the allocated common rates, and constraint~\eqref{eq:P0_qos} enforces the per-user minimum QoS requirement.  

\section{Proposed HQC-HAC Framework}
\label{sec:framework}

\subsection{Overall Framework}

Problem~(P0) is a mixed-integer non-convex program. The IRS-selection variables $\boldsymbol{\varphi}$ are discrete with a combinatorial feasible set of size $(M+1)^K$; the per-element phase shifts $\theta_{m,n}$ are restricted to the quantized set $\mathcal{Q}_\theta$; and these discrete choices are tightly coupled with the continuous precoding powers $\mathbf{w}$ and common-rate shares $\mathbf{C}$ through the SINR expressions. The objective $R_{\mathrm{tot}}$ is non-concave, since each user's rate depends on the ratio of its effective channel gain to the aggregate cross-stream interference, and the per-user QoS constraints~\eqref{eq:P0_qos} couple all four variable blocks simultaneously. Consequently, (P0) is generally NP-hard~\cite{luo2008dynamic} and admits no closed-form solution. Conventional model-based methods --- semidefinite relaxation~\cite{luo2010semidefinite}, alternating optimization, manifold optimization, or branch-and-bound --- scale poorly with $K$, $M$, and $N$, must be re-solved from scratch for every channel realization, and presuppose accurate instantaneous CSI that is unavailable under the imperfect-CSI and user-mobility conditions considered here. These limitations motivate a model-free reinforcement learning (RL) formulation: rather than re-solving (P0) at every coherence interval, we cast the sequential resource-allocation task as a Markov decision process and learn a policy that maps the observed system state directly to the joint action, amortizing the optimization cost into a trained network that adapts online to channel dynamics and estimation error.

The four decision variables of (P0) exhibit a strict sequential dependency: the assignment $\boldsymbol{\varphi}$ determines which IRS panels are active and thereby fixes the number of active groups $G$ and the dimension of the common-power vector $\mathbf{w}_c$; the active set governs the effective channels that underpin the phase-shift design $\boldsymbol{\Phi}$; the resulting channel gains shape the power-allocation landscape $(\mathbf{w}_p,\mathbf{w}_c)$; and the allocated powers finally delimit the feasible common-rate split $\mathbf{C}$. A flat RL agent acting over the entire joint, and mixed discrete--continuous, action space would face an action dimension that explodes with $K$ and $M$ and would have to learn this coupling implicitly. We instead adopt a hierarchical actor--critic (HAC) decomposition that mirrors the natural ordering of the variables: a high-level actor commits to the discrete assignment, and three low-level actors resolve the continuous variables in sequence, each conditioned on the decisions made upstream.

Even after this decomposition, the high-level assignment $\boldsymbol{\varphi}\in\{0,1,\ldots,M\}^K$ remains the principal source of difficulty: its search space of size $(M+1)^K$ is non-differentiable and grows exponentially in both $M$ and $K$, so a classical policy head would require a rapidly expanding parameter budget to represent a sufficiently expressive distribution over candidate assignments. We therefore place a variational quantum circuit (VQC) at the top of the hierarchy. By preparing an $n_q$-qubit state that spans a $2^{n_q}$-dimensional Hilbert space, the VQC represents rich, correlated distributions over the exponentially large assignment space using only $\mathcal{O}(L\,n_q)$ trainable parameters. Its entangling layers --- a nearest-neighbor CZ chain augmented with cross-block bridges coupling the user-qubit and IRS-qubit registers --- further inject an inductive bias matched to the user--IRS coupling that the assignment must capture. Once $\boldsymbol{\varphi}$ is fixed, the remaining sub-problems are smooth, continuous, and low-dimensional, and are handled efficiently by classical multi-layer perceptrons (MLPs); reserving the quantum circuit for the combinatorial bottleneck is precisely what renders the framework \emph{hybrid} quantum--classical.

A direct quantum encoding of the raw state is, however, infeasible. The raw environment observation has a dimension that scales with the number of reflecting elements $N$ and far exceeds the register size of near-term quantum hardware; moreover, much of it --- such as the full per-element phase configuration --- is only weakly informative for the assignment decision. Encoding it qubit-by-qubit would thus demand an impractically large register. We bridge this gap with an autoencoder (AE) that compresses the state into a compact latent vector $\mathbf{z}_t\in\mathbb{R}^{2n_q}$, exactly matching the encoding capacity of the $n_q$-qubit circuit while preserving the information most relevant to link assignment. A reconstruction loss regularizes this latent space. The AE is given a brief reconstruction-only warm-up so that its latent is informative from the outset. Thereafter, it is trained jointly and end-to-end with the policy, rather than being frozen as an inference-only feature extractor. This ensures the representation remains task-aware and co-adapts with the quantum actor throughout learning.

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{images/HQC-HAC}
    \caption{Overall HQC-HAC hierarchical actor-critic framework. The quantum actor (top level) resolves the discrete IRS-user assignment $\boldsymbol{\varphi}$ via a VQC, while three sequential classical sub-actors --- Phase MLP, Power MLP, and Common-Rate MLP --- optimize the remaining continuous variables conditioned on the chosen assignment. A shared classical critic provides state-value estimates for computing advantages across all actors.}
    \label{fig:framework}
\end{figure}

Fig.~\ref{fig:framework} illustrates the full architecture. At each decision step~$t$, the raw channel observation is condensed into a compact \emph{affinity feature vector} $\mathbf{s}_t\in\mathbb{R}^{d_{\mathrm{aff}}}$ (Eq.~\eqref{eq:affinity}) that encodes the end-to-end user--IRS path qualities, the per-user demands, and per-IRS summary statistics --- the quantities that determine the assignment. A flat real/imaginary channel vector is a poor policy input: its dimension grows with the number of reflecting elements $N$ and it does not expose the user--IRS spatial relationships directly, whereas $\mathbf{s}_t$ makes them explicit while discarding the $N$-dependent bulk. The affinity $\mathbf{s}_t$ is the state on which both the quantum actor (through the AE and VQC) and the shared critic operate; the autoencoder further compresses it to the latent $\mathbf{z}_t\in\mathbb{R}^{2n_q}$ that fits the near-term qubit register.

Following the data flow in Fig.~\ref{fig:framework}, the latent $\mathbf{z}_t$ produced by the AE encoder is mapped by the VQC to a measurement vector, which the quantum actor's output head converts into a categorical distribution over candidate assignments; sampling this distribution yields $\boldsymbol{\varphi}$. Conditioned on $\boldsymbol{\varphi}$, the three low-level sub-actors act in sequence: the Phase MLP produces the quantized phase indices $\boldsymbol{\theta}_{\mathrm{idx}}$ (and hence $\boldsymbol{\Phi}$), the Power MLP allocates the private and common powers $(\mathbf{w}_p,\mathbf{w}_c)$, and the Common-Rate MLP distributes the group common rates $\mathbf{C}$ among users. The resulting joint action $(\boldsymbol{\varphi},\boldsymbol{\Phi},\mathbf{w},\mathbf{C})$ is applied to the environment, which returns the next state and a reward equal to the system sum-rate minus a soft QoS-violation penalty. A single shared critic evaluates the same affinity state $\mathbf{s}_t$ (Eq.~\eqref{eq:affinity}) that the quantum actor consumes, and supplies the advantage signal used to update all four actors.

The framework is trained as an actor--critic system optimized by proximal policy optimization (PPO)~\cite{schulman2017proximal}. The shared state-value critic $V(\mathbf{s}_t;\boldsymbol{\psi})$ provides a low-variance, action-independent baseline for advantage estimation, which is essential here for two reasons: the quantum policy gradients, estimated by simultaneous perturbation stochastic approximation (SPSA)~\cite{spall1992multivariate}, are inherently noisy, and the reward is itself stochastic under per-step noise and imperfect CSI; subtracting a state-only baseline reduces this variance without introducing bias and avoids the self-cancellation that a state--action baseline would incur. PPO is adopted in preference to vanilla policy gradient because its clipped surrogate objective bounds the magnitude of each update, yielding trust-region-like stability that is critical in a hierarchical architecture: a large step in the high-level assignment would shift the input distribution seen by every downstream actor, and, combined with noisy quantum gradients, could readily destabilize training. The importance-sampling ratio underlying PPO additionally permits several optimization epochs over each batch of collected rollouts, improving sample efficiency --- a valuable property given the cost of quantum-circuit evaluations. Entropy regularization with an annealing schedule is applied across all actors to sustain exploration over the combinatorial assignment space and to prevent premature convergence, while the per-user QoS requirement~\eqref{eq:P0_qos} is relaxed into a soft quadratic penalty added to the reward so that demand shortfalls are penalized proportionally without blocking gradient flow. The critic is updated by regression toward a generalized-advantage-estimation (GAE)~\cite{schulman2015high} $\lambda$-return target with PopArt value normalization~\cite{vanhasselt2016popart}, and detailed descriptions of all components --- including the AE, the VQC, and the training procedure --- are provided in Sections~\ref{sec:ae}--\ref{sec:training}.

\subsection{Autoencoder-Based Latent State Representation}
\label{sec:ae}

Directly encoding the raw environment observation into a quantum register is impractical: its dimension $d_s = M(3+2K+2N)+3K$ grows with the number of IRS elements $N$, which is unrelated to the IRS-user assignment decision and introduces irrelevant information into the quantum encoding. Instead, the quantum actor operates on the compact \emph{affinity feature vector} $\mathbf{s}_t \in \mathbb{R}^{d_{aff}}$ of Section~\ref{sec:framework}, which retains only the information pertinent to link assignment, where $d_{aff} = K(M+2) + 2M$. The affinity vector is structured as
\begin{equation}
    \mathbf{s}_t
    =
    \bigl[
      \underbrace{a_{k,m}}_{\substack{K \times M \\ \text{IRS-user} \\ \text{affinity}}},\;
      \underbrace{D_k}_{K},\;
      \underbrace{|\hat{g}^{SU}_k|}_{K},\;
      \underbrace{q_m,\; p_m}_{2M}
    \bigr],
    \label{eq:affinity}
\end{equation}
where $a_{k,m} = |\hat{g}^{SR}_m| \cdot |\hat{g}^{RU}_{m,k}|$ is the end-to-end IRS path quality for user $k$ via IRS $m$, $D_k$ is the per-user QoS demand, $|\hat{g}^{SU}_k|$ is the direct satellite-to-user link quality, $q_m = \frac{1}{K}\sum_{k=1}^{K} a_{k,m}$ is the mean affinity of IRS $m$ across all users, and $p_m = |\hat{g}^{SR}_m|^2$ is the received satellite-to-IRS power. Prior to encoding, a group-wise layer normalization is applied to the affinity, direct-link, and IRS feature blocks, while the demand block $D_k$ is kept unnormalized to preserve the absolute rate-scale information it carries.

A dual-branch encoder then compresses $\mathbf{s}_t$ into the latent vector $\mathbf{z}_t \in \mathbb{R}^{2n_q}$, where $n_q$ is the number of qubits. As illustrated in Fig.~\ref{fig:ae_process}, the encoder consists of two parallel branches that process IRS-level and user-level information separately before concatenation. The \emph{IRS branch} processes the IRS summary statistics $[q_m, p_m]_{m=1}^{M} \in \mathbb{R}^{2M}$ through a two-layer MLP,
\begin{equation}
    \mathbf{z}_{IRS}
    =
    \mathbf{W}_{2}^{IRS}
    \,\sigma\!\bigl(
      \mathbf{W}_{1}^{IRS}[q_1,p_1,\ldots,q_M,p_M]^\top
      + \mathbf{b}_{1}^{IRS}
    \bigr)
    + \mathbf{b}_{2}^{IRS}
    \in \mathbb{R}^{2M},
\end{equation}
where $\mathbf{W}_{1}^{IRS} \in \mathbb{R}^{16 \times 2M}$, $\mathbf{W}_{2}^{IRS} \in \mathbb{R}^{2M \times 16}$, and $\sigma(\cdot)$ denotes the ReLU activation. The \emph{user branch} applies a shared micro-encoder $\phi$ independently to each user $k$,
\begin{equation}
    \mathbf{z}_k
    =
    \mathbf{W}_{2}^{U}
    \,\sigma\!\bigl(
      \mathbf{W}_{1}^{U}[a_{k,1},\ldots,a_{k,M},D_k,|\hat{g}^{SU}_k|]^\top
      + \mathbf{b}_{1}^{U}
    \bigr)
    + \mathbf{b}_{2}^{U}
    \in \mathbb{R}^{2},
    \quad \forall k \in \mathcal{K},
\end{equation}
where $\mathbf{W}_{1}^{U} \in \mathbb{R}^{8 \times (M+2)}$ and $\mathbf{W}_{2}^{U} \in \mathbb{R}^{2 \times 8}$ are shared across all $K$ users. The $K$ per-user embeddings are stacked into $\mathbf{z}_{stack} = [\mathbf{z}_1^\top, \ldots, \mathbf{z}_K^\top]^\top \in \mathbb{R}^{2K}$ and projected to the user-block latent subspace by a linear map $\mathbf{P} \in \mathbb{R}^{2(n_q - M) \times 2K}$, giving $\mathbf{z}_{user} = \mathbf{P}\,\mathbf{z}_{stack} \in \mathbb{R}^{2(n_q - M)}$. The final latent vector concatenates the two branches, $\mathbf{z}_t = [\mathbf{z}_{user} \,\|\, \mathbf{z}_{IRS}] \in \mathbb{R}^{2n_q}$, so that the first $2(n_q - M)$ dimensions carry user-assignment information and the last $2M$ dimensions carry IRS-quality information, matching the qubit partition of the VQC.

\begin{figure}[t]
    \centering
    \includegraphics[width=0.72\linewidth]{images/AE_Process}
    \caption{Dual-branch autoencoder architecture for latent state compression.
    The encoder (top path) processes IRS summary features $[q_m, p_m]$ and per-user affinity features $[a_{k,m}, D_k, |\hat{g}^{SU}_k|]$ through separate branches before concatenating into the latent representation $\mathbf{z}_t \in \mathbb{R}^{2n_q}$, which is forwarded to the quantum actor.
    The decoder (bottom path, active during training only) reconstructs the input feature vector $\hat{\mathbf{s}}_t$ from $\mathbf{z}_t$ and computes the reconstruction loss $\mathcal{L}_{AE} = \frac{1}{2}\|\hat{\mathbf{s}}_t - \mathbf{s}_t\|^2$ to regularize the latent space.}
    \label{fig:ae_process}
\end{figure}

A symmetric decoder mirrors the encoder depth and reconstructs $\hat{\mathbf{s}}_t \in \mathbb{R}^{d_{aff}}$ from $\mathbf{z}_t$, producing the reconstruction loss
\begin{equation}
    \mathcal{L}_{AE}
    =
    \frac{1}{2}
    \bigl\|\hat{\mathbf{s}}_t - \mathbf{s}_{\text{norm},t}\bigr\|^2,
    \label{eq:ae_loss}
\end{equation}
where $\mathbf{s}_{\text{norm},t}$ is the group-normalized input. The decoder is used only during training and is discarded at inference time, where solely the encoder and the latent $\mathbf{z}_t$ are forwarded to the VQC.

The autoencoder is trained jointly and end-to-end with the remainder of the framework: the reconstruction loss in~\eqref{eq:ae_loss} is added to the policy objective as an auxiliary regularizer with weight $\alpha_{AE}$, so that the encoder receives gradients from both $\mathcal{L}_{AE}$ and the reinforcement-learning loss back-propagated through the quantum actor. This contrasts with the common practice of training an autoencoder offline and then freezing it as a fixed feature extractor for inference: here the encoder is never frozen, and continues to adapt throughout the reinforcement-learning phase. End-to-end training is preferred for two reasons. First, reconstruction fidelity is not the true objective: a latent optimized solely to reconstruct its input may retain features irrelevant to link assignment while discarding decision-critical ones, whereas end-to-end training lets the policy gradient shape $\mathbf{z}_t$ to be \emph{task-aware}, emphasizing precisely the information that improves the assignment and the resulting sum-rate. Second, the distribution of visited states is non-stationary: as the policy improves, the states it encounters drift, so a frozen latent would become mismatched to the current policy, whereas a jointly trained encoder co-adapts with the evolving policy and the downstream VQC and sub-actors. The reconstruction term is retained throughout because the reinforcement-learning gradient alone can collapse the latent space to a degenerate representation; $\mathcal{L}_{AE}$ thus acts as a structural regularizer that keeps $\mathbf{z}_t$ informative, with $\alpha_{AE}$ trading off representational fidelity against task performance.

To prevent the encoder from lagging behind the faster-adapting policy networks at the start of training, a short reconstruction-only \emph{warm-up} is performed on states collected under a random policy before joint optimization begins. Without this warm-up, a randomly initialized encoder would emit an uninformative latent in the early episodes, effectively feeding noise into the VQC and retarding the initial learning of all downstream actors. The warm-up mitigates this by supplying a sensible starting representation, placing the AE on a comparable footing with the other components. We emphasize that this step is purely an initialization and \emph{not} an offline pre-training of a frozen feature extractor: once joint optimization begins, the encoder is not frozen but continues to be trained together with the policy, so the latent evolves from this reconstruction-faithful starting point into a task-aware representation.

\subsection{Variational Quantum Circuit}
\label{sec:vqc}

The latent vector $\mathbf{z}_t \in \mathbb{R}^{2n_q}$ from the dual-branch encoder is processed by a variational quantum circuit (VQC) acting on an $n_q$-qubit register, which maps it to a compact feature vector $\hat{\mathbf{o}}_t$. The dimension $2n_q$ supplies one rotation angle per qubit along each of two encoding axes (defined below). By exploiting quantum superposition and entanglement, the VQC represents correlations among the user--IRS affinities with a compact trainable-parameter budget (Section~\ref{sec:res_qvsc}).

\subsubsection{Data Encoding}

Before encoding, the latent vector is layer-normalized~\cite{ba2016layer} to stabilize the input range:
\begin{equation}
    \tilde{\mathbf{z}}_t = \boldsymbol{\gamma}\odot\frac{\mathbf{z}_t - \mu_{z}}{\sigma_{z} + \epsilon} + \mathbf{b}_{\mathrm{LN}},
    \label{eq:ln_z}
\end{equation}
where $\mu_{z}$ and $\sigma_{z}$ are the mean and standard deviation of $\mathbf{z}_t$, and $\boldsymbol{\gamma},\mathbf{b}_{\mathrm{LN}}\in\mathbb{R}^{2n_q}$ are a learnable affine trained jointly with the quantum parameters, restoring the DC component that a plain zero-mean LayerNorm would annihilate. The normalized vector is then mapped to per-qubit rotation angles via trainable encoding scales $\boldsymbol{\lambda}_y, \boldsymbol{\lambda}_z \in \mathbb{R}^{n_q}$:
\begin{equation}
    \alpha_i = \pi \tanh\!\bigl(\lambda^y_i \cdot \tilde{z}_{2i}\bigr),
    \qquad
    \delta_i = \pi \tanh\!\bigl(\lambda^z_i \cdot \tilde{z}_{2i+1}\bigr),
    \quad i = 0,\ldots,n_q-1.
    \label{eq:encoding_angles}
\end{equation}
The $\tanh$ squashes each angle to $(-\pi, \pi)$, preventing gradient vanishing from over-rotation. The trainable scales $\boldsymbol{\lambda}$ allow the circuit to selectively amplify or suppress each latent dimension's influence on the quantum state, and are optimized jointly with the variational parameters via SPSA (detailed in Section~\ref{sec:training}).

\subsubsection{Circuit Architecture}

As illustrated in Fig.~\ref{fig:vqc}, the register is first placed in a uniform superposition by a layer of Hadamard ($H$) gates, after which $L$ layers are applied, each composed of three blocks built only from single- and two-qubit gates. The \emph{encoding block} $U_E$ applies a pair of single-qubit rotations $R_y(\alpha_i)$ and $R_z(\delta_i)$ to every qubit; two axes are used because an $R_y R_z$ pair can steer a qubit to an arbitrary point on the Bloch sphere, so the $2n_q$ latent components are loaded into the quantum state without loss. The \emph{variational block} $U_L$ applies trainable rotations $R_y(\theta^y_i)$ and $R_z(\theta^z_i)$ on each qubit; these are the free parameters that the policy gradient tunes and, together with the entangler, form a universal layered ansatz. The \emph{entangling block} $U_{\rm ent}$ uses controlled-$Z$ (CZ) gates: a nearest-neighbor CZ chain builds correlations beyond product states, augmented by $M$ cross-block CZ bridges. These bridges are the key problem-specific design choice --- since the latent is partitioned so that its first $2(n_q{-}M)$ components carry user information and its last $2M$ carry IRS information, the bridges let the circuit model the user--IRS coupling that governs the assignment, which a within-block chain alone cannot represent. Finally, $U_E$ is re-applied at the start of every layer (\emph{data re-uploading}~\cite{perez2020data}); re-injecting $\mathbf{z}_t$ at each depth enlarges the function class the circuit can express and keeps the input information from being washed out by successive entangling layers. The circuit therefore relies solely on the gates $H$, $R_y$, $R_z$, and CZ, which keeps it hardware-efficient.

\begin{figure}[t]
\centering
\resizebox{0.95\linewidth}{!}{%
\begin{quantikz}[row sep=0.4cm, column sep=0.45cm]
\lstick{$|0\rangle\,q_0$} & \gate{H}
  & \gate{R_y(\alpha_0)}\gategroup[4,steps=2,style={dashed,rounded corners,fill=purple!10},background]{$U_E(\mathbf{z};\boldsymbol{\lambda})$}
  & \gate{R_z(\delta_0)} &
  & \gate{R_y(\theta^{y}_0)}\gategroup[4,steps=2,style={dashed,rounded corners,fill=blue!10},background]{$U_L(\boldsymbol{\theta})$}
  & \gate{R_z(\theta^{z}_0)} &
  & \ctrl{1}\gategroup[4,steps=2,style={dashed,rounded corners,fill=orange!18},background]{$U_{\rm ent}$}
  & \ctrl{3} & \meter{} \rstick{$\langle Z_0\rangle$} \\
\lstick{$|0\rangle\,q_1$} & \gate{H}
  & \gate{R_y(\alpha_1)} & \gate{R_z(\delta_1)} &
  & \gate{R_y(\theta^{y}_1)} & \gate{R_z(\theta^{z}_1)} &
  & \control{} & \qw & \meter{} \rstick{$\langle Z_1\rangle$} \\
\lstick{$\vdots$} & \setwiretype{n}\vdots & \vdots & \vdots & & \vdots & \vdots & & \vdots & \vdots & \\
\lstick{$|0\rangle\,q_{n_q-1}$} & \gate{H}
  & \gate{R_y(\alpha_{n_q-1})} & \gate{R_z(\delta_{n_q-1})} &
  & \gate{R_y(\theta^{y}_{n_q-1})} & \gate{R_z(\theta^{z}_{n_q-1})} &
  & \qw & \control{} & \meter{} \rstick{$\langle Z_{n_q-1}\rangle$}
\end{quantikz}}
\caption{Variational quantum circuit (VQC) of the quantum actor, shown for representative qubits $q_0, q_1, \ldots, q_{n_q-1}$.
After $H^{\otimes n_q}$, each of the $L$ layers applies the encoding block $U_E$ with $\alpha_i = \pi\tanh(\lambda^y_i \tilde{z}_{2i})$ and $\delta_i = \pi\tanh(\lambda^z_i \tilde{z}_{2i+1})$, the variational block $U_L(\boldsymbol{\theta})$, and the entangling block $U_{\rm ent}$ (nearest-neighbor CZ chain plus a cross-block bridge linking the user- and IRS-qubit registers).
The encoding block is re-applied at each layer (data re-uploading).
The Pauli expectations $\langle Z_i\rangle$ and $\langle Z_iZ_j\rangle$ form the measurement vector $\hat{\mathbf{o}}_t$.}
\label{fig:vqc}
\end{figure}

\subsubsection{Measurement and Post-processing}

The quantum state is read out through a structured, action-aligned set of Pauli-$Z$ expectation values,
\begin{equation}
    \hat{\mathbf{o}}_t
    =
    \bigl[\,
    \underbrace{\langle Z_u\rangle}_{u\in\mathcal{U}},\;
    \underbrace{\langle Z_u Z_r\rangle}_{u\in\mathcal{U},\,r\in\mathcal{R}},\;
    \underbrace{\tfrac{1}{|\mathcal{U}|-1}\!\!\sum_{u'\neq u}\!\langle Z_u Z_{u'}\rangle}_{u\in\mathcal{U}},\;
    \underbrace{\langle Z_r\rangle}_{r\in\mathcal{R}}\,\bigr]
    \in [-1,1]^{N_Q},
    \quad N_Q = (n_q{-}M)(M{+}2)+M,
    \label{eq:observables}
\end{equation}
where $\mathcal{U}=\{0,\dots,n_q{-}M{-}1\}$ and $\mathcal{R}=\{n_q{-}M,\dots,n_q{-}1\}$ index the user and IRS qubits, respectively; the four blocks --- per-user $\langle Z_u\rangle$, user--IRS $\langle Z_u Z_r\rangle$, mean user-cluster correlation, and per-IRS $\langle Z_r\rangle$ --- are observables aligned with the user$\to$IRS assignment action. Expectation-value readout is preferred to raw single-shot measurements because it returns a smooth, bounded, real-valued vector that yields stable gradient estimates and keeps the policy output well-conditioned. The single-qubit terms summarize each qubit's marginal state, while the two-qubit terms expose the pairwise correlations created by the entangling block, giving the policy access to the encoded user--IRS coupling at a cost of only $N_Q$ action-aligned features. During training the behavior policy samples actions from $S_{\mathrm{tr}}{=}1500$-shot estimates, while the gradient Jacobian and the PPO importance ratio use the exact statevector expectations (low variance); at inference the policy uses $S_{\mathrm{ev}}{=}3000$-shot estimates.

The readout $\hat{\mathbf{o}}_t$ has a fixed length $N_Q$ that does not match the $K(M{+}1)$ logits the assignment requires, and a shallow circuit exposes only a limited set of observables. A lightweight SoftmaxPQC head $\xi$ --- a single linear map followed by a trainable inverse-temperature $\tau$, after~\cite{jerbi2021parametrized} --- therefore maps the quantum features to the action space (its $\sim\!2$K parameters, versus a $\sim\!50$K ReLU-MLP, underpin the parameter-efficiency result of Section~\ref{sec:res_qvsc}). It first concatenates the readout with the raw latent into $\mathbf{h}_t = [\mathbf{z}_t \,\|\, \hat{\mathbf{o}}_t] \in \mathbb{R}^{2n_q + N_Q}$ as a skip connection, which preserves information that the limited readout may discard, and then outputs a per-user assignment distribution,
\begin{equation}
    \boldsymbol{\pi}_k
    =
    \mathrm{softmax}\!\Bigl(\tau\,\bigl[\mathbf{W}\mathbf{h}_t+\mathbf{b}\bigr]_{k(M{+}1):(k{+}1)(M{+}1)}\Bigr)
    \in \Delta^{M},
    \quad k \in \mathcal{K},
    \label{eq:pi_k}
\end{equation}
over the $M+1$ choices (the direct link or one of the $M$ IRS panels). The assignment is sampled $\varphi_k \sim \boldsymbol{\pi}_k$ during training and taken greedily, $\varphi_k = \arg\max \boldsymbol{\pi}_k$, at evaluation. The VQC parameters $\{\boldsymbol{\lambda}_y, \boldsymbol{\lambda}_z, \boldsymbol{\theta}^{(1)},\ldots,\boldsymbol{\theta}^{(L)}\}$ are trained by SPSA (Section~\ref{sec:training}), and $\xi$ by back-propagation.

\subsection{Entropy-Regularized Hierarchical Actor-Critic Training}
\label{sec:training}

The four actors of Section~\ref{sec:framework} are trained together with a single shared critic in an entropy-regularized actor--critic scheme optimized by proximal policy optimization (PPO)~\cite{schulman2017proximal}. The quantum actor and the three classical sub-actors constitute a \emph{joint hierarchical policy} that factorizes the composite action, while the critic supplies a common advantage signal that couples their updates toward the shared goal of sum-rate maximization under per-user QoS constraints.

\subsubsection{Markov Decision Process Formulation}

We cast the sequential resource-allocation task as a Markov decision process $(\mathcal{S},\mathcal{A},\mathcal{P},r,\gamma_{\mathrm d})$. The \emph{state} $\mathbf{s}_t$ is the affinity feature vector of Eq.~\eqref{eq:affinity}, built from the imperfect channel estimates $\hat{g}$. The \emph{action} is the joint decision produced by the hierarchical policy, $\mathbf{a}_t = (\boldsymbol{\varphi},\,\boldsymbol{\Phi},\,\mathbf{w},\,\mathbf{C})$, where the assignment $\boldsymbol{\varphi}$ is drawn from the quantum actor's distribution~\eqref{eq:pi_k}, and $\boldsymbol{\Phi}$, $\mathbf{w}$, and $\mathbf{C}$ are produced in turn by the phase, power, and common-rate sub-actors conditioned on $\boldsymbol{\varphi}$. Once the action is applied, the environment advances the user positions and channels to yield the next state $\mathbf{s}_{t+1}$.

The \emph{reward} links the optimization objective~\eqref{eq:P0_objective} to the QoS constraint~\eqref{eq:P0_qos} through a soft quadratic penalty,
\begin{equation}
    r_t
    =
    R_{\mathrm{tot}}
    \;-\;
    \lambda_D \sum_{k \in \mathcal{K}}
    \left(
      \frac{\max\!\bigl(0,\; R_k^{\min} - R_k\bigr)}{R_k^{\min} + \epsilon_{\mathrm q}}
    \right)^{\!2},
    \label{eq:reward}
\end{equation}
where $R_k = R_{k,p,g} + C_{k,g}$ is the total achievable rate of user $k$, $\lambda_D>0$ weights the penalty, and $\epsilon_{\mathrm q}$ is a small constant that guards the denominator. The penalty vanishes when every user meets its demand and grows quadratically with the normalized shortfall otherwise, so maximizing $r_t$ steers the policy toward the constrained sum-rate optimum of (P0) without imposing a hard, non-differentiable feasibility boundary. To suppress the variance injected by the per-step noise, $r_t$ is averaged over several independent noise realizations of the same action, and future rewards are discounted by a factor $\gamma_{\mathrm d}\in(0,1)$, distinct from the SINR $\gamma$. Here $R_{\mathrm{tot}}$ and the per-user rates $R_k$ entering the reward and the QoS check are the \emph{achieved} rates on the true channel $g$, whereas all four sub-policies are conditioned on the estimated channel $\hat{g}$; the penalty thus charges the policy for the design-versus-reality gap.

\subsubsection{Learning Objective and Critic}

The hierarchical policy is trained to maximize an entropy-regularized expected return,
\begin{equation}
    \mathcal{J}(\boldsymbol{\Theta})
    =
    \mathbb{E}\!\left[
      \sum_{t} \gamma_{\mathrm d}^{\,t}
      \Bigl( r_t + \beta \sum_{j} H\!\bigl(\pi_j(\cdot\,|\,\mathbf{s}_t)\bigr) \Bigr)
    \right],
    \label{eq:objective}
\end{equation}
where $\boldsymbol{\Theta}$ collects the parameters of all four actors, $H(\pi_j) = -\sum \pi_j \log \pi_j$ is the entropy of actor $j$'s policy, and $\beta>0$ weights an entropy bonus that sustains exploration over the combinatorial assignment space and prevents premature convergence to a deterministic policy. The coefficient $\beta$ is annealed linearly from $\beta_0$ to a floor $\beta_{\min}$ over the early training phase, so that exploration dominates initially and gradually gives way to exploitation. The reward term of~\eqref{eq:objective} drives the policy toward the constrained sum-rate optimum, while the entropy term keeps the search broad.

Optimizing~\eqref{eq:objective} by policy gradient requires an estimate of how advantageous each action is relative to the current policy; this is supplied by a shared critic. The critic $V(\mathbf{s}_t;\boldsymbol{\psi})$ is an MLP that estimates the state value and acts as a variance-reducing baseline. The advantage shared by all actors is obtained by generalized advantage estimation~\cite{schulman2015high},
\begin{equation}
    \hat{A}_t = \sum_{l\ge 0} (\gamma_{\mathrm d}\lambda_{\mathrm{GAE}})^{l}\,\delta_{t+l},
    \qquad
    \delta_t = r_t + \gamma_{\mathrm d}\, V(\mathbf{s}_{t+1};\boldsymbol{\psi}) - V(\mathbf{s}_t;\boldsymbol{\psi}),
    \label{eq:gae}
\end{equation}
where $\lambda_{\mathrm{GAE}}\in[0,1]$ trades bias against variance. Conditioning the baseline on the state alone makes $\hat{A}_t$ a low-variance signal common to the whole hierarchy and avoids the gradient self-cancellation of a state--action baseline. Advantages are standardized across each rollout batch before use. The critic's parameters are fitted by regressing toward the resulting $\lambda$-return target $G_t = \hat{A}_t + V(\mathbf{s}_t;\boldsymbol{\psi})$,
\begin{equation}
    \mathcal{L}_V(\boldsymbol{\psi})
    =
    \bigl(\, G_t - V(\mathbf{s}_t;\boldsymbol{\psi}) \,\bigr)^{2},
    \label{eq:critic_loss}
\end{equation}
with $V(\mathbf{s}_t;\boldsymbol{\psi})$ further stabilized by PopArt value-target normalization~\cite{vanhasselt2016popart}: the network regresses a running-mean/variance-normalized target and its output layer is rescaled at every statistics update to preserve the real-valued prediction, decoupling the learning rate from the reward scale, which otherwise shifts markedly across the $R_{\mathrm{LoS}}$ curriculum stages. No separate target network or Polyak averaging is used; the multi-step GAE bootstrap already yields a sufficiently low-variance regression target given the entropy-regularized, clipped PPO updates.

\subsubsection{Policy Optimization via PPO}

Let $j\in\{\varphi,\Phi,\mathbf{w},\mathbf{C}\}$ index the four actors with policies $\pi_j(\cdot\,|\,\mathbf{s}_t;\boldsymbol{\Theta}_j)$, and let $\rho^{j}_t = \pi_j(a^{j}_t|\mathbf{s}_t)/\pi^{j,\mathrm{old}}(a^{j}_t|\mathbf{s}_t)$ denote the importance ratio between the current and behavior policies. Every actor is updated with the clipped PPO surrogate
\begin{equation}
    \mathcal{L}^{j}_{\mathrm{PPO}}
    =
    \mathbb{E}\Bigl[
      \min\!\bigl(\rho^{j}_t \hat{A}_t,\;
      \mathrm{clip}(\rho^{j}_t,\,1-\epsilon_{\mathrm c},\,1+\epsilon_{\mathrm c})\,\hat{A}_t\bigr)
    \Bigr],
    \label{eq:ppo}
\end{equation}
where $\epsilon_{\mathrm c}$ is the clip range. Clipping bounds each update so that a large change in the high-level assignment cannot abruptly shift the conditioning of the downstream actors, and it permits several optimization epochs over the same batch. Combining the clipped surrogate with the entropy bonus of the objective in~\eqref{eq:objective} and, for the quantum actor, the autoencoder reconstruction loss~\eqref{eq:ae_loss}, the per-actor training objectives become
\begin{equation}
\begin{aligned}
    \mathcal{J}_{\varphi}
    &= \mathcal{L}^{\varphi}_{\mathrm{PPO}} + \beta\,H(\pi_\varphi) - \alpha_{AE}\,\mathcal{L}_{AE}, \\[1mm]
    \mathcal{J}_{j}
    &= \mathcal{L}^{j}_{\mathrm{PPO}} + \beta\,H(\pi_j),
    \qquad j\in\{\Phi,\mathbf{w},\mathbf{C}\}.
\end{aligned}
    \label{eq:actor_obj}
\end{equation}
The quantum encoding scales and variational angles $\boldsymbol{\eta}=\{\boldsymbol{\lambda}_y,\boldsymbol{\lambda}_z,\boldsymbol{\theta}\}$ lie inside the circuit and cannot be differentiated by ordinary back-propagation; their gradients are instead estimated by simultaneous perturbation stochastic approximation (SPSA)~\cite{spall1992multivariate}. Whereas the parameter-shift rule evaluates two circuits \emph{per} parameter --- a cost that scales linearly with $\dim(\boldsymbol{\eta})$ --- SPSA perturbs all parameters at once along a single random direction $\boldsymbol{\Delta}$ with $\Delta_i\in\{-1,+1\}$ and estimates each component from one pair of circuit evaluations,
\begin{equation}
    \hat{J}_{o,i}
    =
    \frac{\hat{o}_o(\boldsymbol{\eta}+c_{\mathrm s}\boldsymbol{\Delta})
          - \hat{o}_o(\boldsymbol{\eta}-c_{\mathrm s}\boldsymbol{\Delta})}
         {2\,c_{\mathrm s}}\,\Delta_i,
    \qquad
    \frac{\partial\mathcal{J}_\varphi}{\partial\eta_i}
    =
    \sum_o \frac{\partial\mathcal{J}_\varphi}{\partial\hat{o}_o}\,\hat{J}_{o,i},
    \label{eq:spsa}
\end{equation}
where $c_{\mathrm s}$ is a small perturbation step: SPSA estimates the Jacobian of the circuit readout $\partial\hat{\mathbf{o}}_t/\partial\boldsymbol{\eta}$ from a single pair of circuit evaluations per Rademacher direction $\boldsymbol{\Delta}$ --- a cost independent of $\dim(\boldsymbol{\eta})$ --- and averages the estimate over a few repetitions to reduce its variance; the head, entropy, and autoencoder-reconstruction gradients are then obtained by exact back-propagation through $\partial\mathcal{J}_\varphi/\partial\hat{\mathbf{o}}_t$. This requires roughly $6\times$ fewer circuit evaluations per update than the parameter-shift rule; the price is a higher-variance gradient, which the critic baseline and the conservative PPO clipping are well placed to absorb. The classical encoder and post-processing head of the quantum actor, all three sub-actors, and the critic are updated by standard back-propagation, and every module uses the Adam optimizer with per-module learning rates and gradient-norm clipping.

\subsubsection{Training Procedure}

Training proceeds through repeated rollout-and-update cycles. \emph{(i)~Rollout:} the current policy is executed for a fixed number of episodes, and each transition $(\mathbf{s}_t,\mathbf{a}_t,r_t,V_t)$ is stored together with the behavior log-probabilities $\log\pi^{j,\mathrm{old}}$ of all four actors. \emph{(ii)~Targets:} the stored values and the next-state bootstrap are used to compute the GAE advantages and $\lambda$-return targets~\eqref{eq:gae}--\eqref{eq:critic_loss}, after which the advantages are standardized over the batch. \emph{(iii)~Update:} for several epochs the buffer is shuffled into mini-batches and, for each mini-batch, the four actor objectives~\eqref{eq:actor_obj} and the critic loss~\eqref{eq:critic_loss} are evaluated and their parameters updated; the importance ratios in~\eqref{eq:ppo} make these repeated passes both stable and sample-efficient. \emph{(iv)~Anneal:} the PopArt value-normalization statistics are refreshed and the entropy coefficient and learning rates are decayed on their schedules. The autoencoder warm-up of Section~\ref{sec:ae} is performed once before this loop begins, and the cycle repeats until convergence. The hyperparameter values used in our experiments are reported in Section~\ref{sec:results}.

\section{Experiment}
\label{sec:results}

\subsection{Simulation Setup}

We evaluate the proposed HQC-HAC framework on the IRS-assisted grouped RSMA downlink of Section~\ref{sec:model}: a LEO satellite ($1.58$~GHz, $800$~km) serves $K$ single-antenna users aided by $M$ rooftop-mounted IRS panels of $N=24$ reflecting elements with $2$-bit phase quantization. In each episode the panels and users are spawned within the LoS coverage disk, with a fraction of users placed inside building footprints where the direct link is blocked and only an IRS can restore coverage --- the worst-user bottleneck the framework must resolve. Users then move by random walk at $1.5$~m/s while the three ITU-R links, rain attenuation, and additive noise are re-sampled at every step, and the agent observes only imperfect CSI ($\kappa=0.05$). Each episode runs $200$ steps under the per-user demand $R_k^{\min}=0.1$~bps/Hz. Three topologies scale the problem: $(K,M,P_S)=(5,1,50~\mathrm{dBm})$, $(10,2,50~\mathrm{dBm})$, and $(15,3,50~\mathrm{dBm})$, with VQC depth $L=2/3/5$, respectively; all remaining settings are listed in Table~\ref{tab:hyper}.

\begin{table*}[t]
\centering
\caption{Simulation and Training Hyperparameters}
\label{tab:hyper}
\renewcommand{\arraystretch}{1.15}
\small
\singlespacing
\begin{tabular}{@{}l l @{\hskip 3em} l l@{}}
\toprule
\textbf{Parameter} & \textbf{Value} & \textbf{Parameter} & \textbf{Value} \\
\midrule
\multicolumn{2}{@{}l}{\emph{System, channel, and quantum actor}} & \multicolumn{2}{l}{\emph{Reinforcement learning and training}} \\
Users $K$ / IRS $M$ (Case 1/2/3) & $(5,1)$ / $(10,2)$ / $(15,3)$ & Discount factor $\gamma_{\mathrm d}$ & $0.95$ \\
Elements per IRS $N$ & $24$ & GAE parameter $\lambda_{\mathrm{GAE}}$ & $0.9$ \\
Satellite power $P_S$ (Case 1/2/3) & $50$ / $50$ / $50$ dBm & PPO clip $\epsilon_{\mathrm c}$ / epochs & $0.2$ / $6$ \\
Carrier frequency $f$ / altitude $h_{SR}$ & $1.58$ GHz / $800$ km & Rollout episodes / mini-batch & $12$ / $64$ \\
Antenna gains $G_S, G_U$ & $60$ dBi & Entropy $\beta_0 \rightarrow \beta_{\min}$ & $10^{-3} \rightarrow 3\times10^{-4}$ \\
Noise $n_0$ (mean, variance) & $(-30,\ 10)$ dBW & QoS penalty $\lambda_D$ / guard $\epsilon_{\mathrm q}$ & $1.5$ / $10^{-3}$ \\
Rain attenuation ($\mu$, $\sigma^2$) & $(4\ \mathrm{dB},\ 0.1)$ & Reward noise averaging & $16$ \\
CSI error coefficient $\kappa$ & $0.05$ & LR (AE $\omega$ / VQC $\boldsymbol{\eta}$ / head $\xi$) & $10^{-4}$ / $10^{-4}$ / $3\times10^{-4}$ \\
Phase quantization bits $B$ & $2$ ($4$ levels) & LR (phase,\,power / $C_k$ / critic) & $10^{-4}$ / $5\times10^{-5}$ / $3\times10^{-4}$ \\
QoS demand $R_k^{\min}$ & $0.1$ bps/Hz & Episodes per curriculum stage / steps & $2000$ / $200$ \\
LoS radius $R_{\mathrm{LoS}}$ / IRS height & $0.2$--$0.5$ km (curriculum) / $20$ m & Optimizer & Adam \\
User speed & $1.5$ m/s & Gradient-norm clip (AE / actors) & $0.1$ / $0.5$ \\
Qubits $n_q$ / latent dim $2n_q$ & $12$ / $24$ & & \\
Variational layers $L$ (Case 1/2/3) & $2$ / $3$ / $5$ & & \\
Data re-uploading & enabled & & \\
SPSA step $c_{\mathrm s}$ / repetitions & $0.1$ / $16$ & & \\
Measurement shots $S$ (train / eval) & $1500$ / $3000$ & & \\
Autoencoder loss weight $\alpha_{AE}$ & $0.5$ & & \\
\bottomrule
\end{tabular}
\end{table*}

\emph{Training protocol.} Training follows Section~\ref{sec:training} under a blockage curriculum: the LoS radius $R_{\mathrm{LoS}}$ is raised from $0.2$ to $0.5$~km in stages of $2000$ episodes, each stage warm-starting from the previous one (dotted lines in Figs.~\ref{fig:reward_log_case1}--\ref{fig:reward_log_case2}). Before each update, $12$ rollout episodes are collected; the four actors and the critic are then optimized for $6$ PPO epochs on mini-batches of $64$ transitions, with the entropy coefficient annealed over the first $10\%$ of episodes. Quantum parameters are updated by SPSA and all classical modules by Adam, with per-module gradient-norm clipping; the autoencoder warm-up precedes joint training.

\emph{Evaluation protocol and baselines.} The learned policy is executed greedily ($\varphi_k = \arg\max\boldsymbol{\pi}_k$, $3000$ shots) over $50$ held-out episodes of $200$ steps with a fixed evaluation seed. We report the system sum-rate $R_{\mathrm{tot}}$ as the primary metric, together with the QoS-satisfaction rate --- the fraction of users meeting $R_k^{\min}$. HQC-HAC is compared against four baselines sharing the same grouped-RSMA signal model and differing only in the assignment policy: (i) \emph{greedy}, which selects for each user the strongest estimated link; (ii) \emph{direct-only}, which disables the IRS; (iii) \emph{all-IRS}, which routes every user through an IRS; and (iv) \emph{random} assignment. Robustness to CSI error and receiver noise is assessed zero-shot, re-evaluating the trained policy under test-time $\kappa$ and $\sigma^2$ overrides without retraining.

\subsection{Performance Analysis}

% ============================================================================
% ||  B.1  Sum-rate & QoS vs baselines   (PART 1 — headline result)         ||
% ============================================================================
\subsubsection{Sum-Rate and QoS Against Baselines}
\label{sec:res_baselines}

Figs.~\ref{fig:reward_log_case1}--\ref{fig:reward_log_case2} show the training trajectories across the blockage curriculum, and Tables~\ref{tab:case1_main}--\ref{tab:case2_main} the corresponding greedy-inference results. The learned assignment outperforms every fixed baseline on both metrics: HQC-HAC exceeds the strongest baseline (Greedy) by $19.0\%$ in sum-rate in Case~1 and $11.3\%$ in Case~2, at equal or better QoS, and the QoS separation widens with scale ($+0.8$\,pp at $K=5$ versus $+5.3$\,pp at $K=10$). The blind policies illustrate why the assignment is the decisive variable: Direct-only strands the blocked users, All-IRS strands those with a strong direct link, and both --- like Random --- collapse to $7.6$--$59.0\%$ QoS with large negative rewards, whereas the learned policy sustains $\geq 97\%$ QoS while serving all $K$ users.

% --- Two-panel symmetric layout: left = Case 1 (curve + table), right = Case 2 ---
\begin{figure*}[t]
\centering
% ===== LEFT: CASE 1 =====================================================
\begin{minipage}[t]{0.49\textwidth}
  \centering
  \includegraphics[width=\linewidth]{figures/reward_log_case1}
  \captionof{figure}{Case~1 ($K{=}5$, $M{=}1$) training trajectories of HQC-HAC (VQC) vs.\ the classical DNN actor over the full $R_{\mathrm{LoS}}$ curriculum ($0.2\!\to\!0.5$); dotted lines mark ramp transitions.}
  \label{fig:reward_log_case1}

  \vspace{6pt}
  \captionof{table}{Case~1 ($K=5$, $M=1$, $P_S=50$\,dBm): greedy-inference comparison against baselines. VQC and DNN each at its sweet-spot checkpoint (ep~1900 and ep~1100, respectively); $50$ episodes $\times\,200$ steps, seed~42, eval env $R_{\mathrm{LoS}}=0.5$ (full-unlock spawn geometry).}
  \label{tab:case1_main}
  \footnotesize
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{lccc}
  \toprule
  Policy & Reward & $R_{\mathrm{tot}}$ & QoS \\
  \midrule
  HQC-HAC     & $354.68$ & $1.7748$ & $99.8\%$ \\
  DNN         & $359.98$ & $1.8196$ & $94.9\%$ \\
  Greedy      & $295.42$ & $1.5062$ & $98.3\%$ \\
  All-IRS     & $-415.67$ & $0.7304$ & $57.6\%$ \\
  Direct-only & $-129.24$ & $0.8345$ & $51.3\%$ \\
  Random      & $-448.93$ & $0.5551$ & $30.2\%$ \\
  \bottomrule
  \end{tabular}
\end{minipage}
\hfill
% ===== RIGHT: CASE 2 ====================================================
\begin{minipage}[t]{0.49\textwidth}
  \centering
  \includegraphics[width=\linewidth]{figures/reward_log_case2}
  \captionof{figure}{Case~2 ($K{=}10$, $M{=}2$) training trajectories over the full curriculum ($0.2\!\to\!0.5$); dotted lines mark ramp transitions.}
  \label{fig:reward_log_case2}

  \vspace{6pt}
  \captionof{table}{Case~2 ($K=10$, $M=2$, $P_S=50$\,dBm): greedy-inference comparison.}
  \label{tab:case2_main}
  \footnotesize
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{lccc}
  \toprule
  Policy & Reward & $R_{\mathrm{tot}}$ & QoS \\
  \midrule
  HQC-HAC     & $294.91$ & $1.5300$ & $97.3\%$ \\
  DNN         & $280.03$ & $1.5181$ & $95.4\%$ \\
  Greedy      & $215.28$ & $1.3752$ & $92.0\%$ \\
  All-IRS     & $-1852.63$ & $0.3871$ & $27.2\%$ \\
  Direct-only & $-990.09$ & $0.6867$ & $39.4\%$ \\
  Random      & $-1938.94$ & $0.3037$ & $7.6\%$ \\
  \bottomrule
  \end{tabular}
\end{minipage}
\end{figure*}

% ============================================================================
% ||  B.2  Quantum vs classical actor   (PART 2 — A1 param-eff + Pareto)    ||
% ============================================================================
\subsubsection{Quantum versus Classical Actor: Parameter Efficiency and the Pareto Frontier}
\label{sec:res_qvsc}

To isolate the contribution of the quantum actor, the VQC block is replaced by a classical MLP of identical role, input, and output, trained under the same recipe (Tables~\ref{tab:case1_main}--\ref{tab:case2_main}, row DNN). The two actors reach sum-rates within $3\%$ of each other but settle on two distinct operating points of the same trade-off: the VQC leans toward QoS ($+4.9$\,pp in Case~1, $+1.9$\,pp in Case~2), the DNN toward raw sum-rate, so neither dominates the other --- consistent with the prior that parametrized quantum policies match, rather than exceed, classical ones in task performance~\cite{jerbi2021parametrized}. The separation is in parameter efficiency: the VQC assignment actor attains this operating point with $120$/$144$ trainable parameters against $40{,}586$/$43{,}166$ for the MLP (Table~\ref{tab:param_eff}) --- a $338\times$/$300\times$ reduction --- with the VQC count growing only linearly in the register size (Appendix~\ref{sec:ablation}). In Case~2 the VQC additionally traverses the curriculum transitions with visibly smaller transient drops (Fig.~\ref{fig:reward_log_case2}).

\begin{table}[t]
\centering
\caption{Assignment-actor parameter count: VQC SoftmaxPQC vs.\ classical MLP (phase/power/$C_k$ sub-actors identical in both).}
\label{tab:param_eff}
\begin{tabular}{lccc}
\toprule
Actor & Case~1 & Case~2 & Case~3 \\
\midrule
HQC-HAC (VQC)   & $120$    & $144$    & \emph{(pending)} \\
DNN (MLP)       & $40{,}586$ & $43{,}166$ & \emph{(pending)} \\
Ratio DNN/VQC   & $338\times$ & $300\times$ & \emph{(pending)} \\
\bottomrule
\end{tabular}
\end{table}

% ============================================================================
% ||  B.3  Robustness to system scale & channel   (sensitivity sweeps)      ||
% ============================================================================
\subsubsection{Robustness to System Scale and Channel Conditions}
\label{sec:res_robust}

Table~\ref{tab:nsweep} varies the reflecting-element count: across $N=20$--$36$ the achievable sum-rate stays within $1.71$--$1.75$~bps/Hz at $\geq 99.4\%$ QoS, with no monotone trend in $N$. The passive aperture gain benefits the Greedy baseline identically, so the agent's margin remains at $+13$--$17\%$ throughout: at $P_S=50$~dBm the system is power- rather than aperture-limited, and enlarging the IRS is not the binding lever. Table~\ref{tab:pssweep} instead sweeps the satellite transmit power over $P_S=40$--$70$~dBm: the sum-rate climbs monotonically from $1.41$ to $2.45$~bps/Hz, while QoS saturates near $99.7\%$ for $P_S\geq 50$~dBm and drops to $91.6\%$ at $40$~dBm as the link turns power-limited. The same sweep delimits the value of the IRS --- at $P_S=70$~dBm the direct link alone already serves every user (Direct-only QoS $100\%$), rendering the reflecting surface redundant, whereas at $40$~dBm Direct-only satisfies only $41.5\%$ of users and the IRS becomes the decisive resource.
Table~\ref{tab:noisesweep} evaluates the trained Case-2 policy zero-shot under degraded channel knowledge: quadrupling the CSI error ($\kappa\!=\!0.05\!\to\!0.20$) costs only $0.3$\,pp QoS and $0.2\%$ sum-rate, and raising the receiver-noise variance from $10$ to $16$~dB$^2$ costs $1.0$\,pp QoS, while the lead over Greedy ($\approx\!5$\,pp QoS) is preserved at every noise level --- the policy degrades gracefully without retraining.

\begin{table}[t]
\centering
\caption{Comparison of the achievable sum-rate under various IRS-element counts $N$ on $K=5, M=1$}
\label{tab:nsweep}
\begin{tabular}{lccc}
\toprule
$N$ & $R_{\mathrm{tot}}$ & QoS & Margin vs.\ Greedy \\
\midrule
$20$ & $1.7141$ & $99.4\%$ & $+16.5\%$ \\
$24$ & $1.7465$ & $99.7\%$ & $+16.0\%$ \\
$28$ & $1.7483$ & $99.8\%$ & $+14.8\%$ \\
$32$ & $1.7064$ & $99.5\%$ & $+13.2\%$ \\
$36$ & $1.7529$ & $99.6\%$ & $+15.8\%$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]
\centering
\caption{Comparison of the achievable sum-rate under various satellite transmit power levels $P_S$ on $K=5, M=1$}
\label{tab:pssweep}
\begin{tabular}{lcc}
\toprule
$P_S$ (dBm) & $R_{\mathrm{tot}}$ & QoS \\
\midrule
$40$ & $1.4065$ & $91.6\%$ \\
$50$ & $1.7465$ & $99.7\%$ \\
$60$ & $1.8560$ & $99.9\%$ \\
$70$ & $2.4518$ & $99.5\%$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]
\centering
\caption{Comparison of the achievable sum-rate under various CSI error coefficients $\kappa$ and receiver-noise variances $\sigma^2$ on $K=10, M=2$ (zero-shot, no retraining)}
\label{tab:noisesweep}
\begin{tabular}{lcc}
\toprule
Noise level & $R_{\mathrm{tot}}$ & QoS \\
\midrule
base ($\kappa{=}0.05$) & $1.5300$ & $97.3\%$ \\
$\kappa = 0.10$ & $1.5291$ & $97.2\%$ \\
$\kappa = 0.20$ & $1.5263$ & $97.0\%$ \\
$\sigma^2 = 13$ & $1.5270$ & $96.8\%$ \\
$\sigma^2 = 16$ & $1.5241$ & $96.3\%$ \\
\bottomrule
\end{tabular}
\end{table}


\section{Discussion}
\label{sec:discussion}

The results position HQC-HAC as a \emph{feasibility and efficiency} contribution rather than a claim of quantum task-advantage. On the multi-IRS grouped-RSMA assignment problem the variational quantum actor reaches the sum-rate and QoS of a classical deep-network actor to within $3\%$ (Section~\ref{sec:res_qvsc}) using two orders of magnitude fewer parameters, and --- unlike the per-state convex-optimization pipeline of Tan~et~al.~\cite{tan2025flexible} --- amortizes its cost into a single offline training run, after which each channel realization is served by one forward pass. This is the honest reading advocated throughout: parametrized quantum policies match, rather than exceed, classical ones on natural control tasks~\cite{jerbi2021parametrized}, so the value here lies in the parameter budget and amortized deployment, not in the achievable objective.

The ablations delineate which design choices carry this result and which are inert. The autoencoder is a genuine \emph{scaling enabler}: without it the affinity vector would demand $22$ and $41$ qubits at Cases~2 and~3 (Table~\ref{tab:abl_ae_params}), placing the state-vector simulation with per-step SPSA gradients out of reach, whereas the fixed $12$-qubit register keeps the problem tractable at a reconstruction cost that grows only with the compression ratio (Fig.~\ref{fig:ae_trajectory}). Two ingredients that are load-bearing in other quantum-policy settings are, by contrast, \emph{removable} here: the trainable encoding scales $\boldsymbol{\lambda}$ can be left at their initial values at no measurable cost --- not because the scales are irrelevant, but because the surrounding network co-adapts so that the frozen value already sits at the optimum of the rescaling direction (Appendix~\ref{sec:abl_frozen}) --- and the readout's internal structure is not the performance bottleneck. The hierarchical decomposition is decisive: an oracle-intervention ladder places assignment far above phase, power, and common-rate as the dominant lever, which the sequential factorization isolates cleanly.

The system-level sweeps clarify the operating regime. The agent's margin over the strongest baseline is invariant to the reflecting-element count $N$ at fixed power, indicating a power- rather than aperture-limited regime; the transmit-power sweep makes this explicit, with the IRS contribution vanishing once the direct link alone satisfies every user at high $P_S$. The trained policy also degrades gracefully under zero-shot CSI-error and receiver-noise perturbations, retaining its lead without retraining.

Several limitations qualify these findings. The reported comparisons use a single training seed and a fixed classical configuration; a multi-seed, iso-performance grid would tighten the parameter-efficiency claim. The largest case ($K{=}15$) is characterized only structurally, and the depth, entangling-bridge, and larger-scale readout ablations remain to be run. Finally, the inference cost is an algorithmic per-state complexity measured in simulation, not a wall-clock result on quantum hardware.

\section{Conclusion}
\label{sec:conclusion}

We presented HQC-HAC, a hybrid quantum--classical hierarchical actor--critic for sum-rate maximization in IRS-assisted grouped-RSMA integrated satellite--terrestrial networks. The formulation casts the joint assignment, phase, power, and common-rate problem as a sequential dependency chain under QoS, discrete-phase, blockage, and imperfect-CSI constraints; a variational quantum circuit resolves the combinatorial $(M{+}1)^K$ assignment while three classical sub-actors handle the factorable low-level controls, and an autoencoder compresses the state into a NISQ-sized register. Across two problem scales the method exceeds the strongest heuristic baselines and matches a classical deep-network actor to within $3\%$ using $338\times$/$300\times$ fewer assignment-actor parameters, while amortizing into a single offline training run the per-state solve cost of prior convex-optimization approaches. Ablations attribute the result to the hierarchical decomposition and the autoencoder compression and honestly identify the encoding scales and readout structure as removable degrees of freedom in this task. Future work includes the full $K{=}15$ case, multi-seed and iso-performance comparisons, the remaining architectural sweeps, and evaluation on quantum hardware.

\bibliographystyle{IEEEtran}
\bibliography{reference}

\appendices
\section{Group-Composition Capacity of an IRS}
\label{sec:abl_capacity}

\emph{Setup.} Consider one IRS serving two user groups: group~0 (direct transmission) with
$A$ users and group~1 (IRS-assisted transmission) with $B$ users.
We analyze the incremental effect of adding one free user to the IRS group,
i.e., re-assigning a user from group~0 to group~1.
The power allocation and beamforming weights are assumed fixed throughout the
re-assignment, so that the resulting
$\Delta R_{\mathrm{tot}}$ isolates the pure effect of group composition.
User rates are expressed as functions of the effective SNR,
$R_p(\Gamma)$ and $R_c(\Gamma)$, where
$\Gamma = |h|^2P_S/\sigma^2$:

\begin{equation}
\Delta R_{\mathrm{tot}}(a) =
\underbrace{R_p(\Gamma_a^{\mathrm{IRS}}) - R_p(\Gamma_a^{\mathrm{dir}})}_{\text{(I) private re-route}}
+ \underbrace{R_{c,A}^{\mathrm{new}} - R_{c,A}^{\mathrm{old}}}_{\text{(II) direct-group relief}}
+ \underbrace{\min\!\big(R_{c,B}^{\mathrm{old}},\, R_c(\Gamma_a^{\mathrm{IRS}})\big) - R_{c,B}^{\mathrm{old}}}_{\text{(III) common-rate degradation}}
\label{eq:abl_dRtot}
\end{equation}

\begin{itemize}
\item \textbf{(I):}
$a$'s private rate on the IRS
route (after) minus on the direct route (before).
  \begin{itemize}
  \item $>0$: the reflected path offers the higher private rate (the $N^2$ cascade gain outweighs
  the direct link), favoring re-assignment. For a \emph{blocked} user $\Gamma_a^{\mathrm{dir}}
  \approx 0$, so (I) is always dominant-positive: routing blocked users to their IRS is
  monotonically beneficial, with no threshold in $A$.
  \item $<0$: the direct link offers the higher private rate; the re-assignment loses private
  rate and must be justified by (II).
  \end{itemize}
\item \textbf{(II):} the direct-group common rate after minus before $a$ leaves.
  \begin{itemize}
  \item $=0$: $a$ was not the minimum-SINR member of group~0, so the group minimum --- and hence
  its common rate --- is unchanged.
  \item $>0$: $a$ was the worst --- the common rises to the next-worst member.
  \item $<0$: only when $a$ is the \emph{last} member of group~0: the group vanishes and its
  common stream is forfeited, $(\mathrm{II}) = -R_c(\Gamma_{\mathrm{dir}})$ (specific to fixed
  power weights; predicted $0.141$~bps/Hz against $0.137$ simulated at $P_S{=}50$~dBm).
  \end{itemize}
\item \textbf{(III):}
the IRS-group common rate after minus before $a$ joins. Adding a member can only keep or
lower a $\min$-SINR common rate, so $(\mathrm{III}) \le 0$.
  \begin{itemize}
  \item $=0$: the admitted user does not become the minimum-SINR member of the IRS group; the
  common rate is unchanged.
  \item $<0$: the admitted user becomes the new group minimum; the common rate drops to
  $R_c(\Gamma_a^{\mathrm{IRS}})$, reducing the rate of every member of the group simultaneously.
  \end{itemize}
\end{itemize}

\emph{Marginal of admitting free users.} Under representative two-point SNRs
($\Gamma_B < \Gamma_A$ deterministic), the first admission carries a one-time common-rate
reduction, and every later admission is neutral to the group minimum:

\begin{equation}
    \Delta_{0\to1} = \big[R_p(\Gamma_B) - R_p(\Gamma_{\text{dir}})\big]
+ \underbrace{\big[R_c(\Gamma_B) - R_c(\Gamma_A)\big]}_{\text{one-time common-rate reduction}}.
\end{equation}

Under per-user fading, however, neither the single reduction nor the absence of later ones is
guaranteed --- both are order-statistics events of the group minimum. In expectation,

\begin{equation}
    E[\Delta_{B\to B+1}] =
\underbrace{E\big[R_p(\Gamma_{B+1})\big] - R_p(\Gamma_{\text{dir}})}_{\text{private (own fading realization)}}
+ \underbrace{E\big[R_c(m_{B+1})\big] - E\big[R_c(m_B)\big]}_{<0,\ \downarrow 0\ \text{(common-rate reduction)}},
\end{equation}

where $m_B$ denotes the minimum effective SNR among the $A+B$ group members (characterized below).
The reduction term is negative but vanishes as $B$ grows, so $E[\Delta_{B\to B+1}]$
\emph{increases} with $B$: under i.i.d.\ member fading the cumulative sum-rate is convex in $B$
and its optimum lies at a boundary ($B{=}0$ or the maximal $B$); an interior optimum arises only
when the candidates are heterogeneous and admitted in decreasing order of mean cascade SNR. The
sum-rate carrying capacity is

\begin{equation}
    B^*_{\mathrm{rate}}(A) = \arg\max_{B}\ R_{\mathrm{tot}}(A, B).
\end{equation}

\emph{Order statistics of the group minimum.} Under Rayleigh fading of the IRS--user link, the
cascade SNR of member $j$ factorizes into a deterministic mean (set by the geometry) and a unit
exponential draw:

\begin{equation}
    \Gamma_j = \bar\Gamma_j\, X_j,\qquad X_j \sim \mathrm{Exp}(1)\ \text{i.i.d.},
\end{equation}

so the group minimum is itself exponential --- exactly, for heterogeneous means:

\begin{equation}
    m \sim \mathrm{Exp}(\Lambda),\quad
\Lambda = \sum_{i\in A}\frac{1}{\bar\Gamma_{A,i}} + \sum_{j\in B}\frac{1}{\bar\Gamma_{B,j}},
\qquad
E[m] = \frac{1}{\Lambda},
\qquad
P\big(m = \Gamma_k\big) = \frac{1/\bar\Gamma_k}{\Lambda},
\end{equation}

and every expectation above admits a closed form via the exponential integral $E_1$:

\begin{equation}
    \mathbb{E}\big[\log_2(1 + c\,X)\big] = \frac{e^{\lambda/c}\,E_1(\lambda/c)}{\ln 2},
\qquad X \sim \mathrm{Exp}(\lambda).
\end{equation}

The probability that an admitted user becomes the group minimum --- i.e., that the common rate is
reduced --- is, for the first admission,

\begin{equation}
    P\big(\Gamma_b < \min_{i\in A}\Gamma_i\big)
= \frac{1/\bar\Gamma_B}{1/\bar\Gamma_B + A/\bar\Gamma_A}
= \frac{\bar\Gamma_A}{\bar\Gamma_A + A\,\bar\Gamma_B} \;<\; 1
\end{equation}

and for the $(B{+}1)$-th admission,

\begin{equation}
    P\big(\Gamma_b < \min_{i\in A,B}\Gamma_i\big)
= \frac{1/\bar\Gamma_B}{(B{+}1)/\bar\Gamma_B + A/\bar\Gamma_A}
\;\xrightarrow{\ \bar\Gamma_A \gg \bar\Gamma_B\ }\; \frac{1}{B+1}.
\end{equation}

\emph{QoS bound.} The common-rate shares $C_k$ are decision variables, so a group is feasible iff
its common budget covers the total shortfall $\varsigma_k = \big(R_k^{\min} - R_p(\Gamma_k)\big)^{+}$ ---
exact, with no assumption on how the common rate is shared:

\begin{equation}
    B^*_{\text{QoS}}(A) = \max\Big\{B :\ \textstyle\sum_{k=1}^{A+B} \varsigma_k \le R_c\big(\Gamma_{\min}(B)\big)\Big\}
\end{equation}

(note the double effect of $B$: the shortfall mass grows while the budget $R_c(\Gamma_{\min}(B))$
shrinks, since the minimum is taken over more members). Equal sharing ($C_k = R_c/n$) yields the
conservative special case

\begin{equation}
    B^*_{\text{QoS}} = \lfloor n^*_{\mathrm{mix}} \rfloor - A,
\qquad
n^*_{\mathrm{mix}} = \frac{R_c(\Gamma_{\min})}{R_k^{\min} - R_p(\Gamma_{\mathrm{wk}})},
\end{equation}

which coincides with the general bound when the shortfalls are homogeneous and was verified
exactly in $1066/1066$ simulated $(A,B)$ cells. The binding composition threshold combines both
sides:

\begin{equation}
    \boxed{\;B^*(A) = \min\big(B^*_{\mathrm{rate}},\ B^*_{\mathrm{QoS}}\big)\;}
\label{eq:abl_bstar}
\end{equation}

Table~\ref{tab:abl_bstar} summarizes the resulting thresholds. The first row shows that the
closed forms above reproduce the simulated sum-rate exactly (deviations at machine precision).
The admission marginals (second row) form a decreasing sequence, as predicted for heterogeneous
candidates admitted in decreasing order of cascade SNR: each next user contributes a smaller
private gain and pulls the group minimum lower. The two QoS rows show the general
shortfall-based bound admitting more users than its equal-share special case ($2$ vs.\ $1$ at
$P_S{=}50$~dBm), confirming the latter is conservative, while the binding $B^*$ (second-to-last
row) is set by the sum-rate side in this geometry and is therefore insensitive to the sharing
assumption. The last row reports that all four thresholds are predicted correctly on every
individual realization.

\begin{table}[t]
\centering
\caption{Composition thresholds in a controlled experiment ($K{=}10$, $N{=}24$; one IRS deactivated and excluded from grouping and power allocation, power weights fixed, $A{=}3$ blocked users on the remaining IRS, free users admitted in decreasing order of mean cascade SNR; $50$ independent spawns per power level, all $B$ evaluated on common channel realizations).}
\label{tab:abl_bstar}
\begin{tabular}{lcc}
\toprule
Quantity & $P_S = 50$ dBm & $P_S = 40$ dBm \\
\midrule
$\max \big|R_{\mathrm{tot}}^{\mathrm{theory}} - R_{\mathrm{tot}}^{\mathrm{sim}}\big|$ & $6.7\times10^{-16}$ & $8.9\times10^{-16}$ \\
Mean marginal $\Delta_{0\to1}$, $\Delta_{1\to2}$ (bps/Hz) & $-0.061$, $-0.108$ & $-0.090$, $-0.104$ \\
$B^*_{\mathrm{rate}}$ (mode / mean) & $0$ / $0.34$ & $0$ / $0.32$ \\
$B^*_{\mathrm{QoS}}$, general (mode / mean) & $2$ / $1.44$ & $0$ / $0.54$ \\
$B^*_{\mathrm{QoS}}$, equal share (mode / mean) & $1$ / $1.04$ & $0$ / $0.42$ \\
$B^* = \min(\cdot,\cdot)$ (mode / mean) & $0$ / $0.34$ & $0$ / $0.32$ \\
Per-draw prediction accuracy (all four thresholds) & $100\%$ & $100\%$ \\
\bottomrule
\end{tabular}
\end{table}

\emph{Trained-policy check.} Fig.~\ref{fig:assignment_law} closes the loop from the theory to the
deployed agents: at every greedy-evaluation step, both actors assign almost exactly the blocked
set --- $99.4$--$100\%$ of routed blocked users go to their own IRS, and over-assignment occurs in
at most $3.8\%$ of steps, inside the region where the admission marginal is near zero. The VQC
tracks the rule more tightly than the DNN in Case~2 (mean deviation
$|\Sigma_{\mathrm{IRS}} - \#\mathrm{blocked}| = 0.10$ vs.\ $0.35$; transient under-assignment in
$5.5\%$ vs.\ $27.1\%$ of steps), an assignment-level counterpart of its QoS edge in
Section~\ref{sec:res_qvsc}.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/assignment_law}
\caption{Per-step assignment of the trained policies against the composition rule (dashed line:
assign exactly the blocked set; marker size $\propto$ frequency over $25$ greedy episodes on the
checkpoints of Tables~\ref{tab:case1_main}--\ref{tab:case2_main}). The shaded band marks the
over-assignment region, where the admission marginal of Eq.~\eqref{eq:abl_dRtot} is near zero.}
\label{fig:assignment_law}
\end{figure}

\section{Rate--QoS Pareto Frontier of the Reward}
\label{sec:abl_pareto}

\emph{Definition and protocol.} This appendix characterizes, independently of any learned policy, the Rate--QoS trade-off that the reward of Section~\ref{sec:training} encodes. For a fixed environment define the \emph{frontier}
\begin{equation}
F(q) \;=\; \max_{\boldsymbol{\varphi},\,\boldsymbol{\Phi},\,\mathbf{w},\,\mathbf{C}}\ \mathbb{E}\big[R_{\mathrm{tot}}\big]
\quad \text{s.t.} \quad \mathrm{QoS} \ge q,
\label{eq:abl_frontier}
\end{equation}
the largest expected sum-rate achievable at QoS level $q$ under the constraints of (P0); the reward-optimal policy operates at the point of this frontier selected by the penalty weight $\lambda_D$. We evaluate $F$ by computing per-state optima over all four decision variables on the simulator's own channel draws ($500$ states per case; assignment by exhaustive search or local search, phase exact, power on a grid --- hence achievable lower bounds). The power split is generalized to arbitrary fractions $x_k = w_{p,k}/P_S$, $y_g = w_c^g/P_S$, $\sum_k x_k + \sum_g y_g \le 1$, giving the per-user SINRs

\begin{equation}
\mathrm{SINR}_{p,k} = \frac{x_k\,\Gamma_k}{\Gamma_k\big(1 - x_k - y_{g(k)}\big) + 1},
\qquad
\mathrm{SINR}_{c,k} = \frac{y_{g(k)}\,\Gamma_k}{\Gamma_k\big(1 - y_{g(k)}\big) + 1}.
\end{equation}

\emph{High-power limit (interference-limited invariance).} As $\Gamma_k \to \infty$ --- the $P_S{=}50$~dBm operating regime --- the two SINRs converge to $x_k/(1-x_k-y_{g(k)})$ and $y_{g(k)}/(1-y_{g(k)})$: the channel magnitudes cancel, and every rate becomes a function of the power fractions alone. In this regime the frontier is set by \emph{counting} --- how many users are served and how the unit power budget is split --- which is why the element count $N$ is inert at the operating point (Table~\ref{tab:nsweep}) and why the closed forms below require no channel statistics beyond $\Gamma_{\max}$.

\emph{Rate-maximal endpoint.} Concentrating the entire budget on the single strongest user
removes all intra-system interference --- the only configuration that escapes the
interference-limited regime, since any configuration with two or more streams is bounded by the
concentration identity below:

\begin{equation}
V^*_{\mathrm{rate}} = \mathbb{E}\big[\log_2(1+\Gamma_{\max})\big],
\qquad
\Gamma_{\max} = \max_k \max_{\text{link}} |h_k|^2 P_S/\sigma^2.
\label{eq:abl_vrate}
\end{equation}

\emph{Serve-all plateau.} In the saturated-SINR limit, the private sum-rate under an equal split
across $m$ served users is

\begin{equation}
R(m) = m \log_2 \frac{m}{m-1}
\;\;\xrightarrow{\ m \to \infty\ }\;\; \frac{1}{\ln 2} \approx 1.443,
\label{eq:abl_concentration}
\end{equation}

a decreasing function of $m$: serving everyone costs sum-rate, with a floor of
$1/\ln 2$~bps/Hz. For Case~2, $R(10) = 1.520$ plus the common streams
($\approx 0.09$) reproduces the serve-all frontier level $1.61$ to three digits.

\emph{Critical penalty weight.} Under the quadratic QoS penalty, fully withholding service from
one user costs $\lambda_D\,\hat p$ with $\hat p = \big(R_k^{\min}/(R_k^{\min}+\epsilon_{\mathrm
q})\big)^2 \approx 0.98$, so the reward-optimal number of served users follows the piecewise
maximum

\begin{equation}
V^*(\lambda_D) = \max_{m \in \{1..K\}} \big\{ R^*(m) - \lambda_D\,(K-m)\,\hat p \big\},
\qquad
\boxed{\;\lambda_{\mathrm{crit}} = \frac{V^*_{\mathrm{rate}} - R^*(K)}{(K-1)\,\hat p}\;}
\label{eq:abl_lcrit}
\end{equation}

where $R^*(m)$ is the best sum-rate serving $m$ users and $\lambda_{\mathrm{crit}}$ is the
penalty weight above which the serve-all optimum takes over; it follows by equating the two
endpoint arms of the piecewise maximum --- the single-user arm $R^*(1) = V^*_{\mathrm{rate}}$
carrying the full parking penalty $\lambda_D\,(K{-}1)\,\hat p$, the serve-all arm $R^*(K)$
carrying none.

\emph{Why $100\%$ QoS is not the reward optimum.} The penalty's marginal cost
$2\lambda_D t/(R_k^{\min}+\epsilon_{\mathrm q})^2$ vanishes as the shortfall $t \to 0$, so the
reward-optimal common-rate allocation is a water-filling solution --- the shares solve
$\sum_{k \in \mathcal{K}_g}(\varsigma_k - t^\star)^+ = R_{c,g}$ for a common level $t^\star$,
exact for the quadratic penalty --- that settles the weakest users
marginally \emph{below} their demand: even $\lambda_D \to \infty$ attains only ${\sim}92\%$
counted QoS under per-step noise. Reward optimality and $100\%$ QoS are therefore distinct
targets, and we compare policies on the frontier at \emph{matched} QoS.

Table~\ref{tab:abl_pareto} summarizes the numerical frontier, evaluated in the same environments
as Tables~\ref{tab:case1_main}--\ref{tab:case2_main}. The closed form \eqref{eq:abl_vrate}
matches the numerical oracle to four digits. The critical weights place the two cases on
\emph{opposite sides} of the training value $\lambda_D = 1.5$: in Case~1 the reward's global
optimum is the degenerate single-user concentration (the curriculum and entropy regularization
steer the learned policy to the serve-all region instead), while in Case~2 the reward itself
already prefers serving all users. The frontier shapes also differ --- Case~1 satisfies
$R_p(\Gamma_{\mathrm{wk}}) \ge R_k^{\min}$ (private rates alone cover the demand), leaving power
free to concentrate, so its serve-all frontier ($3.11$~bps/Hz) far exceeds the equal-split level
of Eq.~\eqref{eq:abl_concentration}; in Case~2 the group load reaches the QoS capacity of
Appendix~\ref{sec:abl_capacity} --- serving all users under concentration requires each group's
common budget to cover the withdrawn private rates, $(n_g{-}m_g)\,R_k^{\min} \le
R_{c,g}(\Gamma^{g}_{\min})$, the shortfall bound in its homogeneous form --- which forces a
near-equal split and flattens the frontier
(serving $8 \to 10$ users costs only $1.80 \to 1.61$~bps/Hz). At matched QoS the trained agents
attain ${\approx}70\%$ of the frontier in Case~1 (both actors; the gap lies on the
power-concentration axis) and $95\%$/$94\%$ (VQC/DNN) in Case~2 --- consistent with the two
operating points observed in Section~\ref{sec:res_qvsc}.

\begin{table}[t]
\centering
\caption{Pareto-frontier quantities per case (closed forms vs.\ the numerical per-state oracle, $500$ states, environments of Tables~\ref{tab:case1_main}--\ref{tab:case2_main}).}
\label{tab:abl_pareto}
\begin{tabular}{lcc}
\toprule
Quantity & Case 1 ($K{=}5$) & Case 2 ($K{=}10$) \\
\midrule
$V^*_{\mathrm{rate}}$, Eq.~\eqref{eq:abl_vrate} / oracle & $10.963$ / $10.963$ & $11.723$ / $11.723$ \\
Serve-all frontier $R^*(K)$ (bps/Hz) & $3.11$ & $1.61$ \\
Critical weight $\lambda_{\mathrm{crit}}$, Eq.~\eqref{eq:abl_lcrit} & $2.00$ & $1.15$ \\
Training $\lambda_D = 1.5$ vs.\ $\lambda_{\mathrm{crit}}$ & below (single-user regime) & above (serve-all regime) \\
Frontier attainment at matched QoS (VQC / DNN) & ${\approx}70\%$ / ${\approx}70\%$ & $95\%$ / $94\%$ \\
\bottomrule
\end{tabular}
\end{table}

\section{Frozen-$\lambda$ Analysis}
\label{sec:abl_frozen}

\emph{Setup.} The encoding scales $\boldsymbol{\lambda} = (\boldsymbol{\lambda}_y,
\boldsymbol{\lambda}_z) \in \mathbb{R}^{2n_q}$ of Section~\ref{sec:vqc} are trained jointly with
the variational angles by SPSA. Across the entire Case-2 curriculum (four stages, $8000$
episodes --- the checkpoint chain behind Fig.~\ref{fig:reward_log_case2}) they exhibit no
meaningful drift, a regime we refer to as \emph{frozen} $\boldsymbol{\lambda}$. This subsection
quantifies the effect, identifies its cause, and bounds its cost.

\emph{Observation: empirical freeze.} Fig.~\ref{fig:lambda_traj} tracks $\|\lambda_y\|_\infty$
and $\|\lambda_z\|_\infty$ over the full curriculum: both remain inside a $0.05$-wide band for
$8000$ episodes. Between the first and the final checkpoint,

\begin{equation}
\rho_\lambda = \frac{\|\boldsymbol{\lambda}_T - \boldsymbol{\lambda}_0\|_2}
{\|\boldsymbol{\lambda}_0\|_2} = 0.14,
\qquad
\|\boldsymbol{\lambda}_T - \boldsymbol{\lambda}_0\|_\infty = 0.109 \approx
c_{\mathrm s} = 0.1,
\label{eq:abl_rho}
\end{equation}

i.e., over the whole curriculum no single component of $\boldsymbol{\lambda}$ moves by more than
approximately one SPSA perturbation step. The per-update diagnostics show why no progress
accumulates: the mini-batch gradient magnitude is healthy
($\overline{\|\hat{\mathbf g}_\lambda\|} = 0.48$) while its batch mean is an order of magnitude
smaller ($\|\bar{\mathbf g}_\lambda\| = 0.044$; ratio $r = 0.09$ on average, $0.14$ at the 95th
percentile).

\begin{figure}[t]
\centering
\includegraphics[width=0.8\columnwidth]{figures/lambda_trajectory_chain}
\caption{Maximum absolute encoding scales $\|\lambda_y\|_\infty$, $\|\lambda_z\|_\infty$ across
the full Case-2 curriculum ($8000$ episodes; dotted lines mark curriculum-stage boundaries).
Both fluctuate marginally around their initial values and never drift.}
\label{fig:lambda_traj}
\end{figure}

\emph{Cause: gradient interference, not vanishing gradients.} On the final checkpoint we
recompute per-sample $\lambda$-gradients over $240$ rollout samples. The per-sample magnitude is
large, $\overline{\|\hat{\mathbf g}_b\|} = 10.4$ --- far above the barren-plateau scale
$O(2^{-n_q})$~\cite{mcclean2018barren} --- yet the mean pairwise cosine between samples,

\begin{equation}
\mathbb{E}_{b \neq b'}\big[\cos(\hat{\mathbf g}_b, \hat{\mathbf g}_{b'})\big] = +0.009,
\label{eq:abl_cos}
\end{equation}

is essentially zero: individual samples carry strong but mutually uncorrelated directions, so
their batch mean cancels and the first moment available to the optimizer stays near zero.
Removing the advantage sign raises the cosine to $+0.10$, indicating that a substantial part of
the cancellation stems from sign flips of the advantage estimate rather than from the quantum
landscape itself.

\emph{The frozen value is rescaling-optimal.} Sweeping a global rescaling $\boldsymbol{\lambda}
\to c\,\boldsymbol{\lambda}$, $c \in [0.1, 3]$, on the final checkpoint ($60$ frozen channel
states $\times\,5$ seeds) finds the frozen value itself to be the best global factor in every
seed ($\Delta(c^*) - \Delta(1) = 0.00$), with the per-state-optimal rescaling adding at most
$0.01$--$0.04$ against a reward scale of ${\sim}1.5$. Away from the frozen value the reward
degrades sharply (at $c{=}2$: $-1.07$ to $-3.09$ across seeds): the encoding scales are far from
irrelevant, but the surrounding network has co-adapted so that the frozen value sits at the
optimum of the rescaling direction. Freezing therefore costs nothing measurable.

\emph{Encoder contribution beyond the classical bypass.} Finally, we check that the encoding
stage with frozen scales is not decorative. We attenuate the quantum encoder by a factor
$c \in \{1, 0.75, 0.5, 0.25, 0\}$ (with $c{=}0$ leaving only the classical bypass through the
post-processing head) and measure the policy's state-discrimination signal --- the mean pairwise
KL divergence between the assignment distributions produced for distinct channel states. On the
final checkpoint the signal drops from $7.06$ (full encoder) to $5.30$ (bypass only): an
encoder-only share of $25\%$. The degradation is smooth in $c$ (every attenuation step within
$11\%$ of the linear trend), so the contribution is a genuine continuous signal rather than a
discontinuity artifact. Across the four checkpoints of the curriculum the absolute signal grows
by $2.4\times$ while the encoder share declines from ${\sim}50\%$ to ${\sim}25\%$ --- the
classical bypass grows faster in late training, but the frozen-scale encoder retains a
substantial share throughout.

Taken together, the frozen-$\lambda$ regime reflects an optimization property of this task ---
directionally incoherent per-sample gradients whose batch mean cancels, around a co-adapted
optimum --- rather than a degeneracy of the encoding stage itself; an honest, task-dependent
contrast to the trainable-scale benefit reported in~\cite{jerbi2021parametrized}. Table~\ref{tab:abl_frozen}
summarizes the verification quantities.

\begin{table}[t]
\centering
\caption{Frozen-$\lambda$ verification on the Case-2 anchor (chain $8000$ episodes; final
checkpoint for the per-sample and rescaling probes).}
\label{tab:abl_frozen}
\begin{tabular}{lc}
\toprule
Quantity & Value \\
\midrule
Relative drift $\rho_\lambda$, Eq.~\eqref{eq:abl_rho} & $0.14$ \\
$\|\boldsymbol{\lambda}_T - \boldsymbol{\lambda}_0\|_\infty$ vs.\ $c_{\mathrm s}$ & $0.109$ vs.\ $0.1$ \\
In-training $\overline{\|\hat{\mathbf g}_\lambda\|}$ / $\|\bar{\mathbf g}_\lambda\|$ / ratio $r$ & $0.48$ / $0.044$ / $0.09$ \\
Per-sample $\overline{\|\hat{\mathbf g}_b\|}$ ($240$ samples) & $10.4$ \\
Mean pairwise cosine, Eq.~\eqref{eq:abl_cos} & $+0.009$ \\
Cosine with advantage sign removed & $+0.10$ \\
Global-rescaling gain $\Delta(c^*) - \Delta(1)$ ($5$ seeds) & $0.00$ \\
Per-state-optimal rescaling gain & $\le 0.04$ \\
Encoder-only share of state discrimination (final ckpt) & $25\%$ (smooth in attenuation) \\
\bottomrule
\end{tabular}
\end{table}



\section{Architectural and Training Ablations}
\label{sec:ablation}
% [PLACEHOLDER 07-18: Lemma 1 + Lemma 2 CUT theo yêu cầu user — nguyên văn lưu ở
%  Research Paper/CUT-BLOCKS-2026-07-18.md, paste lại khi user viết lại A3/A4]

% ---- A1 : quantum vs classical actor (5-axis benchmark) -- maps to C2/C3 ----
\subsection{Quantum vs.\ Classical Actor}
\label{sec:abl_qvc}
\noindent\textbf{Protocol.} All comparisons swap only the high-level assignment actor and hold everything else fixed. The quantum pipeline --- AE encoder, VQC, and SoftmaxPQC head --- is replaced by a fully classical network of standard size: a plain MLP encoder ($d_s \!\to\! 128 \!\to\! 24$) followed by a policy MLP ($24 \!\to\! 256 \!\to\! 128 \!\to\! K(M{+}1)$ logits). The classical actor consumes the identical state features, emits the same latent interface $\mathbf{z}_t$ to the unchanged sub-actors, and is trained by the identical PPO recipe, seed, and episode budget. We report five axes: task performance and training budget, zero-shot generalization, model scalability, sample efficiency, and representation quality.

\subsubsection{Performance and Training Budget}
Across both scales the two actors reach sum rates within $3\%$ of each other at two distinct operating points --- the VQC leans toward QoS ($+4.9$/$+1.9$\,pp at Cases~1/2), the DNN toward raw rate (Tables~\ref{tab:case1_main}--\ref{tab:case2_main}) --- consistent with the prior that parametrized quantum policies match, rather than exceed, classical ones on natural control tasks~\cite{jerbi2021parametrized}. The training budgets differ by the cost of classical simulation: $23$--$37$\,s per episode for the VQC actor against $0.7$--$1.1$\,s for the DNN (median over the final $100$ episodes, identical machine), a ${\sim}30\times$ overhead incurred by ${\approx}38{,}000$ statevector evaluations of a $2^{12}$-amplitude register per episode rather than by the algorithm itself; on quantum hardware the corresponding cost unit is measurement shots. Simulated inference costs ${\approx}0.17$ versus ${\approx}0.008$\,s per decision step.

\subsubsection{Generalization}
Table~\ref{tab:abl_genshift} evaluates both trained Case-2 actors \emph{zero-shot} --- no retraining --- under test-time environment shifts. Both degrade gracefully on every axis except transmit power, which switches the regime from interference- to power-limited (Appendix~\ref{sec:abl_pareto}); the VQC's QoS margin is preserved under every shift ($+1.7$ to $+2.1$\,pp) and widens under power stress ($+3.7$/$+4.0$\,pp at $P_S{=}45/40$~dBm), while unbalanced (natural) user spawning is virtually free for both actors. The element count $N$ admits no zero-shot transfer, as the phase sub-actor's output dimension is tied to $N$; $N$-robustness therefore requires per-$N$ retraining (Table~\ref{tab:nsweep}). All entries are single-seed and we do not read the margins as systematic.

\begin{table}[t]
\centering
\caption{Zero-shot distribution shift (Case~2): both actors evaluated without retraining under test-time environment overrides. Base = the operating point of Table~\ref{tab:case2_main} ($R_{\mathrm{LoS}}{=}0.5$, $P_S{=}50$\,dBm, $\kappa{=}0.05$, $\sigma^2{=}10$\,dB$^2$, speed $1.5$\,m/s, balanced spawn); greedy inference, $50$ episodes, seed $42$. Per-episode sum-rate standard deviations lie within $0.0014$--$0.0027$ (VQC) and $0.0027$--$0.0055$ (DNN) across all shifts; the VQC--DNN gaps exceed these fluctuations.}
\label{tab:abl_genshift}
\setlength{\tabcolsep}{4pt}
\footnotesize
\begin{tabular}{lcccc}
\toprule
 & \multicolumn{2}{c}{HQC-HAC (VQC)} & \multicolumn{2}{c}{DNN} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
Shift & $R_{\mathrm{tot}}$ & QoS & $R_{\mathrm{tot}}$ & QoS \\
\midrule
base                & $1.5307$ & $97.3\%$ & $1.5182$ & $95.4\%$ \\
$P_S = 40$~dBm      & $1.2947$ & $74.5\%$ & $1.2654$ & $70.5\%$ \\
$P_S = 45$~dBm      & $1.4462$ & $87.1\%$ & $1.4268$ & $83.4\%$ \\
$P_S = 60$~dBm      & $1.5818$ & $99.6\%$ & $1.5777$ & $99.2\%$ \\
speed $3$~m/s       & $1.5244$ & $97.1\%$ & $1.5120$ & $95.0\%$ \\
speed $6$~m/s       & $1.4928$ & $95.3\%$ & $1.4840$ & $93.2\%$ \\
natural spawn       & $1.5312$ & $97.2\%$ & $1.5122$ & $95.3\%$ \\
$\kappa = 0.10$     & $1.5291$ & $97.2\%$ & $1.5170$ & $95.4\%$ \\
$\kappa = 0.20$     & $1.5263$ & $97.0\%$ & $1.5150$ & $95.3\%$ \\
$\sigma^2 = 13$     & $1.5270$ & $96.8\%$ & $1.5150$ & $94.8\%$ \\
$\sigma^2 = 16$     & $1.5241$ & $96.3\%$ & $1.5119$ & $94.3\%$ \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Model Scalability}
The assignment-specific parameter budget separates the actors by orders of magnitude at matched task performance. The quantum decision core --- variational angles, encoding scales, and encoding-normalization affine ---
\begin{equation}
  |\boldsymbol{\theta}_{\mathrm{VQC}}|
  = \underbrace{2 n_q L}_{\boldsymbol{\theta}^y,\boldsymbol{\theta}^z}
  + \underbrace{2 n_q}_{\boldsymbol{\lambda}_y,\boldsymbol{\lambda}_z}
  + \underbrace{4 n_q}_{\boldsymbol{\gamma},\mathbf{b}_{\mathrm{LN}}}
  = 2 n_q (L{+}3) = \mathcal{O}(n_q L),
  \label{eq:vqc_params}
\end{equation}
counts $120$/$144$ parameters at Cases~1/2 against $40{,}586$/$43{,}166$ for the classical policy MLP of matched role --- a $338\times$/$300\times$ reduction that grows only linearly in the register size, whereas a classical block of matched input--output dimensions scales as $\mathcal{O}(d_h(2n_q{+}N_Q))$ in its hidden width $d_h$. At the full inference-time actor level (AE decoder discarded at deployment) the counts are $1{,}063$/$2{,}761$ against $45{,}986$/$52{,}022$ ($43\times$/$19\times$). The stated ratios are specific to the chosen classical width. A first iso-performance point --- a $2{,}230$-parameter policy MLP trained under the identical recipe at the $R_{\mathrm{LoS}}{=}0.2$ stage --- already matches both the $40$K classical baseline and the VQC ($1.516$/$97.4\%$ against $1.509$/$96.5\%$ and $1.512$/$96.8\%$, single seed), indicating that the standard-width baseline is over-parameterized for this task and that the matched-performance ratio of assignment-decision parameters is nearer $15\times$ ($2{,}230$ versus $144$); the remaining width grid is left to future work.

\subsubsection{Sample Efficiency}
Both actors train under the identical PPO recipe and episode budget, so differences in learning speed are attributable to the actor alone. At the two hardest curriculum transitions (resuming into $R_{\mathrm{LoS}}{=}0.4$, and into $0.5$ with full spawn unlock), both actors recover to within $90\%$ of their final QoS at the same speed (${\sim}25$ episodes), but the transient dip is consistently shallower for the VQC: minimum sampled QoS $87\%$/$91\%$ at the two transitions, against $83\%$/$84\%$ for the DNN. The curriculum-robustness advantage therefore lies in the depth, not the duration, of the transient (single seed).

\subsubsection{Representation Quality}
A distillation probe quantifies how much assignment-relevant information the latent $\mathbf{z}_t$ carries, by supervised recovery of the oracle assignment from frozen latents. For the quantum pipeline, a lightweight classical probe recovers $51\%$ of the oracle-assignment reward gap from $\mathbf{z}_t$, against $27\%$ from the raw state and none by the live policy during standard training --- the latent concentrates, rather than merely compresses, the assignment-relevant information. The classical-actor counterpart of this probe is pending: \emph{---}.

% ---- A2 : autoencoder latent compression -- maps to C3 --------------------
\subsection{Autoencoder Latent Compression}
\label{sec:abl_ae} The AE compresses the high-dimensional state into a latent $\mathbf{z}_t\in\mathbb{R}^{2n_q}$ that (a) is necessary to fit the input into the $n_q$-qubit register, and (b) concentrates, rather than merely preserves, the assignment-relevant information. We assess (i) the qubit cost of encoding without compression against the fixed $n_q{=}12$ with AE; and (ii) the information loss of the fixed-width latent as the problem scales, governed by the compression ratio $d_{\mathrm{aff}}/2n_q$ with $d_{\mathrm{aff}}=K(M{+}2){+}2M$ growing in $K$ and $M$ ($N$ is filtered out by the affinity construction).

\begin{table}[t]
\centering
\caption{Encoding cost and compression per case. The VQC input dimension $d_{\mathrm{aff}}=K(M{+}2){+}2M$ sets the register a raw, AE-free angle encoding would need, $n_q^{\mathrm{raw}}=\lceil d_{\mathrm{aff}}/2\rceil$ (two rotations per qubit); the AE fixes it at $n_q{=}12$. $L_{AE}$ is the converged reconstruction floor (Fig.~\ref{fig:ae_trajectory}); Case~3 is untrained.}
\label{tab:abl_ae_params}
\setlength{\tabcolsep}{4pt}
\begin{tabular}{ccccc}
\toprule
$(K,M,N)$ & $d_{\mathrm{aff}}$ & $n_q^{\mathrm{raw}}$ & $n_q^{\mathrm{AE}}$ & $L_{AE}$ \\
\midrule
$(5,1,24)$  & $17$ & $9$  & $12$ & $2\!\times\!10^{-4}$ \\
$(10,2,24)$ & $44$ & $22$ & $12$ & $5\!\times\!10^{-3}$ \\
$(15,3,24)$ & $81$ & $41$ & $12$ & \emph{---} \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/ae_trajectory}
\caption{AE reconstruction loss $L_{AE}$: pre-training (dashed, $5000$ steps on random states, left of the solid vertical line) hands off to joint training over the full $R_{\mathrm{LoS}}$ curriculum (solid, $0.2\!\to\!0.5$; dotted lines mark ramp transitions; log scale). Pre-training on random inputs floors at $L_{AE}\!\approx\!7\!\times\!10^{-3}$ (Case~1) and $\approx\!6.3$ (Case~2) --- a heuristic starting point; joint training on the structured rollout states then drives $L_{AE}$ to $\sim\!2\!\times\!10^{-4}$ under expansion (Case~1, $d_{\mathrm{aff}}{=}17{<}24$, near machine-lossless) and to a floor $\sim\!5\!\times\!10^{-3}$ under compression (Case~2, $d_{\mathrm{aff}}{=}44{>}24$) --- the floor is reached, not traded away for task loss.}
\label{fig:ae_trajectory}
\end{figure}




% ---- A4 : VQC architectural choices -- user sẽ viết lại prose -------------
\subsection{VQC Architectural Choices}
\label{sec:abl_arch}
Five architectural knobs define the quantum actor; each is varied one at a time against the reference configuration ($n_q{=}12$, $L{=}2/3$, nearest-neighbor CZ chain with $M$ cross-block bridges, $R_yR_z$ angle encoding with data re-uploading, R1 readout).

\subsubsection{Circuit Depth}
The number of variational layers $L$ widens the accessible frequency spectrum of the re-uploading circuit~\cite{schuld2021effect}, so performance is expected to improve with $L$ up to saturation. The depth sweep is pending: \emph{---}.

\subsubsection{Entanglement Pattern}
Candidate patterns of increasing connectivity --- none, linear chain, ring, full --- are compared against the reference chain augmented with $M$ cross-block CZ bridges. The bridges are the problem-specific element: they couple the user- and IRS-qubit registers and are hypothesized to carry the user--IRS interaction that the assignment must model, whereas denser generic patterns add entanglement without this alignment. The toggle runs are pending: \emph{---}.

\subsubsection{Qubit Count and Latent Dimension}
The register size $n_q$ fixes the latent width $2n_q$ and hence the compression ratio $d_{\mathrm{aff}}/2n_q$; it trades representation capacity against the $2^{n_q}$ simulation cost. A preliminary Case-1 comparison found $n_q{=}8$ to underperform the unified $n_q{=}12$ configuration by a wide QoS margin; a controlled sweep is pending: \emph{---}.

\subsubsection{Feature Map}
The reference encoding loads each latent pair through $R_y R_z$ rotations with a $\tanh$ squash and re-applies the encoding block at every layer (data re-uploading~\cite{perez2020data}). Alternative feature maps (amplitude encoding, IQP-style maps) change the accessible function class and the loading cost. A comparison is pending: \emph{---}.

\subsubsection{Readout Design}
The readout ladder sweeps Pauli observable sets of increasing richness at otherwise identical hyperparameters; Table~\ref{tab:abl_readout} reports both cases.

\begin{table}[t]
\centering
\caption{Readout-design ladder (Case~1\,/\,Case~2), identical hyperparameters, greedy inference. The observable count grows single-$Z\!\to\!Z\!+\!$NN-$ZZ\!\to\!Z\!+\!$full-$ZZ\!\to\!$R1; only the readout differs. R1 is the structured set of Eq.~\eqref{eq:observables}.}
\label{tab:abl_readout}
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lccc}
\toprule
Readout & $|\hat{\mathbf{o}}|$ (C1/C2) & $R_{\mathrm{tot}}$ (C1/C2) & QoS (C1/C2) \\
\midrule
single-$Z$        & $12/12$ & $1.7890$/$1.5025$ & $99.9\%$/$97.1\%$ \\
$Z\!+\!$NN-$ZZ$   & $23/23$ & $1.7715$/$1.5072$ & $99.9\%$/$96.5\%$ \\
$Z\!+\!$full-$ZZ$ & $33/41$ & $1.7282$/$1.5012$ & $99.7\%$/$95.5\%$ \\
R1 (structured)   & $34/42$ & $1.7471$/$1.5017$ & $99.7\%$/$96.8\%$ \\
\bottomrule
\end{tabular}
\end{table}

% ---- A5 : entropy regularization & annealing -- maps to C2 ----------------
\subsection{Entropy Regularization and Annealing}
\label{sec:abl_entropy}
\noindent\textbf{Hypothesis~5.} \emph{The annealed entropy bonus of~\eqref{eq:objective} is necessary to explore the combinatorial assignment space: a fixed-low $\beta$ converges prematurely, a fixed-high $\beta$ fails to exploit, and the $\beta_0\!\to\!\beta_{\min}$ schedule dominates both.}
The entropy gradient $\nabla H(\pi_j)=-\nabla\!\sum\pi_j(1{+}\log\pi_j)$ pushes each actor toward uniform exploration with strength $\beta$, mirroring the ``trainable greediness'' role of the inverse temperature in softmax-PQC policies~\cite{jerbi2021parametrized}.
\noindent\textbf{Setup.} Compare fixed-low, fixed-high, and annealed $\beta$ (reference); report exploration proxies (assignment entropy, IRS-usage) alongside sum-rate.
\noindent\textbf{Observations.}
\begin{itemize}
  \item The training logs bear out the hypothesis qualitatively: a fixed-high $\beta$ holds the assignment distribution near-uniform so the policy never commits (the ``entropy-dominated'' regime), whereas a fixed-low $\beta$ collapses early onto a sub-optimal assignment; the $\beta_0\!\to\!\beta_{\min}$ schedule --- which explores first and exploits later --- is used throughout and avoids both failure modes. A controlled fixed-versus-annealed sweep at matched seeds is left to future work.
\end{itemize}

% ---- A6 : power-fairness blending -- maps to the Power MLP execution path --
\subsection{Power-Fairness Blending}
\label{sec:abl_power}
\noindent\textbf{Hypothesis~6.} \emph{The power-fairness coefficient $\alpha_{\mathrm{pf}}\!\in\![0,1]$, which blends the \emph{executed} split/common/private power toward the QoS-optimal equal allocation while leaving the sampled action and its log-probability unchanged, is a monotone Rate--QoS control knob whose QoS-optimal operating point is case-specific rather than universal.}
Concretely, the Power MLP's categorical split/common/private probabilities $(p_s,p_c,p_p)$ still parameterize the policy gradient unaltered; only the powers used to \emph{execute} the environment step are blended, $p^{\mathrm{eff}} = (1-\alpha_{\mathrm{pf}})\,p + \alpha_{\mathrm{pf}}\,p^{\star}$, where $p^{\star}$ targets a fixed private-vs-common split and a uniform distribution within each axis. $\alpha_{\mathrm{pf}}{=}0$ recovers the raw learned allocation and $\alpha_{\mathrm{pf}}{=}1$ forces the QoS-favoring equal split, so $\alpha_{\mathrm{pf}}$ trades sum-rate concentration for QoS coverage without touching the gradient path.
\noindent\textbf{Setup.} Sweep $\alpha_{\mathrm{pf}}$ with every other hyperparameter fixed (identical seed, recipe, and curriculum): $\alpha_{\mathrm{pf}}\in\{0.3,0.4,0.5\}$ on Case~1 and $\alpha_{\mathrm{pf}}\in\{0.5,0.7,0.9\}$ on Case~2; report greedy-inference $R_{\mathrm{tot}}$ and QoS for each point.
\noindent\textbf{Observations.}
\begin{itemize}
  \item \emph{Power-fairness sweep} (Table~\ref{tab:abl_power}). % [pending alpha-sweep]
  Runs pending at the time of writing; only the endpoints already used as the Case-1 ($\alpha_{\mathrm{pf}}{=}0.5$, Table~\ref{tab:case1_main}) and Case-2 ($\alpha_{\mathrm{pf}}{=}0.9$, Table~\ref{tab:case2_main}) main-result operating points are available. We expect QoS to rise monotonically in $\alpha_{\mathrm{pf}}$ at a mild sum-rate cost, consistent with $\alpha_{\mathrm{pf}}$ pushing power away from the rate-maximizing concentration and toward equal coverage; the QoS-optimal $\alpha_{\mathrm{pf}}$ is expected to sit higher for Case~2 than Case~1, since a larger $K$ leaves proportionally more users exposed to the worst-case shortfall that equal power directly relieves. Because the two cases were locked at different endpoints of the sweep ($0.5$ vs.\ $0.9$) rather than a shared grid, the frontier is case-specific and should not be read as a single universal knob value.
\end{itemize}

\begin{table}[t]
\centering
\caption{Power-fairness sweep (Case~1 / Case~2), identical hyperparameters and curriculum except $\alpha_{\mathrm{pf}}$, greedy inference.}
\label{tab:abl_power}
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lcc}
\toprule
$\alpha_{\mathrm{pf}}$ & $R_{\mathrm{tot}}$ & QoS \\
\midrule
C1, $0.3$  & \emph{---} & \emph{---} \\
C1, $0.4$  & \emph{---} & \emph{---} \\
C1, $0.5$  & \emph{---} & \emph{---} \\
C2, $0.5$  & \emph{---} & \emph{---} \\
C2, $0.7$  & \emph{---} & \emph{---} \\
C2, $0.9$ & \emph{---} & \emph{---} \\
\bottomrule
\end{tabular}
\end{table}

% ---- A7 : warm-up strategy isolation -- maps to the oracle/phase warm-up ---
\subsection{Warm-Up Strategy Isolation}
\label{sec:abl_warmup}
\noindent\textbf{Hypothesis~7.} \emph{Warming the assignment and/or phase sub-policies on oracle targets before the PPO objective is switched on suppresses the early reward ``yank'' seen from a cold start; isolating the two warm-up paths shows the assignment (routing) warm-up carries most of the benefit, while the phase warm-up saturates once the power blend of Appendix~\ref{sec:abl_power} is set.}
\noindent\textbf{Setup.} At fixed $\alpha_{\mathrm{pf}}$ on Case~2, compare four warm-up configurations: \emph{(i)} vanilla (no warm-up); \emph{(ii)} assignment-only, warming the quantum actor's Q-head toward the oracle blocked-user routing target; \emph{(iii)} phase-only (\texttt{--phase-warmup}), warming the Phase MLP toward the closed-form phase oracle; \emph{(iv)} dual (\texttt{--oracle-warmup}), warming both assignment and phase jointly. Metrics: greedy-inference $R_{\mathrm{tot}}$ and QoS, plus a convergence-speed proxy (episodes to reach $90\%$ of final QoS).
\noindent\textbf{Observations.}
\begin{itemize}
  \item \emph{Warm-up isolation} (Table~\ref{tab:abl_warmup}). % [pending]
  Runs pending. \emph{(i)}, \emph{(iii)}, and \emph{(iv)} are launch-ready with existing flags; \emph{(ii)} currently has no dedicated flag --- \texttt{--oracle-warmup} warms assignment and phase jointly and cannot isolate the Q-head path alone, so an assignment-only warm-up requires adding a flag that runs the assignment branch of the existing oracle-warmup routine without the phase branch before this row can be filled. We expect the ordering $\mathrm{(ii)}\!\approx\!\mathrm{(iv)}\!>\!\mathrm{(iii)}\!\approx\!\mathrm{(i)}$: the routing bootstrap is expected to dominate the benefit (consistent with the oracle-intervention finding that substituting the assignment yields the largest single-actor gain), while the phase warm-up alone saturates and contributes comparatively little once the power blend is in place.
\end{itemize}

\begin{table}[t]
\centering
\caption{Warm-up strategy isolation (Case~2), identical hyperparameters and curriculum except the warm-up configuration, greedy inference. The dual row is the canonical Case-2 operating point ($R_{\mathrm{LoS}}{=}0.2$ stage of the Table~\ref{tab:case2_main} chain); the remaining configurations are pending.}
\label{tab:abl_warmup}
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lccc}
\toprule
Warm-up & $R_{\mathrm{tot}}$ & QoS & Convergence \\
\midrule
Vanilla (none)         & \emph{---} & \emph{---} & \emph{---} \\
Assignment-only         & \emph{---} & \emph{---} & \emph{---} \\
Phase-only (\texttt{--phase-warmup})  & \emph{---} & \emph{---} & \emph{---} \\
Dual (\texttt{--oracle-warmup})       & $1.512$ & $96.8\%$ & \emph{---} \\
\bottomrule
\end{tabular}
\end{table}

\end{document}