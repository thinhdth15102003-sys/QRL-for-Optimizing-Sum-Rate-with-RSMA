================================================================================
  COMMON KNOWLEDGE  ·  IRS-assisted RSMA Quantum-RL  (case-agnostic)
================================================================================
  Kiến thức CHUNG về mô hình & cách train, KHÔNG phụ thuộc (K,M) hay R_LoS.
  Hyperparameter cụ thể theo từng case → xem Training-Case-<N>.txt.
  Cập nhật lần cuối: 2026-06-01  (Case1 r1→11; +[I-2],[J](C2 r1), [B-2](r2), [B-2b]GAE(r3), [H-2]TDR(r4),
                                 [B-2b] clipfrac-trước-lr + clip-là-triệu-chứng (C2 r6/r7), [B-2c] oracle-sweep,
                                 lưu-ý QoS⊥critic (C2 r5/6/7); +PopArt-stable-capped + đọc-explVar-đúng + counterfactual
                                 power-probe (C2 r8); +code-clean gỡ TD/target-net (PopArt+GAE-return only);
                                 +stable-capped-caveat + [B-2d] DRIFT-ATTRIBUTION timeline (C2 r8 drift ep1068→1404);
                                 2026-06-02: [E] frozen-λ ROOT=directionless-gradient (not noise/LN) + smoke-r-unreliable
                                 + λ≠cause-of-PhaseMLP-idle + E-fix0/1/2 + probe-set/STEP3.5 (phase_quality probe);
                                 2026-06-03: [E] CAVEAT GỠ — H3 confirmed on arch-2 LINEAR head (still flat) → CHỐT E-fix2, đóng λ-question;
                                 2026-06-03: [B] CEILING-DEFINITION CAVEAT (full-state vs s_t-oracle 0.59; dead-units+repr BÁC; critic gần trần thực dụng);
                                 2026-06-03: Part5#8 PRIORITY ĐỔI — SUM-RATE trước QoS (sum-rate=objective C1, QoS=constraint, trade qua λ_D ramp sau);
                                 2026-06-03: [I-3] λ_D PHẢI TĂNG theo ramp (curriculum rule, asymmetric risk, probe_wc_optimality evidence))
--------------------------------------------------------------------------------
  CÁCH DÙNG BỘ FILE NÀY:
   - Common-Knowledge.txt (file này): cơ chế mô hình, bảng tra lỗi, tín hiệu chẩn đoán,
     nguyên tắc — đúng cho mọi case.
   - Training-Case-1.txt: K=5,M=1 — hyper từng ramp 0.2→0.5 + lịch sử thực tế.
   - Training-Case-2.txt: K=10,M=2 — hyper + những điểm cần chú ý.
   - params.py định nghĩa Case 1/2/3 = (K=5,M=1) / (K=10,M=2) / (K=20,M=4).
--------------------------------------------------------------------------------


================================================================================
  PHẦN 1 — TỔNG QUAN MÔ HÌNH
================================================================================

Bài toán: IRS-assisted RSMA cho vệ tinh (NTN băng L, 1.58 GHz). Agent RL điều khiển
4 quyết định mỗi bước để tối đa reward = sum_rate − QoS_penalty.

Actor pipeline (4 stage/step):
  1. IRS selection (LƯỢNG TỬ): state → AE encoder → z_t → QC (nq qubit, L=3, data
     re-uploading) → post-NN → logits gán mỗi user vào {direct, IRS_1..M}.
  2. PhaseMLP: chọn pha IRS (N=24 phần tử/IRS × M, mức rời rạc 2-bit). Trọng số CHIA
     SẺ theo phần tử.
  3. PowerMLP: phân bổ công suất [w_c (common, theo group), w_p (private)] Σ=P_S.
  4. CkMLP: chia common-rate C_k trong group (softmax).

Critic: V(s) MLP (state-value). Dùng làm baseline cho advantage.
AE: dual-branch encoder (IRS branch + user micro-encoder chia sẻ theo user) → z_t;
    decoder tái tạo d_aff = K*(M+2)+2M (regulariser).
Lượng tử: tham số học được λ (encoding scales) + θ (variational angles), train bằng
    parameter-shift / SPSA. λ encoding: α=π·tanh(λ_y·z), δ=π·tanh(λ_z·z).
Thuật ngữ mạch: đây là VQC (data re-uploading) — PQC có tham số được tối ưu variational.


================================================================================
  PHẦN 2 — CƠ CHẾ CỐT LÕI (hiểu "tại sao")
================================================================================

REWARD:  reward = sum_rate − qp_penalty
  qp = λ_D · Σ_k (shortfall_k / (D_k+ε))²,  shortfall_k = max(0, D_k − R_tot_k)
  → reward RẤT NHẠY với QoS: 1 user fail → penalty lớn (∝ λ_D). reward âm sâu khi
    nhiều user dưới ngưỡng. Đây là "vòng xoáy" khi QoS tụt (penalty tăng → reward
    càng âm → advantage nhiễu → policy càng tệ).

GAE (gae_lambda): Â_t = Σ(γλ)^l δ_{t+l}. λ=0 → TD 1-bước (bias cao, KHÔNG credit dài
  hạn). λ→1 → Monte-Carlo. Phối hợp IRS/power/Ck là bài toán DÀI HẠN (200 bước) →
  cần λ cao (≈0.9) để credit đúng. λ=0 là nguyên nhân gốc của collapse (xem [A]).

CRITIC + WARM-UP: advantage chỉ tốt khi V(s) chính xác. Critic dựng mới (blind,
  explVar≈0) feed advantage rác cho actor đã tốt → phá policy. Warm-up = roll policy
  ĐÓNG BĂNG, fit V(s) theo Monte-Carlo return TRƯỚC PPO → V đúng thang từ ep 1.
  Critic được lưu kèm checkpoint → resume tự nạp lại. Đổi λ_D/R_LoS làm dịch thang
  return → critic_target_tau cao hơn giúp re-calibrate nhanh.

ENTROPY (β): khuyến khích explore. NHƯNG "β·H phải ≪ L_pg, KỂ CẢ khi hội tụ". Khi
  policy gần tối ưu (nhất là với gae>0) L_pg → rất nhỏ → β cố định/cao làm β·H lấn át
  → policy trôi về NGẪU NHIÊN → un-learn QoS. Harness in cờ "⚠ ENTROPY-DOMINATED".
  → β phải anneal về sàn; resume policy tốt thì β thấp + anneal nhanh.

SPSA (lượng tử): ước lượng gradient QC bằng n_reps nhiễu (rẻ hơn parameter-shift).
  nq lớn → SPSA nhiễu hơn → λ dễ "đóng băng" ở init (không học). Tăng spsa_n_reps
  cho gradient sạch hơn.

CURRICULUM (R_LoS): tăng độ khó hình học dần 0.2→0.3→0.4→0.45→0.5, resume mỗi bước.
  R_LoS lớn = user/IRS xa hơn = link yếu hơn = QoS khó hơn. ỔN ĐỊNH ở bước hiện tại
  RỒI mới lên — lên sớm = mang lỗi sang env khó hơn.

CHỌN MODEL TỐT NHẤT: theo QoS từ checkpoints/, KHÔNG dùng thư mục best/ (best lưu theo
  REWARD, không cùng thang giữa các λ_D, và đỉnh reward thường là ep sớm chứ không phải
  đỉnh QoS).


================================================================================
  PHẦN 3 — BẢNG TRA SỰ CỐ  (triệu chứng → nguyên nhân → cách khắc phục)
================================================================================
  (Giá trị hyper cụ thể tùy case → đối chiếu Training-Case-<N>.txt.)

[A] COLLAPSE — QoS/reward lên đỉnh rồi TỤT, common-stream sụp (wc→~24%, Ck→~0.46).
    Nguyên nhân: credit dài hạn yếu (gae_lambda=0) → luồng common bị xói khi hết
    exploration nâng đỡ. KHẮC PHỤC: gae_lambda=0.9. Theo dõi wc & Ck giữ ổn định.

[B] CRITIC MÙ — explVar≈0 / V̄ sai thang (vd ≈−1 khi return thật ±20).
    Hai biến thể (phân biệt bằng explVar có tự khỏi sau khi gỡ entropy không):
    (B-1) Cold-start/scale (sau resume, hoặc warm-start policy tốt): critic chưa calibrate.
          KHẮC PHỤC: bật critic warm-up; nạp lại critic đã lưu; critic_target_tau↑ (≈0.02).
    (B-2) DAI DẲNG dù đã gỡ entropy-domination [C] (explVar vẫn ~0.1 sau khi cờ ⚠ tắt, qua
          nhiều trăm ep): KHÔNG phải entropy → critic KHÔNG FIT NỔI return. Nguyên nhân:
          return variance quá cao (policy còn stochastic → cùng s_t, action sample khác →
          return khác; + nhiễu env κ/mobility/noise) và/hoặc mismatch target (critic học
          TD(0) 1-bước + target net chậm, nhưng explVar đo vs GAE-return).
          KHẮC PHỤC (thử theo thứ tự): reward_noise_avg↑ (vd 8→16, giảm variance ~1/√2);
          bật critic warm-up CHO CẢ FRESH (cho V scale tốt từ đầu → bootstrap); nếu vẫn mù/
          BẤT ỔN → ⭐ train critic bằng GAE-return ('ret'=Â+V) thay TD(0) — xem B-2b.
    (B-2b) BẤT ỔN / GRAD V KHÔNG GIẢM: dấu hiệu = ‖∇‖V plateau LỚN (vd 700-1000, clip 1.0 cắt
          mạnh) KHÔNG giảm + explVar DAO ĐỘNG (vd 0.14↔0.64, không giữ >0.5) + QoS kẹt — DÙ
          explVar đã > blind (vd ~0.4). = critic "đuổi" target TD(0) nhiễu, không settle.
          GỐC: critic học TD(0) (r+γV_target(s')) phương sai cao + mismatch (explVar đo vs GAE-ret).
          ⭐ FIX GỐC: đổi critic target TD(0) → GAE-return t['ret'] (train.py: critic.compute_grads_batch
          dùng t['ret'] thay t['td_target']). ret (λ-return) ít nhiễu hơn (avg ~1/(1-γλ) bước) +
          align explVar + là value-target PPO CHUẨN → grad V giảm/settle, explVar ổn. (TD(0)+
          target-net là phi chuẩn cho PPO — nguồn bất ổn.) Phụ trợ: lr_critic↓ (dampen) nếu còn wobble.
          (Case 2 result_3: warm-up+noise_avg đưa explVar 0.08→0.4 nhưng grad plateau 700-1000 +
           explVar wobble + QoS kẹt 51% → B-2b → đổi GAE-target ở result_4.)
          ⭐ QUY TẮC THỨ TỰ (general): khi grad_clip_critic BIND (clipfrac→~100%, tức ‖∇‖V preclip
          ≫ ngưỡng clip — vd preclip 400-1300 vs clip 1.0), thì chỉnh lr_critic GẦN NHƯ VÔ NGHĨA:
          gradient đã bị rescale về đúng clip-norm → bước hiệu dụng = lr·clip_norm·hướng, chỉ còn
          HƯỚNG của grad có tác dụng; hạ lr chỉ thu nhỏ một bước vốn đã bị clip. → PHẢI đọc clipfrac
          (preclip ‖∇‖V vs ngưỡng) TRƯỚC khi tune lr. Nếu clipfrac cao → NỚI grad_clip_critic (vd
          1.0→5.0) để critic nhận đủ tín hiệu, RỒI mới xét lr. (Tier-2 critic_diag đo clipfrac sẵn.)
          (Case 2 result_6: lr_critic 5e-4→3e-4 ĐƠN LẺ KHÔNG fix — explVar vẫn wobble 0.21-0.66,
           grad V plateau 400-1300, clipfrac~100% ở clip=1.0. lr ko chạm tới vì grad đã bị clip.)
          ⚠ CẢNH BÁO (general, Case 2 result_7): grad_clip BIND chỉ là TRIỆU CHỨNG, KHÔNG chắc là
          BỆNH. Nới clip (1.0→1000) → clipfrac 100%→0% (preclip tự co 400-1300→~200 vì critic fit
          được) NHƯNG explVar VẪN wobble −0.37↔+0.62 + QoS kẹt → clip KHÔNG phải nút thắt gốc.
          Khi nới clip: kiểm explVar có GIỮ >0.5 ỔN ĐỊNH không, ĐỪNG mừng vì clipfrac rớt. Nếu vẫn
          wobble → gốc sâu hơn = RETURN SCALE-DRIFT / target non-stationarity (bias=μ(V−ret) trôi
          mạnh là dấu hiệu) → ⭐ FIX = RETURN NORMALIZATION (z-score target / PopArt: critic dự đoán
          return chuẩn hóa, un-scale ở output bằng running μ,σ). Bake-in target-norm làm critic fit
          tốt hơn + BỀN với grad_clip (xem [DIAG-ORACLE]).
          (Case 2 result_8: FULL PopArt → T1 grad O(1-5), T2 bias~0, POP|ΔV|=1e-15. T3 post-anneal
           explVar mean0.45 STD0.11 MIN0.30 KHÔNG dip âm → critic ỔN, agent cải thiện QoS 51→57%, reward+.)
          ⭐ ĐỌC explVar ĐÚNG (general): ĐỪNG đọc binary "mean>0.5". explVar mean<0.5 NHƯNG std THẤP
          (<0.15) + KHÔNG dip âm = STABLE-CAPPED (ổn định ở ~TRẦN ALEATORIC, CHẤP NHẬN) — KHÁC HẲN
          unstable-WOBBLE (std cao + dip xuống âm). Trần aleatoric tuỳ policy-stochasticity + env noise
          (Tier-1 probe đo). → đọc std + min + có-dip-âm-không, KHÔNG chỉ mean. Stable-capped = ĐỦ TỐT
          để tiến (đừng đào critic mãi khi nó chỉ "capped" chứ không "loạn").
          ⚠⚠ CEILING-DEFINITION CAVEAT (general, 2026-06-03 — sửa hiểu nhầm tồn tại từ r5/6): Tier-1 probe
          (probe_critic_ceiling / temp-sweep) tính ceiling bằng SNAPSHOT env rồi replay → điều kiện trên
          TOÀN BỘ env-state (channels+positions+blockage), KHÔNG phải trên s_t critic THẤY. → ceiling Tier-1
          (~0.81 trained) = FULL-STATE upper bound; critic-nhìn-s_t KHÔNG đạt được nếu s_t lossy. TRẦN
          s_t-THẬT = OFFLINE ORACLE-FIT CÙNG policy. → "stable-capped = at aleatoric ceiling" PHẢI so
          explVar với s_t-ORACLE (cùng policy), KHÔNG so full-state probe ceiling.
          ✅ ĐÃ ĐO (probe_critic_repr_ablation, result_11 ep300 TRAINED policy, clean-MC, noise16, 2026-06-03):
          s_t-oracle (fresh, arch 512x256x128x64) = 0.59 ·  full-state ceiling = 0.82  ·  live ~0.40-0.45.
          ⭐ KẾT LUẬN (cả 2 giả thuyết BỊ BÁC):
            – DEAD-UNITS/capacity ❌ BÁC: fresh oracle CÙNG arch đạt 0.59 → capacity/dead-unit KHÔNG phải giới hạn.
            – REPRESENTATION (richer input) ❌ KHÔNG phải lever sạch: thêm |g|,∠g (V2/V3) ≈ base 0.59 hoặc thấp
              hơn (channels REDUNDANT với s_t — s_t vốn DERIVED từ |channel|; phase thêm ~0); +blockage/+full
              (V1 0.31/V4 0.05) RỚT = overfit/optim-instability (same arch+epochs+6k), KHÔNG phải info vô dụng.
              Gap 0.59→0.82 nằm ở per-element channel detail (M×N×K) mà obs ĐÃ aggregate đi → KHÔNG thực tế để feed.
            – Residual live(0.40-0.45)→s_t-oracle(0.59) ~0.15 = TRAINING-DYNAMICS (online PPO+GAE+non-stationarity
              vs offline clean-MSE), KHÔNG phải arch/repr. Lever = critic-side training (epochs/warmup/refit), payoff modest.
          → critic Ở GẦN TRẦN s_t THỰC DỤNG (~0.59). KHÔNG có lever arch/repr giá trị cao. Judge critic vs ~0.59
            (KHÔNG vs 0.82). "stable-capped 0.45 = aleatoric" hơi PESSIMISTIC: trần s_t là 0.59, residual là dynamics.
          ⚠ LESSON: ĐỪNG so oracle RANDOM-policy (0.376) với ceiling TRAINED-policy (0.81) = lệch POLICY (đã sửa:
            trained s_t-oracle thật = 0.59, không phải 0.376). ⚠ Auto-verdict của 2 probe (temp-sweep "close",
            repr-ablation "≈ live") THRESHOLD LỎNG → đọc số thủ công, đừng tin dòng verdict tự động.
          ⚠ STABLE-CAPPED CAVEAT (general, Case2 result_8 drift): "stable-capped" chỉ chứng minh critic
          ỔN trong CỬA SỔ ĐO (vd post-anneal ep400-700). Run DÀI HƠN có thể drift muộn: critic destabilize
          sau ep1000+ (explVar dip âm) → advantage nhiễu → actor head trôi degenerate. → mean<0.5 dù
          std thấp = "borderline" còn nguy cơ non-stationarity dài hạn (T3-WOBBLE branch tiềm tàng).
          PHÒNG: EARLY-STOP-BY-TASK-METRIC (QoS rolling không vượt best sau N ep) bảo vệ checkpoint tốt
          nhất khi critic chưa đủ bền cho run rất dài.
    (B-2d) ⭐ DRIFT-ATTRIBUTION (general — khi actor HEAD đi degenerate giữa run): TRƯỚC KHI đổ tội head,
          check TIMELINE NHÂN-QUẢ với critic explVar. Trình tự PHỔ BIẾN: critic destabilize (explVar dip
          âm / sụp) → advantage nhiễu → head trôi vào degenerate basin (vd wc spike→max, common-collapse
          [A], head extreme value). HIẾM KHI head tự loạn rồi critic theo sau. → ĐỌC: tại ep head extreme
          xuất hiện, explVar đã wobble/dip TRƯỚC chưa? CÓ → head là BỀ MẶT (fix head = treat triệu chứng);
          gốc thật = critic non-stationarity / λ_D / lever khác. Fix head VẪN ĐÁNG nếu head có vấn đề
          ĐỘC LẬP (vd PowerMLP wp-concentration trần QoS — quan sát Ở ĐỈNH lúc lành, tách biệt với drift),
          nhưng KHÔNG kỳ vọng fix head chữa drift.
          (Case2 result_8 ep1068→1404: explVar dip −0.21 ep1068 → sụp ep1356 → wc spike 71% ep1404 SAU.
           PowerMLP=bề mặt; gốc=critic non-stationarity run dài [B-2b T3-wobble branch] + λ_D=1.5 cho
           phép QoS-abandonment.)
    (B-2c) ⭐ ORACLE-SWEEP DIAGNOSTIC (general method — tách "giới hạn ARCH/capacity" khỏi "giới
          hạn TRAINING-DYNAMICS"): khi critic loạn dai dẳng qua nhiều run mà không rõ gốc, train
          critic OFFLINE trên 1 dataset CỐ ĐỊNH (collect random/mixed policy, reward_noise_avg cao
          ~128 để khử n0) theo GRID arch × grad_clip × target-norm, plain MSE, không PPO/policy-drift.
          Đọc MARGINAL spread mỗi knob = đòn bẩy. (Case 2: arch spread 0.104 ≫ norm 0.042 ≫ clip
          0.004 → arch TO=tốt nhất (đừng nhỏ), norm_tgt=True là lever, clip vô nghĩa.) explVar tốt
          nhất offline vs ceiling Tier-1 → nếu ≈ceiling: arch fit được, lỗi là training-dynamics
          (fixable); nếu ≪ceiling kể cả config tốt nhất: arch/feature bottleneck (redesign input).
          Script: sweep_oracle_critic.py. RẺ (CPU, ~2h), không tranh GPU, không confound policy-drift.
    LƯU Ý KIẾN TRÚC: critic là V(s_t), ĐỘC LẬP với actor readout/o_hat → đổi readout/
    entanglement KHÔNG cứu critic mù. Phải fix critic riêng. Kiểm: explVar>0.5 + V̄ đúng thang.
    LƯU Ý NÚT THẮT (Case 2 result_5/6/7/8): critic ổn KHÔNG đồng nghĩa QoS lên. Nếu QoS kẹt 1 mức
    XUYÊN SUỐT nhiều run BẤT KỂ explVar (kể cả lúc explVar cao) → nút thắt KHÔNG ở critic mà ở
    POLICY/HEAD. Đừng fix critic mãi; tách "critic health" (explVar) với "task metric" (QoS) — độc lập.
    ⭐ TÌM HEAD-NÚT-THẮT bằng COUNTERFACTUAL PROBE (general, RẺ — không cần train ablation): giữ NGUYÊN
    policy, thay 1 head bằng baseline đơn giản, recompute metric trên CÙNG state + CÙNG noise draw.
    (Case 2 result_8: QoS kẹt + user rớt trên MỌI link (IRS lẫn direct) → nghi POWER. probe_power_qos.py
    thay w_p học-được bằng CHIA ĐỀU → QoS 58%→83% (+25pts), sum-rate gần như không đổi → PowerMLP
    CONCENTRATE power (wp-top2~48%, ôm 2 user, đói 8 user) là thủ phạm QoS THẬT, KHÔNG phải phase/
    assignment. met user có 2.7× power vs unmet.) → counterfactual cô lập đúng head hỏng, rẻ hơn nhiều.
    ⭐ BỘ PROBE (analysis/, skill log-reading STEP 3.5 — symptom→probe→confirm root TRƯỚC khi fix):
    probe_critic_ceiling (#1 aleatoric ceiling) · probe_phase_quality (#2 PhaseMLP coherence |Σφ|/N +
    alignment%, GREEDY live — entropy thấp KHÔNG đủ, phải confirm pha THẬT căn chỉnh; baseline result_8
    ≈25% align) · probe_power_qos + probe_qos_by_assignment (#4 QoS head/link) · probe_irs_vs_direct
    (#2 physical IRS-opt-vs-direct ceiling). Probe = counterfactual rẻ → cô lập root, tránh tune sai head.
    LƯU Ý: head concentrate power = agent tối đa sum-rate biên ở λ_D thấp → bỏ QoS. Lever = sửa head
    đó (power-spread / QoS-aware), HOẶC λ_D (per-ramp) — nhưng λ_D↑ phải sau khi critic ổn.

[C] ENTROPY-DOMINATED — cờ "⚠", ent/pg phase>1, policy trôi về random, QoS un-learn.
    Nguyên nhân: β quá cao so với L_pg (nhỏ khi gần tối ưu / gae cao). KHẮC PHỤC:
    beta_entropy↓ (≈0.001) + anneal nhanh; nếu còn → hạ beta_entropy_min.
    ↔ Ngược với [D]: nếu đang plateau thì lại cần β cao hơn (kèm λ_D↑).

[D] PLATEAU — reward/QoS đi ngang, best không bị vượt, L_pg≈0.
    Nguyên nhân: reward phẳng dọc ridge QoS↔sum-rate; advantage bé ở gần optimum.
    KHẮC PHỤC: lambda_D↑ (nghiêng ridge → tạo dốc); n_rollout↑ (advantage ít nhiễu);
    explore burst nhẹ (β↑ vừa, KÈM λ_D↑ để L_pg không quá nhỏ → tránh [C]); gae>0.

[E] λ LƯỢNG TỬ ĐÓNG BĂNG — "λ|max| y/z" đứng yên ở init (≈0.50/0.45) suốt run.
    Nguyên nhân ban đầu (nghĩ): SPSA quá nhiễu / lr_qc thấp. KHẮC PHỤC (thử): spsa_n_reps↑,
    lr_actor_qc↑. Kiểm λ|max| có nhúc nhích khỏi init.
    ⭐ ROOT THẬT (Case2, sau nhiều lần thử — 2026-06-02): KHÔNG phải noise, KHÔNG phải zero-mean-LN.
    λgrad CÓ magnitude (mag~0.26) nhưng BATCH-MEAN ~0 (net~0.018, r=net/mag~0.06) → Adam 1st-moment≈0
    → KHÔNG có hướng nhất quán → λ đứng yên. = DIRECTIONLESS gradient (credit-assignment / advantage-
    decorrelation): tác động của encoding-scale lên return KHÔNG correlate nhất quán với advantage qua
    batch (λ ở thượng nguồn, signal pha loãng qua VQC+head; mỗi state có encoding-strength tối ưu khác
    → trung bình triệt tiêu). λ KHÔNG drift kể cả qua 1500 ep → directionless (không phải small-consistent).
    ĐÃ THỬ & THẤT BẠI (ĐỪNG lặp — DON'T-REPEAT): spsa 8→16 (r không đổi, không phải noise) ·
    Δ1 LayerNorm+affine γβ (r không đổi ở full-batch) · R1 readout + SoftmaxPQC head (r không đổi).
    ⭐⭐ PROVEN INTERFERENCE (analysis/probe_lam_interference.py, result_8 2026-06-02): per-sample
    ||g_b||=12.35 (KHỎE, KHÔNG vanishing) NHƯNG mean pairwise cos(g_b,g_b')=−0.001≈0 → per-sample
    λ-grads hướng NGẪU NHIÊN → batch-mean triệt tiêu → Adam 1st-moment≈0 → frozen. Direction-only
    cos (bỏ advantage-sign) cũng ≈0 (+0.005) → interference NỘI TẠI ở encoding-sensitivity (nhân tử
    z_enc data-dependent), KHÔNG chỉ advantage-sign. → DỨT KHOÁT: interference, KHÔNG phải barren-
    plateau/vanishing/depth. Probe này = chẩn đoán chuẩn (mean cos + ||g_b|| + ρ phân biệt 2 cơ chế).
    ⭐⭐ H1/H2/H3 DISCRIMINATOR (analysis/probe_lambda_landscape.py, OFFLINE no-train): interference
    probe chứng minh CƠ CHẾ (freeze) nhưng KHÔNG phân biệt ROOT. 3 giả thuyết: H1 λ*(s) khác theo state
    (conflict bản chất → fix λ(s)/MoE) · H2 có λ* global tốt nhưng PG không tìm ra (→ representation
    objective) · H3 λ init đã near-optimal / λ ít ảnh hưởng (→ E-fix2 không làm gì). Method: giữ ckpt cố
    định, SCALE λ, đo greedy-assignment reward/QoS trên batch states (downstream cố định). Test A global
    sweep · Test B per-state-oracle gap · Test C cluster-vs-blockage.
    KẾT QUẢ (result_8, MLP head): λ-landscape PHẲNG (reward ×0.1→×3.0 chỉ đổi ~0.1) + gap per-state≈0.04
    → ⭐ H3: λ-frozen HARMLESS, λ ít ảnh hưởng → KHÔNG cần fix (E-fix2). ⚠ CAVEAT: MLP head capacity cao
    có thể HẤP THỤ λ-variation → λ trông irrelevant. PHẢI re-run trên arch-2 (SoftmaxPQC LINEAR head):
    nếu vẫn phẳng → frozen-λ thật harmless. → ƯU TIÊN chạy discriminator TRƯỚC khi đầu tư bất kỳ λ-fix.
    ✅✅ CAVEAT ĐÃ GỠ (result_11 arch-2 ep_300, SoftmaxPQC LINEAR head softmax_head=true/r1, 2026-06-03):
    re-run discriminator → VẪN PHẲNG (Test A ×0.1→×3.0 reward 1.51→1.53, Δ(best−init)=+0.00; Test B
    per-state gap=+0.03≈0; Test C corr blockage 0.00). Head LINEAR KHÔNG hấp thụ được λ-variation mà
    landscape VẪN phẳng → H3 CONFIRMED DỨT KHOÁT: frozen-λ GENUINELY HARMLESS. → CHỐT E-fix2 (chấp nhận
    λ fixed feature-map, có thể freeze hẳn khỏi optimizer). ĐÓNG câu hỏi λ-frozen — KHÔNG xây E-fix1 /
    bất kỳ λ-lever nào nữa. (Probe vẫn IN dòng CAVEAT mặc định — đọc actor_config.json xác nhận
    softmax_head=true/r1 trước khi tin verdict; ckpt này đã xác nhận. "⭐ large→H1" cạnh gap=+0.03 chỉ là
    annotation threshold thô — verdict tích hợp A+B kết luận H3; +0.03 trên reward~1.5 = 2% = negligible.)
    ⭐ INSIGHT PUBLISHABLE: frozen quantum encoding ở đây = HARMLESS (λ ít ảnh hưởng task), KHÔNG phải
    bệnh lý barren-plateau. Củng cố honest-prior PQC≈DNN + param-eff (λ fixed = literature-consistent).
    ⚠ SMOKE λgrad r KHÔNG ĐÁNG TIN: smoke 2-ep cho r=0.65 "should move" NHƯNG full run r=0.06 "frozen".
    Vì r = net/mag, net = |mean qua MINI-BATCHES của g_λ|: smoke ~6 mini-batch entries → ít cancel → net
    cao GIẢ TẠO; full rollout ~222 entries → cancel → net→0. → CHỈ đọc r ở FULL-BATCH (full rollout),
    KHÔNG bao giờ kết luận λ từ smoke.
    ⚠ λ-FROZEN ≠ NGUYÊN NHÂN PhaseMLP IDLE (sửa misdiagnosis L11): PhaseMLP nhận z_t (AE latent) +
    channel, KHÔNG nhận o_hat (VQC output). λ chỉ ảnh hưởng VQC encoding (→ assignment), KHÔNG ảnh hưởng
    z_t. → 2 vấn đề ĐỘC LẬP. "Fix λ để gỡ phase" là sai chuỗi nhân-quả. Test phase RIÊNG = probe_phase_quality.
    HƯỚNG GỠ (ranked, chưa thử cái nào ngoài 3 cái fail trên):
      (E-fix0) lr riêng cho λ ↑5-10× — ❌ ĐÃ BÁC bằng probe_lam_interference: gradient directionless
                  (mean cos≈0) → lr cao chỉ scale 1 vector batch-mean≈0 = thêm noise. Probe = E-fix0
                  rẻ hơn (không cần train run). SKIP train run, dùng probe kết luận.
      (E-fix1 principled) AUXILIARY ENCODING-INFORMATIVENESS LOSS: thêm term train λ TRỰC TIẾP (vd maximize
                  variance/MI của o_hat w.r.t. action-relevant features) → gradient trực tiếp, bypass dilution
                  policy-gradient. Đúng gốc nhưng = research sub-project + cân loss.
      (E-fix2 ⭐ pragmatic, KHUYẾN NGHỊ cho timeline) CHẤP NHẬN λ FIXED = random feature-map (literature-
                  consistent: Jerbi/Kruse/nhiều QRL freeze encoding). VQC vẫn expressive (data re-up + θ + readout
                  train). Param-eff claim KHÔNG cần λ trainable. Có thể FREEZE λ hẳn (bỏ khỏi optimizer → rẻ hơn).
                  λ-frozen thành honest limitation, KHÔNG phải dealbreaker. Quyết bằng PERFORMANCE (phase-quality
                  + QoS), KHÔNG bằng r.
      (LOẠI) better gradient estimator (multi-term param-shift): KHÔNG cứu được true-zero gradient → vô ích.

[F] PHASE HEAD CÒN RANDOM/ĐUỐI — ent phase ~65% max, KL & grad lớn nhưng entropy
    không giảm. Phân biệt bằng GREEDY eval: greedy≈sampled → landscape pha phẳng,
    entropy là đệm → ĐỂ YÊN. greedy≫sampled → chưa hội tụ → sharpen (lr_phase↑ /
    nới n_hidden_phase / giảm entropy riêng cho phase).

[G] REWARD 1 SỐ EP RẤT THẤP — vật lý hay dao động agent? Xem Blk/K, QoS/K, R_tot:
    Blk/K cao bất thường + QoS thấp → SPAWN KHÓ (trần vật lý, hiếm).
    Blk/K bình thường + QoS thấp → DAO ĐỘNG AGENT (sample action kém ep đó, phần lớn).
    λ_D cao khuếch đại. → Báo cáo hiệu năng bằng GREEDY eval, không bằng reward rollout.

[H] CLI ĐỨNG / EP KHÔNG NHẢY nhưng process còn chạy (log mtime cũ, %CPU cao). 2 biến thể:
    (H-1) DEADLOCK GPU: 2 job cùng GPU, hoặc 1 job bị Ctrl+Z (state 'T') giữ GPU context.
          Dấu hiệu: state T (suspended) hoặc 2 PID. nvidia-smi mem giữ.
    (H-2) CUDA HANG do CONTENTION/TDR (single job): process state R, wchan=0 (userspace
          SPIN), GPU util ~0% (idle), kẹt nhiều giờ ở một PPO update. Nguyên nhân: GPU bị
          tranh bởi app KHÁC (vd GAME Windows — WSL2 chia sẻ GPU vật lý) → kernel training
          quá hạn → Windows TDR reset driver → CUDA context training hỏng → process spin.
          (Case2 result_4: treo ep96 sau khi launch lúc game đang dùng 14GB/86% GPU.)
    KHẮC PHỤC (cả 2): ps -eo pid,stat,cmd|grep train + cat /proc/PID/wchan (0=userspace spin)
    → nvidia-smi (mem thấp+util thấp = hang chứ không OOM) → kill -9 <PID> → nvidia-smi xác nhận
    → resume từ checkpoint gần nhất. (stack: py-spy dump --pid, cần ptrace_scope=0.)
    PHÒNG: 1 job GPU/lúc; dừng bằng Ctrl+C (KHÔNG Ctrl+Z); KHÔNG train khi GAME/app khác
    dùng GPU (TDR risk). Nếu BUỘC train+game: raise Windows TdrDelay + cap game FPS + giữ
    VRAM headroom (xem giải đáp train+game — rủi ro vẫn còn, runs quan trọng nên GPU sạch).

[I] λ_D SAI — reward rất âm từ đầu, QoS trần thấp / agent kẹt DƯỚI baseline tầm thường.
    Hai biến thể:
    (I-1) R_LoS lớn → nhiều user bất khả thi → λ_D phạt cả user không thể phục vụ.
          KHẮC PHỤC: xem "Servable %"; nếu <~95% → HẠ lambda_D.
    (I-2) λ_D QUÁ CAO ở ramp DỄ (penalty-domination → critic không học được). Khi một
          policy TẦM THƯỜNG đã đạt ~100% QoS (xem "Direct-only %" / "All-IRS %"), QoS là
          trivial → λ_D cao chỉ tạo qp ≫ sum-rate → reward âm sâu & cực nhiễu → critic
          cold-start KHÔNG fit nổi (explVar~0.1, kéo theo [B]) → advantage nhiễu → agent
          kẹt DƯỚI cả baseline Direct-only. KHẮC PHỤC: HẠ λ_D mạnh (vd 3.0→1.5-2.0) để
          return học được; nếu đang fresh thì resume từ checkpoint để critic warm-up
          re-fit V về thang mới. Dấu hiệu nhận biết: qp/step ≫ R̄, explVar kẹt ~0.1,
          QoS < Direct-only baseline.
    QUY TẮC CALIBRATE λ_D: nhắm theo baseline TẦM THƯỜNG TỐT NHẤT (gồm cả Direct-only,
    KHÔNG chỉ All-IRS). Trivial-baseline ~100% QoS → giữ λ_D THẤP. Baseline thấp/khó →
    λ_D cao hơn. (result_1 Case 2: Direct-only=100% mà dùng λ_D=3.0 → critic mù, kẹt 54%.)
    (I-4) ⭐⭐ λ_D=1.5 = SWEET SPOT cho ramp R_LoS=0.2 BASELINE (chốt 2026-06-08 sweet-spot search):
          Evidence:
            r11 Case 2 (λ=1.5 fresh): best 214 @ep1261, mild drift -52% from peak → STABLE
            r3 Case 1 (λ=1.5 fresh): best 191 @ep552, modest drift to -123 → workable
            r6 Case 2 (λ=1.5 fresh ongoing): climbing healthy
            r1/r17 (λ=2.0 fresh): peak early then CATASTROPHIC drift -350% to -420% from peak
            r2/r4 (λ_D bump on resume): LAMBDA-BUMP-COLLAPSE [K] catastrophic
          RULE: ramp=0.2 fresh launch → λ_D=1.5 (BOTH Case 1 + Case 2 + Case 3).
          Higher λ_D at R_LoS=0.2 → drift-prone equilibrium (too aggressive penalty before
          QoS landscape hardens). Save λ_D≥2 cho ramp 0.3+ where Direct-only QoS drops below 95%.

[K] LAMBDA-BUMP-COLLAPSE — bump λ_D mid-training on resume → cascade failure.
    Symptoms (canonical r2 Case 1 λ 2→3 from r17/ep1000):
      • Best reward drops to negative immediately after resume (-226 @ep11)
      • Reward μ collapses rapidly: -500 → -1000 → -1200 within 200ep
      • ent_ck → 0 (Ck head deterministic, top user gets ~100% common rate)
      • ‖∇‖ck explodes pre-clip: 5000-90000 (clipped to 0.5)
      • V̄ goes deep negative (-100 to -200) reflecting return scale shift
      • KL_ck > 0.5 DIVERGENCE flag fires
    Mechanism (5-step cascade):
      1. λ_D bump shifts reward landscape, returns drop ~λ_D×qp magnitude immediately
      2. Critic V at OLD scale → advantage = return - V huge negative magnitude
      3. PPO grad ∝ advantage × (one_hot - π) on smallest-action-space head (CkMLP within-group)
      4. Ck softmax collapses to one-hot deterministic (ent_ck=0, top user 96%+)
      5. Once collapsed, saturated softmax keeps grad explosion compounding
    Why CkMLP specifically (not q/pw/ph):
      Ck within-group softmax has SMALLEST action space (group size ≤ K) → softmax landscape flat →
      easiest to push to deterministic under advantage explosion.
    KHẮC PHỤC: KHÔNG bump λ_D on resume. Always fresh restart cho λ_D changes.
      Acceptable: graceful gradient λ_D over 200ep linear interp (untested, theoretical).
      Acceptable: ramp R_LoS on resume (different mechanism, OK per r14 evidence).

    (I-3) ⭐ λ_D PHẢI TĂNG KHI RAMP TĂNG — quy tắc curriculum, áp lực QoS scale theo độ khó.
          Lý do gốc (chứng minh bằng probe_wc_optimality result_11 ep600 R_LoS=0.2 λ_D=1.5):
          sum-rate-opt và λ_D-opt KHÁC NHAU (wc=0.80 vs 0.60). λ_D=1.5 đang ÉP wc xuống 0.60
          để bảo vệ QoS = đúng cho ramp dễ này (Direct-only=100% → QoS slack, ép nhẹ là đủ).
          Khi ramp lên (0.3, 0.5+): IRS THIẾT YẾU, QoS slack BIẾN MẤT, sum-rate-max bắt đầu
          phải BỎ RƠI weak users để tối ưu R_tot. Nếu λ_D KHÔNG tăng theo → agent học bỏ user
          (user-abandonment) → QoS sụp. Mà 1 khi học rồi RẤT KHÓ GỠ (= [B-2d] head trôi degenerate).
          QUY TẮC RAMP TRANSITION:
            • Trước mỗi resume sang ramp mới: chạy feasibility probe → đo Direct-only/All-IRS QoS.
            • Direct-only ≥95% → λ_D thấp (1-2): QoS gần free, ép nhẹ.
            • Direct-only 70-90% → λ_D trung (2-3): QoS bắt đầu cần lever.
            • Direct-only <50% → λ_D cao (3-5): IRS thiết yếu, áp lực QoS phải binding.
            • Direct-only <30% → kiểm Servable trước (có thể [I-1]: cần HẠ vì user bất khả thi).
          ⚠ ASYMMETRIC RISK: λ_D thấp → "user abandonment" (khó gỡ); λ_D cao → "penalty-dom" ([I-2],
          sửa được bằng critic warm-up). → khi DOUBT, ưu tiên λ_D HƠI CAO HƠN; nhưng KHÔNG vượt
          calibrate-rule (tránh [I-2]). Đọc qp/step vs R̄: qp/step ≪ R̄ = an toàn nâng tiếp; qp/step ≫ R̄ = quá cao.
          ⚠ EVIDENCE PHỤ (probe ep600): ΔR per-state shrink khi λ_D tăng (0.06 ở λ_D=0.5 → 0.04 ở
          λ_D=1.5) → reward landscape sharper ở λ_D cao → policy ÍT cần per-state-adapt wc, càng ép
          QoS càng đơn giản hóa choice. Củng cố "λ_D đủ cao = QoS bảo vệ + landscape gọn".

[J] DIRECT-ATTRACTOR / IRS CHICKEN-AND-EGG — agent bỏ dần IRS, kẹt local optimum
    "all-direct"; phase head idle (đói data) → không học pha → IRS mãi tệ → càng tránh.
    Dấu hiệu: behaviour `dir` tăng dần về K, `irs=[..]` tụt về ~0, ent phase kẹt CAO
    (~50-60% max) dù IRS không dùng; QoS có thể vẫn thấp vì power/IRS path chưa học.
    Nguyên nhân (vòng luẩn quẩn): pha KHỞI TẠO NGẪU NHIÊN (env random_phi) → IRS link
    tệ lúc đầu → critic credit "IRS xấu" → agent chọn direct → phase head ít sample IRS
    → pha mãi ngẫu nhiên. Bị che bởi D_k THẤP: direct dù bị block vẫn vượt D_k (QoS
    trivial) nên IRS "không cần" cho QoS — nhưng IRS-aligned cho SUM-RATE cao hơn nhiều
    cho user bị block (beamforming gain ∝ N²) → reward tối ưu THỰC SỰ dùng IRS.
    ⚠️ CAVEAT PROBE: baseline "All-IRS %" dùng PHA NGẪU NHIÊN (AllIRSPolicy KHÔNG set
    phase_idx) → là lower-bound BẤT CÔNG, KHÔNG phải tiềm năng IRS. ĐỪNG kết luận "IRS
    tệ hơn direct" từ nó. Với pha căn chỉnh, IRS thường vượt direct (nhất là user bị block).
    → DÙNG probe_irs_vs_direct.py (so |h_IRS|_opt vs |h_direct| per user) để biết IRS-usage
      cao có optimal không. PHÁT HIỆN (Case2 R_LoS=0.2): IRS-opt thắng direct 100% ở user
      BỊ BLOCK (+38dB) nhưng chỉ MARGINAL ở non-block (+1.5dB). Pha ngẫu nhiên yếu ~14dB
      (|Σφ| N→√N) → block vẫn thắng (dùng IRS), non-block lật sang THUA (giữ direct).
      → IRS/K ≈ block-count là HỢP LÝ khi pha xấu. LEVER đẩy IRS-usage cao (route non-block)
      = CHẤT LƯỢNG PHA, KHÔNG phải assignment/readout. Thứ tự: critic→pha→readout.
    KHẮC PHỤC: (1) [mạnh nhất] PHASE WARM-START — pretrain pha về căn chỉnh kênh
    (max |g_SR·Φ·g_RU|) thay random init → agent thử IRS thì IRS đã tốt sẵn → phá vòng.
    (2) giữ exploration q-head (IRS-select) lâu hơn để phase có data (coi chừng [C]).
    (3) curriculum R_LoS tăng dần → IRS cần thêm từng chút → agent thích nghi dần,
    tránh nhảy vào ramp khó với IRS path trắng. KHÔNG resume từ checkpoint đã "tránh IRS"
    (warm-up sẽ re-fit critic theo policy direct-only → đóng đinh local optimum).
    (result_1 Case 2 @R_LoS=0.2: Blk~6/10 nhưng agent drift dir 4→8, irs→~1, phase idle.)


================================================================================
  PHẦN 4 — BẢNG TÍN HIỆU CHẨN ĐOÁN  (đọc nhanh trong training_log.txt)
================================================================================
  Đọc: dòng diag[...], rolling-50, behaviour.

  Tín hiệu          | Khỏe / Mong muốn          | Báo động → mã
  ------------------+---------------------------+----------------------------
  ── CRITIC (sức khỏe critic — đọc cụm này CÙNG NHAU) ──
  explVar (critic)  | > 0.5 & ỔN ĐỊNH           | ≈0 → mù [B-1/B-2] · dao động 0.1↔0.6 → bất ổn [B-2b]
  V̄ (mean value)    | ≈ thang return thật       | ≈±1 khi return lớn → sai thang [B-1]
  σV                | có spread (state-dep)     | ≈0 (đoán ~hằng số) → mù [B]
  ‖∇‖V (grad critic)| GIẢM dần → settle         | plateau LỚN (vd 700-1000) không giảm → bất ổn [B-2b]
  ── ACTOR/POLICY ──
  ent/pg phase      | < ~0.3                    | > 1 → entropy-dominated [C]
  L_pg              | nhỏ = gần hội tụ          | ≈0 + reward ngang → plateau [D]
  λ|max| y/z        | DI CHUYỂN khỏi init       | đứng ở 0.50/0.45 → QC chết [E]
  wc (common pwr%)  | ổn định                   | →~24% → common collapse [A]
  Ck tot            | ổn định                   | →~0.46 → common collapse [A]
  QoS/K             | tiến tới trần servable    | tụt dần → un-learn [A]/[C]
  Blk/K vs QoS      | QoS cao dù Blk cao = tốt  | (mục [G] để phân biệt)
  dir / irs=[..]    | dùng IRS khi IRS có lợi   | dir→K, irs→~0 + phase idle → [J]
  KL per head       | nhỏ, ổn định              | tăng vọt → update quá mạnh

  ⚠️ Probe baselines: "Servable %" = trần thật (best link). "Direct-only %" = direct
  thật. "All-IRS %" = IRS với PHA NGẪU NHIÊN (lower-bound, KHÔNG phải tiềm năng IRS) → [J].


================================================================================
  PHẦN 5 — NGUYÊN TẮC BẤT BIẾN
================================================================================
  1. Bake từ ep 1: gae_lambda=0.9, critic warm-up bật, β thấp + anneal nhanh.
     CRITIC = V(s) + GAE-return target + PopArt (value-target normalisation). ĐÃ GỠ
     TD(0)/target-net (forward_target/polyak/critic_target_tau bỏ — code clean 2026-06-01).
  2. Curriculum R_LoS 0.2→0.5, resume mỗi bước, ổn định rồi mới lên.
  3. KHÔNG transfer trọng số giữa các Case (shape khác: nq, output, d_s). Mỗi case
     train từ đầu nhưng bake sẵn bài học.
  4. KHÔNG thêm độ sâu MLP broadly — hidden đã rộng; thêm = khó train = kém ổn định.
     Chỉ nới bề rộng lớp đầu của net có input theo K (xem Training-Case).
  5. Re-tune λ_D qua feasibility probe ở MỖI R_LoS (đừng bê số cũ). ⭐ DEFAULT: λ_D PHẢI
     TĂNG theo ramp (xem [I-3] — Direct-only QoS↓ ⇒ λ_D↑). User-abandonment khó gỡ hơn
     penalty-dom → bias hơi cao thay vì hơi thấp khi doubt.
  6. 1 job GPU/lúc; dừng bằng Ctrl+C. Lấy best model theo QoS từ checkpoints/.
  7. lr_decay_start & beta_entropy_anneal_end là % của --episodes → đổi episodes nhớ
     canh lại fraction.
  8. ⭐ THỨ TỰ ƯU TIÊN TỐI ƯU (khi bắt đầu ramp, đặc biệt R_LoS=0.2 — cũng là thứ tự ĐỌC
     KẾT QUẢ): CRITIC → PhaseMLP (học optimize IRS) → KHÔNG-IDLE (mọi head, đặc biệt các
     agent SAU VQC: phase/power/ck) → SUM-RATE → QoS.
     (⭐ ĐỔI 2026-06-03: SUM-RATE giờ ĐỨNG TRƯỚC QoS — trước đây QoS→sum-rate.)
     LÝ DO: (a) critic mù → advantage nhiễu → MỌI ramp sau khó optimize → critic là gốc,
     fix TRƯỚC. (b) PhaseMLP phải học optimize IRS Ở RAMP DỄ (0.2, QoS còn slack); không
     học được ở đây thì ramp khó không bao giờ. (c) head idle (entropy phẳng ~max, không
     hội tụ) = lãng phí capacity + chặn pipeline phía sau VQC.
     (d) ⭐ SUM-RATE TRƯỚC QoS vì: sum-rate là objective PhaseMLP TRỰC TIẾP cải thiện (phase tốt
     → effective channel tốt → rate↑) → gradient DÀY & ALIGNED với việc của PhaseMLP. QoS là metric
     NGƯỠNG (đạt D_k hay không), bị POWER/assignment chi phối, KHÔNG thưởng cải thiện channel biên →
     tín hiệu NGHÈO/sai cho PhaseMLP. Ở ramp dễ (0.2) Direct-only QoS=100% → QoS BÃO HÒA, vô tín hiệu
     cho IRS; sum-rate vẫn thưởng dùng IRS. Khớp C1 (sum-rate = OBJECTIVE, QoS = CONSTRAINT).
     (e) QoS = constraint, ÉP bằng λ_D Ở RAMP SAU (per feasibility probe) khi IRS/QoS trở nên BINDING.
     ⚠ TIMING: ĐỪNG trade-in QoS QUÁ MUỘN — sum-rate-max kéo dài có thể bake-in USER-ABANDONMENT ([B-2d])
     khó gỡ; tái áp λ_D Ở RAMP mà IRS bắt đầu CẦN THIẾT (users rớt khỏi tầm direct), đừng để policy khóa
     vào thói bỏ user. → ĐỪNG nhảy xuống sum-rate/QoS khi critic/PhaseMLP/head chưa khỏe; đo task-metric
     trên nền chưa ổn = vô nghĩa (xem [B-2b] lưu-ý QoS⊥critic).


================================================================================
  GHI CHÚ BẢO TRÌ
================================================================================
  Khi gặp & giải quyết lỗi MỚI mang tính CHUNG (đúng cho mọi case) → cập nhật file
  này: thêm mã kế tiếp [J]/[K]… vào Phần 3 (triệu chứng→nguyên nhân→cách sửa) và (nếu
  có tín hiệu nhận biết) 1 dòng vào Phần 4. KHÔNG nhân đôi mã đã có — mở rộng mã cũ.
  Lỗi/bài học theo hyper CỤ THỂ của 1 case → cập nhật Training-Case-<N>.txt (không để ở đây).
  Quy trình này do skill "irs-rsma-log-reading" (Step 5) tự động thực hiện sau mỗi lần đọc log.
================================================================================
