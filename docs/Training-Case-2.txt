================================================================================
  TRAINING CASE 2  ·  K=10, M=2  ·  hyperparameter theo từng ramp R_LoS
================================================================================
  Kiến thức chung (cơ chế, bảng tra lỗi, tín hiệu) → Common-Knowledge.txt.
  Bài học gốc kế thừa từ Case 1 → Training-Case-1.txt.
  File này: setup Case 2 + BẢNG TÓM TẮT RUN (dưới) + nhật ký chi tiết (L1-L14 cuối file).
  Trạng thái: R_LoS=0.2 đang tinh chỉnh kiến trúc (architecture-2). Cập nhật: 2026-06-02
--------------------------------------------------------------------------------

================================================================================
  BẢNG TÓM TẮT RUN  (quick-reference — chi tiết xem L1-L14 ở "NHẬT KÝ RUN" cuối file)
================================================================================
  Tất cả ở R_LoS=0.2, K10 M2 nq12. "=" = giữ như run trước. Lever = thứ MỚI thử mỗi run.
  Cột: λ_D | β(entropy) | noise(reward_avg) | critic | spsa | PowerMLP | enc/readout/head

  Run    | λ_D | β anneal      | noise | critic        | spsa | PowerMLP        | enc/readout/head      | → RESULT                          | LIMITATION (chưa fix)
  -------+-----+--------------+-------+--------------+------+----------------+----------------------+----------------------------------+----------------------------------
  r1 L1  | 3.0 | 0.002        | 8     | TD0 warmup   | 8    | single-softmax | LN/generic25/MLP     | STUCK QoS54%, critic mù explVar.1 | λ_D quá cao→penalty-dom [I-2]
  r1 L2  | 1.5 | 0.002        | 8     | =            | =    | =              | =                    | reward bớt âm, QoS60%; critic mù  | critic explVar0.07 + entropy-dom
  r2 L3  | 1.5 | 0.001/anneal | 8     | =            | =    | =              | =                    | entropy-dom TẮT; phase 40→35      | critic VẪN mù explVar0.08 (H1 bác)
  r3 L4  | 1.5 | =            | 16    | +warmup fresh| =    | =              | =                    | explVar 0.08→0.4-0.6 (1 phần)     | critic bất ổn: grad-plateau, wobble
  r4 L5  | 1.5 | =            | 16    | TD0→GAE-ret  | =    | =              | =                    | grad V giảm (tích cực)            | TREO ep96 (CUDA/game [H]) inconcl.
  r5 L6  | 1.5 | =            | 16    | GAE-ret      | =    | =              | =                    | —                                 | GAE 1 mình ko đủ: explVar wobble±, QoS thoái hóa
  r6 L7  | 1.5 | =            | 16    | lr_crit→3e-4 | =    | =              | =                    | —                                 | lr↓ đơn lẻ ko fix (clipfrac~100%)
  r7 L8  | 1.5 | =            | 16    | grad_clip→1k | =    | =              | =                    | clipfrac 100%→0%                  | explVar VẪN wobble (clip=triệu chứng)
  r8 L9  | 1.5 | =            | 16    | +FULL PopArt | =    | =              | =                    | ⭐GOLD: critic stable .45, QoS51→60%, best233, reward âm→+ | drift @ep1500; trần QoS58% (wp-conc); λ frozen; phase idle
  r9     | 1.5 | =            | 16    | PopArt       | 8    | factored bias+1.0 | =                 | priority3 PowerMLP ok             | bias quá mạnh: wc LOCK70%→starve phase
  r10 L11| 1.5 | =            | 16    | PopArt       | 8    | factored bias+0.3 | =                 | PowerMLP ok (π_split57% no-lock)  | phase REGRESS (root=λ frozen); QoS52%
  r11 L13| 1.5 | =            | 16    | PopArt       | 16   | factored bias+0.3 | =                 | task hơi tốt hơn r10              | ❌λ VẪN frozen r0.06 (spsa ko fix=STRUCTURAL); ko vượt r8
  r13*   | 1.5 | =            | 16    | PopArt       | 16   | factored bias+0.3 | Δ1 affine ONLY    | (partial code, dir xóa nhầm)      | thiếu Δ4+R1 → λ grad yếu ~2.8e-4 vẫn frozen
  arch2  | 1.5 | =            | 16    | PopArt       | 16   | factored bias+0.3 | ⭐Δ1 affine + R1(42obs) + SoftmaxPQC head | smoke: λgrad r0.06→0.65! (CHƯA full run) | TBD — full run R_LoS=0.2 next
  -------+-----+--------------+-------+--------------+------+----------------+----------------------+----------------------------------+----------------------------------
  * r13 = run launch 15:09 GIỮA lúc sửa code → chỉ load Δ1, thiếu Δ4/R1-wiring (commit 19:22). Dir xóa nhầm, ko khôi phục.

  ⭐ GOLD BASELINE = r8 (best reward 233, QoS peak 60%). Mọi arch mới phải VƯỢT r8.
  ⭐ 4 LIMITATION CỐT LÕI còn lại sau r8 (priority order Common Part5 #8):
     #1 critic drift run-dài (ep1000+) — PopArt stable ngắn hạn, cần non-stationarity lever (chưa làm)
     #2 PhaseMLP idle (ent ph ~56-61% max) — gated bởi #3
     #3 λ FROZEN [E] — STRUCTURAL (LN zero-mean) → arch2 Δ1 affine + SoftmaxPQC nhắm fix (r=0.65 smoke)
     #3 PowerMLP wp-concentration trần QoS58% → factored arch (r9/r10) nhắm fix
--------------------------------------------------------------------------------

# ── CẤU HÌNH CASE 2 (params.py ACTIVE CASE) ─────────────────────────────────
K=10, M=2, P_S_dBm=70, n_qubits=12, n_latent=24.   d_aff = K*(M+2)+2M = 44.

# ── KIẾN TRÚC (hidden dims) — KHÁC Case 1 ───────────────────────────────────
n_hidden_ae    = [128, 64]            # BẮT BUỘC (từ [32]): decoder dựng lại d_aff=44.
n_hidden_ck    = [128, 128, 64, 32]   # nới lớp đầu (input 5K=50).
n_hidden_power = [128, 128, 64]       # nới lớp đầu (input 2K+nlat=44).
n_hidden_phase = [64, 128, 256, 128]  # giữ (hoặc 128,128,256,128 nếu phase đuối).
n_hidden_post  = [256, 128]           # giữ (đã rộng).
critic_hidden  = [512, 256, 128, 64]  # giữ (512 ≫ input 44).
# NGUYÊN TẮC: KHÔNG thêm độ sâu broadly — chỉ nới bề rộng lớp đầu net input-theo-K.

# ── HYPER ỔN ĐỊNH (kế thừa Case 1 — bake từ ep 1) ───────────────────────────
gae_lambda=0.9
critic_target_tau=0.02
beta_entropy=0.001, beta_entropy_min=0.0003, beta_entropy_anneal_end=0.1
n_rollout_episodes=12        # cân nhắc 16 (K=10 variance cao hơn).
lr_decay_start=0.2, lr_min_frac=0.25
reward_noise_avg=8           # cân nhắc 12 nếu reward quá nhiễu ở K=10.
D_k_bps_hz=0.1, user_free_radius_frac=0.4

# ── HYPER ĐIỀU CHỈNH RIÊNG CHO CASE 2 (nq=12, K=10) ─────────────────────────
spsa_n_reps   = 8            # TĂNG từ 4: nq=12 → SPSA nhiễu hơn → λ dễ đóng băng [E].
lr_actor_qc   = 1e-4         # cân nhắc 2e-4 nếu λ|max| đứng yên sau vài trăm ep.
critic_warmup_episodes = 24  # TĂNG từ 16: state lớn hơn, variance cao hơn.
critic_warmup_epochs   = 50

lambda_D = THEO SCHEDULE BÊN DƯỚI  # đừng bê thẳng 4.5 từ Case 1 — calibrate per-ramp.


================================================================================
  SCHEDULE λ_D  (rút từ Case 1, điều chỉnh cho Case 2)
================================================================================

# ── BẢNG KHỞI ĐIỂM (theo pattern Case 1: λ_D ≈ 3 + 10·(R_LoS−0.3) cho R_LoS≥0.3) ──
#   R_LoS  | λ_D khởi điểm
#   -------+--------------
#   0.2    | 3.0
#   0.3    | 3.5
#   0.4    | 4.0
#   0.45   | 4.5
#   0.5    | 4.5 – 5.0
#
# ⚠️ ĐÂY CHỈ LÀ KHỞI ĐIỂM. Case 2 KHÁC Case 1: P=70dBm (+20dB → link mạnh hơn nhiều
#    → dễ phục vụ hơn) nhưng K=10 (tải nặng hơn), M=2. Net difficulty CHƯA BIẾT →
#    PHẢI calibrate qua feasibility probe ở từng ramp.

# ── QUY TRÌNH CALIBRATE MỖI RAMP (quan trọng hơn con số khởi điểm) ──────────────
#   1. Chạy → đọc dòng feasibility probe: "Servable %" và "All-IRS QoS %".
#   2. Nếu Servable < ~95% → ĐỪNG đẩy λ_D cao (phạt user bất khả thi, lỗi [I]); giữ vừa.
#   3. So All-IRS baseline với Case 1 cùng R_LoS:
#        - Case 2 CAO HƠN nhiều (dễ hơn nhờ P↑) → HẠ λ_D ~0.5–1 so với bảng trên.
#        - Thấp hơn → giữ/tăng.
#   4. Theo triệu chứng mà tinh chỉnh:
#        - plateau [D] (reward phẳng, best không vượt)      → TĂNG λ_D
#        - reward âm sâu ngay đầu / QoS trần thấp [I]        → GIẢM λ_D

# ── KIM CHỈ NAM: nhắm λ_D theo All-IRS baseline (map từ Case 1) ─────────────────
#   All-IRS QoS ≈ 70%  → λ_D ≈ 3.0
#   All-IRS QoS ≈ 65%  → λ_D ≈ 3.5–4.0
#   All-IRS QoS ≈ 60%  → λ_D ≈ 4.5
#   (Case 1 ref: 0.2→70%, 0.3→63%, 0.4→60%, 0.45→59%, 0.5→59%, servable luôn 98.9%.)

# ── LÝ THUYẾT: λ_D KHÔNG cần scale theo K ──────────────────────────────────────
#   λ_D là trade-off PER-USER (QoS vs sum-rate biên). Penalty là Σ trên K user nhưng
#   cân bằng per-user độc lập K; advantage được chuẩn hóa per-rollout. → schedule trên
#   hợp lý cho K=10; chỉ calibrate theo ĐỘ KHÓ (probe), KHÔNG nhân theo số user.


================================================================================
  HYPER THEO TỪNG RAMP R_LoS  (điền khi train)
================================================================================
  Mẫu mỗi ramp:
    R_LoS = X.X | resume từ: ____ | λ_D: ____ | episodes: ____
      Feasibility: Servable __% | All-IRS __% | Direct __%
      Kết quả: QoS đỉnh __% @ep__ | wc/Ck: __ | sự cố [A-I]: __
      Checkpoint tốt nhất (theo QoS): ep_____
      Bài học / hyper cần đổi: ____

── R_LoS = 0.2 ──  (BẮT ĐẦU: fresh, AE-pretrain bật)
    Feasibility (cố định): Servable 100% | Direct-only 100% (ΣRtot 1.72) | All-IRS 76.8% (ΣRtot 1.17)
    → QoS TRIVIAL (Direct=100%); thử thách thật ở 0.2 = SUM-RATE + agent có HỌC OPTIMIZE IRS không.

    [Lần 1] result_1 λ_D3.0 β0.002 fresh ep4000:
       STUCK QoS 54-60% < Direct100%. critic MÙ explVar~0.1 [B], λ frozen [E], reward ~-400.
       Nguyên nhân: λ_D=3.0 quá cao (Direct đã 100%) → qp(4.2)≫sumrate(1.5) → penalty-dominated
       → critic cold-start ko fit. → FIX: hạ λ_D.  [→ Common-Knowledge [I-2]]

    [Lần 2] result_1 λ_D1.5 β0.002 fresh ep4000 (~ep233):
       λ_D↓ giúp reward bớt âm (-400→+44), qp 4.2→~1.0, QoS nhích 54→60%. NHƯNG:
       - critic VẪN MÙ: explVar kẹt ~0.07, σV~0.3 (đoán hằng số) → λ_D KHÔNG đủ gỡ critic.
       - PhaseMLP IDLE: ent ph phẳng ~41 (~62% max), KHÔNG giảm → KHÔNG học optimize IRS [F/J].
       - ⚠ENTROPY-DOMINATED cả run (phase ent/pg~1.05) dù fresh (phase L_pg quá nhỏ).
       Nguyên nhân kép: critic mù (ko tín hiệu) + entropy-domination (β ép phase random).
       → FIX: hạ β (gỡ entropy-domination → policy bớt random → return ổn → critic fit → phase học).

    [Lần 3] result_2 λ_D1.5 β0.001 anneal0.1 fresh ep4000 (~ep983) — KẾT QUẢ:
       ✅ β-fix LÀM ĐÚNG: ⚠ENTROPY-DOMINATED TẮT (ent/pg ph 1.05→0.17-0.26); phase entropy
          bắt đầu giảm chậm (40→35, vẫn ~53% max).
       ❌ NHƯNG critic VẪN MÙ: explVar bounce 0-0.23, kẹt ~0.08, KHÔNG leo qua 0.5 — kể cả
          sau β về sàn (ep400) tới ep983. QoS phẳng ~57-60%. λ frozen (r~0.08).
       → KẾT LUẬN: H1 (entropy→critic mù) BỊ BÁC. Critic mù là H2 = critic KHÔNG FIT NỔI
          return (variance cao + policy còn stochastic + TD(0)-target/GAE-eval mismatch).
          Critic là V(s), ĐỘC LẬP readout → đổi readout KHÔNG cứu được critic.

    [Lần 4] result_3 λ_D1.5 β0.001 + reward_noise_avg16 + critic warm-up FRESH (24×50)
            fresh ep4000 (~ep133) — KẾT QUẢ:
       ✅ MỘT PHẦN: warm-up explVar=0.79; train explVar lên ~0.4-0.6 (so result_2 chỉ 0.08)
          → warm-up + noise_avg=16 gỡ MÙ một phần. Hướng đúng.
       ❌ NHƯNG critic BẤT ỔN (under-damped): grad V drop 2475→721 rồi PLATEAU 600-1000
          (KHÔNG giảm tiếp, còn nhích lên), explVar DAO ĐỘNG 0.14↔0.64 (không giữ >0.5),
          QoS KẸT ~51%. → critic "đuổi" TD(0) target nhiễu, không settle → advantage nhiễu
          → policy không cải thiện.
       → KẾT LUẬN: warm-up+noise_avg đúng hướng nhưng critic cần ỔN ĐỊNH hơn. Root: TD(0)
          target phương sai cao + mismatch (explVar đo vs GAE-return).

    [Lần 5] result_4 λ_D1.5 β0.001 noise16 warmup + CRITIC TARGET=GAE-return (t['ret']).
       Code: train.py:1395 td_target→ret. — KẾT QUẢ (CHẾT SỚM ep96, salvage ep1-96):
       ✅ GAE-target GIẢM grad V: ~400-500 (vs result_3 TD0 plateau 700-1000), trend xuống nhẹ
          → đúng dự đoán (target ít nhiễu). Tín hiệu TÍCH CỰC một phần.
       🟡 explVar: 0.52-0.57 đầu (ổn hơn chút) rồi wobble 0.02-0.43. QoS vẫn ~51-53%.
          → CHƯA KẾT LUẬN (chết ở ep96, chưa qua β-anneal ep400 / critic chưa settle).
       💥 TREO ep96: userspace CPU-spin 6.4h, GPU idle, single job → KHÔNG phải code/GAE
          (chạy 8 PPO update OK). Nhiều khả năng CUDA hang do GAME Windows tranh GPU lúc
          launch (TDR — Windows reset GPU driver khi kernel quá hạn under contention). [H]
       → GIỮ GAE-target (grad-giảm tích cực). Chạy lại trên GPU SẠCH (no game), qua ep400.

    [Lần 6] result_5 GAE-target, GPU sạch, chạy tới ep1055 (qua ep400) — KẾT QUẢ:
       🔴 GAE-target KHÔNG ổn định critic [B-2b]: explVar WOBBLE −0.46↔+0.69 (TB ~0.38,
          KHÔNG giữ >0.5, có lúc ÂM); ‖∇‖V dao động RỘNG 300-1300 (không settle). Không
          khá hơn result_3 đáng kể.
       🔴 QoS THOÁI HÓA: 51→45-47% (đáy ~45% ep600-800); reward GIẢM -138→-271; R_tot
          TĂNG 1.42→1.92. → QoS↓+R_tot↑+reward↓ = policy thoái hóa do advantage nhiễu
          (critic loạn), KHÔNG phải tối ưu-cho-rate. best 169.9 (ep đầu), không bị vượt.
       → KẾT LUẬN: GAE-target một mình KHÔNG đủ. Critic vẫn under-damped.

    >>> ĐIỂM DỪNG (cho conversation mới) — CẬP NHẬT 2026-05-31 <<<
    ĐÃ THỬ ở R_LoS=0.2 (result_1→5): λ_D 3.0→1.5 · β 0.002→0.001 · reward_noise_avg 8→16 +
      critic warm-up FRESH · GAE-return target. → critic từ MÙ (explVar 0.08) thành
      FUNCTIONAL-NHƯNG-LOẠN (explVar đạt 0.6-0.79 đôi lúc, không giữ). QoS chưa từng >60%.
    ĐÃ ĐỔI params: lr_critic 5e-4 → 3e-4 (line params.py:202). Sẵn sàng cho result_6.

    >>> DIAGNOSTIC PIPELINE (4 tiers, build 2026-05-31) — CHẠY TRƯỚC result_6 <<<
    Mục đích: tách nguyên nhân critic loạn giữa ENV (aleatoric) / PIPELINE / THEORY.
    Files đã thêm vào repo:
      Tier 1  probe_critic_ceiling.py     · CSI/env_probe.py
      Tier 2  RL/critic_diag.py           · train.py inserts (crit-diag block + JSONL)
      Tier 0  train_oracle_critic.py
      Tier 3  analyze_critic_run.py
    Output: results/probe_critic_ceiling_*.txt · results/oracle_critic_*.txt
            results/result_N/critic_diag.jsonl  · results/result_N/analysis_critic/

    KẾT QUẢ PROBE BAN ĐẦU (--quick run, 2026-05-31):
      ⭐ Ceiling explVar(V(s)) phụ thuộc POLICY:
         random policy        → 0.16 (variance dominated by random actions)
         result_5 ep_01000    → 0.81 (trained policy, action variance thấp hơn)
      → Observed online explVar ~0.40 (result_5) so với ceiling 0.81 ở trained-policy:
         GAP 0.41 → critic VẪN CÒN nhiều room. KHÔNG phải env-bound như nghĩ ban đầu.
      → Per-source contribution (H-step return, trained policy):
         policy stoch baseline (all env off): σ 1.98
         channel_resample only:               σ 2.67  ← nguồn ENV lớn nhất
         CSI κ only:                          σ 2.41
         mobility only:                       σ 2.27
         n0 only:                             σ 2.06  ← nhỏ nhất
         (sample size n=10 R=8 quick mode — chạy full để tin cậy)

    PHÁT HIỆN ĐẦU TỪ TIER 2 (smoke test, 14 ep — KHÔNG dùng làm dữ liệu thật):
      ⚠ clipfrac = 100% — grad_clip_critic=1.0 đang cắt MỌI minibatch → đã fix ở result_7 (clip=1000)
        NHƯNG clip KHÔNG phải nút thắt gốc (xem VERDICT bên dưới — H1 bị bác).
      ⚠ Critic layers dead 46-63% (ReLU) — nhưng oracle cho thấy arch 512 vẫn fit tốt nhất → ko phải vấn đề.
      ⚠ "17/44 dead state dims" = FALSE POSITIVE (ngưỡng tuyệt đối 1e-3 trên state scale ~1e-3).
        Đã fix detector scale-aware → thực ra: 10 const-by-design (demand block D_k cố định) + 2 collapsed
        (p_m=|g_SR|² scale ~1e-7). KHÔNG bệnh lý.
      ⚠ bias |V-ret| swing → return scale drift → ĐÚNG, là đòn bẩy thật (oracle norm_tgt + PopArt result_8).

    TRẠNG THÁI result_6 (đang chạy khi build pipeline): lr_critic=3e-4 ĐƠN LẺ
      (grad_clip vẫn 1.0, reward_noise_avg16). KHỞI ĐỘNG TRƯỚC Tier-2 → KHÔNG có
      critic_diag.jsonl. Tới ep305: explVar chạm 0.61 nhưng QoS kẹt 49%, reward μ -119,
      best 175.6 không vượt 274 ep → CÙNG pattern stuck. → lr↓ đơn lẻ KHÔNG đủ (đúng
      như Tier-2 dự đoán: clipfrac=100% chưa được fix).

    >>> KẾT QUẢ result_6 + result_7 + ORACLE SWEEP (2026-05-31) — VERDICT <<<
    result_6 (lr_critic 3e-4 ĐƠN LẺ, clip=1.0): stuck. explVar chạm 0.61 lúc đầu rồi
      wobble, QoS 49%, best 175.6 unbeaten 274ep. → lr↓ đơn lẻ KHÔNG đủ.
    result_7 (grad_clip 1.0→1000, Tier-2 on, dừng ep587):
      ✅ CƠ HỌC ĐÚNG: clipfrac 100%→0.0% (preclip μ~200 < 1000 → clip thông hoàn toàn;
         preclip tự co từ 400-1300 xuống ~200 vì critic fit được). Tín hiệu sớm tốt (ep11-29 explVar 0.6).
      ❌ NHƯNG KHÔNG GIẢI QUYẾT: qua β-anneal, explVar SỤP lại wobble −0.37↔+0.62 (mean 0.29),
         QoS kẹt 52% (R_tot↑1.50→1.61 = tối ưu rate, bỏ QoS), best 185 unbeaten 538ep [D].
      → H1 (grad_clip là nút thắt) BỊ BÁC. clip=1.0 bind chỉ là TRIỆU CHỨNG, không phải bệnh.

    ORACLE SWEEP (32 config arch×clip×norm, 20k samples clean noise=128, results/oracle_sweep_K10M2_R0.20.txt):
      MARGINAL spread (= đòn bẩy): arch 0.104 ≫ norm_tgt 0.042 ≫ grad_clip 0.004.
      • grad_clip spread 0.004 = VÔ NGHĨA → xác nhận ĐỘC LẬP H1 bị bác (clip không phải lever).
      • arch: 512x256x128x64 (HIỆN TẠI) fit TỐT NHẤT (0.342 > 256x128 0.268 > 128x64 0.244)
        → H2 (over-param) BÁC. ĐỪNG thu nhỏ critic.
      • norm_tgt=True > False (+0.042) → ĐÒN BẨY THẬT. + live bias=−0.79 xác nhận return scale-drift.
      • Ceiling oracle ~0.38 (random-policy data) — KHÁC Tier-1 trained-policy ceiling ~0.81.

    ⭐ PHÁT HIỆN LỚN: QoS kẹt ~52% XUYÊN SUỐT result_5/6/7 BẤT KỂ trạng thái critic (kể cả lúc
      explVar 0.6). → critic KHÔNG phải nút thắt chính của QoS. QoS kẹt vì agent route ~5/10 user
      lên IRS PHA NGẪU NHIÊN (PhaseMLP idle ent~57% phẳng, λ frozen) → rớt. Đáng lẽ giữ non-block
      trên direct + phục vụ block qua IRS-pha-tốt. → vấn đề PHA/HỌC, KHÔNG phải critic/penalty.

    LEVER ĐÃ LOẠI (ĐỪNG lặp — có bằng chứng):
      • λ_D↑ : result_1 λ_D=3 → qp4.2≫sumrate1.5 → penalty-dominated → critic MÙ [I-2]. KHÔNG tăng.
      • grad_clip : oracle spread 0.004 + result_7 wobble → không phải lever. (giữ 1000, vô hại.)
      • critic arch nhỏ lại : oracle → TO = tốt hơn. Giữ [512,256,128,64].
      • readout redesign / frozen-λ : per VQC-Readout-Design SEQUENCING, chỉ A/B SAU khi critic ổn
        (explVar>0.5 ổn định). Readout KHÔNG sửa frozen-λ (output vs encoding — khác phần mạch). Premature.

    >>> CHIẾN LƯỢC ĐÃ CHỐT (user, 2026-05-31): S1 critic-first, bản TOÀN DIỆN. Thứ tự ưu tiên
        ramp 0.2 = CRITIC → PhaseMLP (học optimize IRS) → KHÔNG-IDLE head (đb sau VQC) → QoS → SUM-RATE
        (đã ghi Common-Knowledge Part5 #8 + skill). QoS KHÔNG ép ở 0.2 — trade bằng λ_D ramp sau.
        PRETRAIN = phương án CUỐI CÙNG. Implement toàn diện thì OK; nếu phải approximate → kèm diagnose.

    result_8 = FULL PopArt (toàn diện — van Hasselt 2016): critic dự đoán value CHUẨN HÓA (running
      μ,σ của return); khi μ,σ update → RESCALE lớp cuối (W,b) bảo toàn output (phần "ART", chống
      gián đoạn V); un-scale ở read cho GAE. Gỡ tận gốc scale-drift (bias −0.79), oracle-backed
      (norm_tgt +0.042). Giữ arch512 clip1000 λ_D1.5 GAE-target.

      ── PASS CRITERIA result_8 (T1-T5, đọc log theo đây) ──────────────────────────────
      T1 grad-scale  : preclip‖∇‖V μ < ~20 (result_6/7 là 400-1300), clipfrac~0%   [crit-diag]
      T2 bias        : |bias=μ(V−ret)| < 0.3 BỀN, không trend (result_7 trôi −0.79)  [crit-diag/analyzer]
      T3 ⭐explVar    : post-ep400 mean>0.5 · std<0.15 · min>0.3 = STABLE (hết wobble) [analyzer ★T3]
      T4 tracking    : PopArt track Δμ nhỏ so σ, σ bám ret_σ                          [crit-diag PopArt]
      T5 POP correct : |ΔV|max < 1e-3 (toán đúng → ~1e-15; ĐÃ verify smoke 1.8e-15)   [crit-diag PopArt]
      ĐỌC KẾT QUẢ:
        T1+T2+T4+T5 PASS + T3 STABLE → ✅ gốc critic = scale-drift, PopArt gỡ → sang STAGE PhaseMLP.
        T1+T2+T4+T5 PASS NHƯNG T3 WOBBLE → ❌ bác scale-drift; gốc = NON-STATIONARITY policy-drift
          → lever mới: target-net cho value / GAE-λ thấp hơn / freeze-then-fit critic.
        T5 ⚠BUG (|ΔV| lớn) → lỗi implement POP, fix trước khi kết luận.
      Sau ~ep700: python analyze_critic_run.py results/result_8 → summary.txt tự verdict T3/T5.
      ──────────────────────────────────────────────────────────────────────────────────

    SAU result_8 (critic ổn) → STAGE PhaseMLP (học optimize IRS, KHÔNG pretrain trước):
      thứ tự thử (data-flow trước, pretrain cuối): optimistic q-init → entropy-target q-head →
      counterfactual phase loss (PhaseMLP học từ MỌI user, gỡ data-starvation) → forced-IRS warmup
      → intrinsic alignment reward → (CUỐI) phase pretrain. Mỗi can thiệp KÈM diagnose: phase-quality
      probe (|Σφ| đạt vs optimal N vs random √N; IRS-routed user có thắng direct không), phase entropy
      trend, greedy-vs-sampled QoS. → cần thiết kế phase-quality probe (giống probe_irs_vs_direct
      nhưng dùng PhaseMLP LIVE).
    SAU PhaseMLP → QoS (λ_D per-ramp) → sum-rate. Readout redesign: chỉ sau khi critic+pha ổn.

    >>> VITAL ANALYSIS + LATENT RISKS (2026-05-31, result_8 ep311 pre-anneal) <<<
    Q1 — "reward cao + IRS thấp": KHÔNG mâu thuẫn ưu tiên, là HỢP LÝ. Data: IRS/K ≈ Blk/K ≈ 6
      → agent route ĐÚNG (blocked→IRS, non-block→direct). Probe đã chứng minh pha NGẪU NHIÊN →
      IRS thua direct cho non-block → dùng direct = đúng. "Optimize IRS" ≠ ép IRS usage; nghĩa là
      làm PHA TỐT → IRS đáng dùng → usage tự lên. ĐỪNG ép IRS (pha xấu → ép = reward tụt). Cái
      thật phải lo = PhaseMLP IDLE (ent phẳng ~58%); IRS-không-tăng chỉ là TRIỆU CHỨNG.
    Q2 — LATENT RISKS (init phase 0.2 nặng vì các hành vi khóa cứng ở ramp sau):
      • ⭐ DIRECT-ATTRACTOR LOCK-IN [J]: 0.2 Direct=100% servable → agent đủ sống bằng direct,
        KHÔNG học IRS → khóa "lazy direct". Ramp khó (0.3-0.5, direct yếu) → không có kỹ năng IRS
        bù → SỤP. → kỹ-năng-IRS PHẢI học Ở 0.2 (= lý do PhaseMLP-stage ưu tiên cao).
      • ⚠ BUG ĐÃ FIX (train.py:907): PopArt stats stale khi resume ramp. Resume LOAD critic →
        nạp pa_μ,σ ramp cũ; pa_initialized=True → warm-up chỉ EMA(β0.1) đuổi chậm → critic sai
        thang ~10 rollout đầu ramp mới (lúc fragile nhất). FIX: reset pa_initialized=False khi
        resume → warm-up SNAP scale mới + re-fit head trước actor update.
      • λ FROZEN [E]: encoding lượng tử không học (y/z~0.50, r=0.06) — khóa init, persistent.
      • Easy-ramp trap: 0.2 quá dễ → không ÉP học IRS (cấu trúc; PhaseMLP-stage chống cái này).
    Q3 — VQC đổi có gián tiếp giúp PhaseMLP? CÓ, 2 kênh: (a) assignment chính xác hơn (R1-b per-IRS
      ZZ → route đúng blocked→IRS → PhaseMLP có data liên quan, gỡ data-starvation); (b) latent z_t
      tốt hơn → input PhaseMLP tốt hơn. NHƯNG GATE: chicken-egg (route nhiều IRS chỉ giúp nếu pha
      lên) + sequencing (readout bước 4). VQC KHÔNG sửa frozen-λ, KHÔNG trực tiếp tối ưu pha.
      → fix pha TRỰC TIẾP hơn = counterfactual phase loss (học từ MỌI user). VQC readout = HỖ TRỢ
      gián tiếp (đúng intent doc VQC-Readout-Design), không thay thế phase fix.

    Code hiện tại: train.py:1395 GAE-target. params: λ_D1.5 β0.001/anneal0.1 noise16 warmup24×50
      gae0.9 lr_critic3e-4 tau0.02 grad_clip_critic1000 checkpoint100. Best ckpt (QoS): chưa có (~52%).
    Smoke test artifact result_7/ (14 ep, --no-pretrain): xóa được — chỉ dùng để verify diag pipeline.

── R_LoS = 0.3 ──  (resume từ checkpoint tốt nhất của 0.2)
    λ_D khởi điểm: 3.5 | Feasibility: ____ | [điền]

── R_LoS = 0.4 ──
    λ_D khởi điểm: 4.0 | Feasibility: ____ | [điền]

── R_LoS = 0.45 ──
    λ_D khởi điểm: 4.5 | Feasibility: ____ | [điền]

── R_LoS = 0.5 ──  (bước cuối, khó nhất)
    λ_D khởi điểm: 4.5–5.0 | Feasibility: ____ | [điền]


================================================================================
  ROADMAP TĂNG IRS-USAGE  (mục tiêu: IRS/K ≥ ~2/3, không chỉ phục vụ user bị block)
================================================================================
  Bối cảnh: các run trước Blk/K~6-7/10 nhưng IRS/K chỉ ~6/10 đổ xuống (≈ random).
  Mong muốn: agent GỘP user (kể cả có direct link) dùng IRS để LOAD-BALANCE giảm tải
  group-0 (direct/common) → IRS/K cao hơn.

  HIỂU ĐÚNG (đừng kỳ vọng nhầm):
   - IRS-usage là PHƯƠNG TIỆN, không phải mục tiêu. Mục tiêu là reward (sum_rate+QoS).
   - Readout làm assignment ĐÚNG HƠN (info), KHÔNG tự đẩy IRS lên. IRS-usage cao là
     KẾT QUẢ của: IRS thật sự đáng dùng (pha tốt + benefit verified) + critic chạy +
     load-balancing trả tiền (reward tăng). Ép IRS khi direct tốt hơn = DƯỚI TỐI ƯU.
   - IRS thấp hiện tại HỢP LÝ khi pha ngẫu nhiên: IRS-random thua direct → agent chỉ
     dùng IRS cho user bị block. Muốn IRS cao → IRS phải đáng dùng TRƯỚC.

  CÁC BƯỚC (theo thứ tự — gate nhau):
   1. ✅ VERIFY (làm được NGAY, không đụng training): probe pha-tối-ưu
      → file: probe_irs_vs_direct.py (so |h_IRS|_opt vs |h_direct| per user, blocked/non).
      → trả lời: IRS-aligned có THẮNG direct cho user NON-BLOCK không?
        - Có → mục tiêu 2/3 IRS hợp lý/optimal → đi tiếp.
        - Không → ép 2/3 là dưới tối ưu → xem lại mục tiêu hoặc đổi hệ (↑N, ↓G_U…).
   2. Fix CRITIC (đang làm result_3) — agent mới học được load-balancing dài hạn.
   3. PHA TỐT (phase pretrain/optimize) → IRS cho rate cao → agent có lý do route nhiều hơn.
   4. READOUT redesign (VQC-Readout-Design.txt) → assignment đúng hơn (HỖ TRỢ, sau 2-3).
   5. (Tùy chọn) ENCOURAGE-IRS: optimistic q-init / IRS-usage bonus → đẩy usage lên — CHỈ
      nếu bước 1 xác nhận IRS đáng (nếu không = ép policy dưới tối ưu, yếu cho paper).

  KẾT QUẢ PROBE (probe_irs_vs_direct.py, 2026-05-30, R_LoS=0.2):
   Blk/K=71.5%. IRS-opt > direct: TẤT CẢ 87.9% (+34.6dB) | BLOCK 100% (+37.8dB) |
   NON-BLOCK 57.4% (median +1.5dB, p25 −3.2, p75 +6.4).
   → Mục tiêu 2/3 IRS: KHẢ THI (thậm chí bảo thủ — optimal ~88% IRS với pha tối ưu).
   ⭐ INSIGHT then chốt: pha NGẪU NHIÊN làm IRS yếu ~14dB (|Σφ| N→√N).
      - BLOCK: optimal +38dB → kể cả random (+24dB) vẫn thắng → agent ĐÃ dùng IRS (đúng).
      - NON-BLOCK: optimal chỉ +1.5dB → random (−12.5dB) THUA direct → agent đúng khi giữ direct.
      → IRS/K hiện ~6/10 (≈block count) là HỢP LÝ với pha xấu, KHÔNG phải lỗi.
   → LEVER để đẩy IRS/K > 7 (route cả non-block) = CHẤT LƯỢNG PHA (pretrain/optimize PhaseMLP),
     KHÔNG phải readout. Margin non-block +1.5dB chỉ hiện khi pha tối ưu. Thứ tự: critic→pha→(readout).


================================================================================
  HYPER CẦN CHÚ Ý Ở CASE 2  (dự đoán dựa trên Case 1, set thế nào → ra sao)
================================================================================
  - λ_D: PHẢI re-tune per-ramp qua feasibility (Case 1 dùng 3→4→4.5 nhưng K,M,P khác).
    Thấp → plateau [D]; cao quá ở R_LoS lớn → reward âm/phạt user bất khả thi [I].
  - λ lượng tử [E]: nq=12 → đóng băng TỆ HƠN Case 1. Theo dõi λ|max| sát; nếu đứng yên
    → spsa_n_reps↑ thêm / lr_actor_qc↑. Đây là rủi ro lớn nhất khi scale qubit.
  - PhaseMLP [F]: giờ 48 phần tử (2 IRS × 24) → khó gấp đôi Case 1. Theo dõi cờ
    entropy-domination của phase (entropy ~gấp đôi). Nếu đuối → nới n_hidden_phase.
  - IRS-select: 10 user × 3 lựa chọn (3¹⁰) — không gian gán lớn hơn nhiều → cần
    curriculum + gae=0.9 + critic tốt để phối hợp.
  - Compute: nq=12 → state 2¹²=4096 (16× nq=8) + SPSA nhiều rep → mỗi ep CHẬM hơn nhiều.
    Chỉ 1 job GPU/lúc.

  CHƯA TRANSFER ĐƯỢC trọng số Case 1 → Case 2 (shape khác). Train từ đầu ở R_LoS=0.2.


================================================================================
  Ý TƯỞNG ĐỂ XEM XÉT SAU  (chưa làm — quan sát fresh run λ_D=1.5 trước)
================================================================================

# ── (A) GỠ "DIRECT-ATTRACTOR / IRS chicken-and-egg" [J] — encourage agent dùng IRS ──
# Nút thắt: phase head ĐÓI DATA (chỉ học từ user gán IRS). → ưu tiên cách TĂNG TẦN SUẤT
# gán IRS (data flow), KHÔNG chỉ tăng giá trị IRS. Thứ tự ưu tiên:
#
#   Nhóm tăng DATA FLOW (targeted, đúng nút thắt — KHÔNG phải pretrain):
#   1. ⭐ Optimistic init q-head: bias logit gán về IRS lúc init → agent thử IRS nhiều
#        → phase có data → học alignment → sau đó chọn hợp lý. Rẻ, KHÔNG bóp méo reward.
#   2. Entropy có mục tiêu trên q-head (tách khỏi phase/power), anneal chậm hơn → giữ
#        IRS được thử. Coi chừng entropy-domination [C].
#   3. Forced-IRS warmup: N ep đầu ép 1 phần user lên IRS rồi thả. Thô nhưng chắc data.
#
#   Nhóm tăng GIÁ TRỊ IRS (reward shaping — kém targeted hơn, dễ bóp méo optimum):
#   4. Bonus dùng IRS có anneal về 0 (nên potential-based để bảo toàn optimum).
#   5. Intrinsic alignment reward: thưởng |g_SR·Φ·g_RU| cho IRS active (≈ pretrain trá hình).
#
#   Kiến trúc: 6. Counterfactual phase loss (cho phase head học cả user gán direct).
#
#   Quy tắc: encourage (1-3) = "cho IRS được thử nhiều hơn"; pretrain = "cho IRS điểm
#   xuất phát tốt". Bổ sung nhau. Thứ tự thử: quan sát fresh λ_D=1.5 → nếu drift về direct
#   [J] thì #1+#2 → nếu pha vẫn không học (data đủ nhưng PG quá chậm ở 48-element) → pretrain.
#   LƯU Ý: tất cả chỉ ăn khi critic hoạt động (λ_D đã fix) + IRS thật sự tốt khi được thử.

# ── (B) SỬA λ LƯỢNG TỬ FROZEN [E] — param-shift vs SPSA (PHỤ, cho paper) ──
#   CAVEAT quan trọng: KHÔNG đổi đại sang param-shift. Với DATA RE-UPLOADING, encoding
#   angle α (→λ) xuất hiện ở NHIỀU lớp (shared param). Param-shift 2-term naive (code hiện
#   tại shift α ở mọi vị trí cùng lúc) → KHÔNG chính xác (biased) cho param lặp.
#   Ngược đời: SPSA loạn biến α trực tiếp → đo ĐÚNG tổng đạo hàm (unbiased, chỉ nhiễu)
#   → SPSA THỰC RA ĐÚNG HƠN cho encoding re-uploaded.
#   → Muốn param-shift đúng phải multi-term (shift TỪNG lần lặp rồi cộng = 2·#lần·nq eval).
#   Hướng gỡ frozen đúng: (i) chạy diagnostic "λgrad r" (đã thêm vào train.py) — nếu r≈0
#   do NOISE → tăng spsa_n_reps cho encoding / multi-term param-shift; nếu r≈0 do TRIỆT TIÊU
#   CẤU TRÚC (grad λ ∝ mean batch của (·z_enc), z_enc layer-norm zero-mean → tự triệt tiêu)
#   → param-shift KHÔNG cứu được, phải sửa normalization/lr_lam. → CHỜ diagnostic rồi mới chọn.


================================================================================
  NHẬT KÝ RUN (Case 2)  —  điền khi có run đầu tiên
================================================================================
  (R_LoS=0.2, các lần thử ở ramp đầu — fix critic mù dai dẳng)
  L1 (result_1): λ_D3.0 β0.002 fresh → STUCK QoS54%, critic mù explVar~0.1 [B/I-2]; λ_D quá cao. FIX: hạ λ_D.
  L2 (result_1): λ_D1.5 β0.002 fresh → reward bớt âm, QoS~60%, critic VẪN mù (explVar0.07) +
        PhaseMLP IDLE + ⚠entropy-dom. FIX: hạ β.
  L3 (result_2): λ_D1.5 β0.001 fresh ep983 → β-fix gỡ entropy-dom (cờ tắt, phase 40→35) NHƯNG
        critic VẪN mù (explVar~0.08). → H1 BÁC; critic mù = H2 (fit/return-variance), độc lập readout.
  L4 (result_3): λ_D1.5 β0.001 + reward_noise_avg16 + warm-up FRESH → explVar lên ~0.4-0.6
        (vs 0.08, MỘT PHẦN ăn) NHƯNG critic BẤT ỔN: grad V plateau 700-1000 (ko giảm), explVar
        wobble 0.14-0.64, QoS kẹt 51%. → cần critic ỔN ĐỊNH hơn (root: TD(0) target nhiễu).
  L5 (result_4): + CRITIC TARGET=GAE-return (td_target→ret) → grad V GIẢM ~400-500 (vs 700-1000,
        tích cực) NHƯNG TREO ep96 (CUDA hang do game Windows tranh GPU/TDR [H]) → inconclusive.
        GIỮ GAE-target, chạy lại GPU sạch.
  L6 (result_5): GAE-target, GPU sạch, ep1055 → GAE KHÔNG ổn định critic: explVar wobble
        −0.46↔0.69 (ko giữ >0.5), grad V 300-1300 loạn. QoS THOÁI HÓA 51→47% + reward -138→-271.
        → GAE một mình ko đủ. NEXT: lr_critic 5e-4→3e-4. Nếu vẫn loạn → RETHINK (xem ĐIỂM DỪNG).
  L7 (result_6): lr_critic 3e-4 ĐƠN LẺ (grad_clip vẫn 1.0), GAE-target, fresh, đọc @ep~305 →
        lr↓ KHÔNG fix [B-2b]: explVar wobble 0.21↔0.66 (mean~0.45, ko giữ >0.5), grad V plateau
        400-1300 (ko giảm, clip 1.0 cắt ~99.7%) — Y HỆT result_5. QoS kẹt 49-52% (DƯỚI Direct100%
        & All-IRS76.8%; R_tot1.50<Direct1.72 → thua baseline cả 2 trục). PhaseMLP IDLE (ent ph
        phẳng ~58% max), λ frozen [E]. best175.6 unbeaten 274ep [D]. → grad_clip=1.0 là thủ phạm
        gốc (Tier-2 clipfrac~100%), lr ko chạm tới. NEXT: grad_clip_critic 1.0→5.0 + Tier-2 on → result_7.
  DIAG (2026-05-31, KHÔNG phải run train): build 4-tier critic-diagnostic pipeline + chạy
        probe sơ bộ. KẾT QUẢ THAY ĐỔI CHẨN ĐOÁN:
        - Ceiling explVar(V(s)): random-policy 0.16 NHƯNG trained-policy (result_5 ckpt) 0.81.
          → observed ~0.40 CÁCH ceiling 0.41 → critic KHÔNG env-bound, còn nhiều room (bác giả
            thuyết "đã sát trần aleatoric" của phân tích trước).
        - Tier-2 trên smoke-test phát hiện: clipfrac=100% (grad_clip_critic=1.0 cắt mọi minibatch,
          preclip 30-210) [II-2]; critic ReLU dead 46-63%; EV xấu dần theo σ² (q1 0.36→q4 0.19,
          n0 hại critic ở step nhiễu); bias |V-ret| drift mạnh (scale drift [Nhóm A]).
        → NEXT (override "chỉ lr_critic"): grad_clip_critic 1.0→5.0 KÈM lr_critic↓ (cùng nhóm [B]).
  L8 (result_7): grad_clip 1.0→1000 (one-var vs result_6), Tier-2 on, dừng ep587 →
        clipfrac 100%→0.0% (clip thông, preclip tự co 400-1300→~200) NHƯNG explVar VẪN wobble
        −0.37↔+0.62 (mean0.29), QoS kẹt 52%, R_tot↑1.50→1.61 (bỏ QoS), best185 unbeaten538ep [D].
        → H1 (grad_clip=nút thắt) BỊ BÁC: clip bind chỉ là TRIỆU CHỨNG. Critic moderate-noisy
        (beats baseline 96-100%) nhưng KHÔNG phải nút thắt QoS (QoS kẹt 52% bất kể explVar).
  ORACLE (2026-05-31, sweep 32 config clean data): MARGINAL spread arch 0.104 ≫ norm_tgt 0.042
        ≫ grad_clip 0.004. → arch 512 fit tốt nhất (đừng nhỏ), norm_tgt=True là lever, grad_clip vô nghĩa.
        Xác nhận độc lập H1 bác + chỉ hướng PopArt.
  L9 (result_8): FULL PopArt (one-var vs result_7: clip giữ 1000), GAE-target, fresh.
        T1 grad O(1-5)✅ · T2 bias~0✅ · T5 POP|ΔV|=1e-15✅ · T4 track ok. T3 post-anneal(ep>400):
        explVar mean0.45 std0.11 min0.30 frac>0.5=44% → STABLE-CAPPED (KHÔNG dip âm — khác HẲN
        result_5/6/7 wobble −0.46). "WOBBLE" theo ngưỡng cứng mean>0.5 nhưng std+min PASS = ổn định
        ở ~trần aleatoric K10. → PopArt ĂN: critic ỔN + agent CẢI THIỆN (QoS 51→59→giữ57% post-anneal,
        reward âm→+50; result trước CHƯA BAO GIỜ). → critic ĐỦ TỐT, sang tier sau. (run không kill, dõi tiếp.)
  PROBE QoS (2026-06-01, ckpt ep400):
        • QoS-by-assignment: IRS-assigned 60.8% > direct 55.4% → IRS-routing serve TỐT (kể cả pha random,
          IRS thắng blocked-direct). KHÔNG phải gánh nặng. NHƯNG ~40% user rớt D_k trên CẢ 2 link.
        • ⭐ POWER-QoS counterfactual (probe_power_qos.py): wp-top2=48% (agent ôm 2 user nửa private power).
          CHIA ĐỀU power (giữ y assignment/phase) → QoS 58%→83% (+25.5pts!), sum-rate gần như không đổi
          (1.51→1.47). met user có 2.7× power vs unmet. → NÚT THẮT QoS = PowerMLP CONCENTRATE POWER,
          KHÔNG phải critic/phase/assignment. PowerMLP under-optimized (ôm power max sum-rate biên → đói
          8 user → rớt). → ⭐ LEVER QoS CHÍNH = fix PowerMLP (dàn power / QoS-aware), không phải phase.
  DRIFT ANALYSIS (2026-06-01, result_8 ep1500 — run dừng SIGINT, model tốt = ep_00700):
        TIMELINE NHÂN-QUẢ (explVar đáng tin từ ep924; wc/Ck đáng tin toàn run):
          ep 300-684  : explVar 0.45-0.64 ổn · wc 19→45% · Ck 0.25→0.66 — RISE LÀNH (wc tăng dần = dùng
                        common-stream HỢP LỆ)
          ep 700-1020 : explVar ~0.45 còn OK · wc 36-52% · Ck 0.5-0.67 — QoS chớm slip 60→57%, reward
                        bắt đầu giảm
          ep 1068     : explVar −0.21 (DIP SÂU đầu) · wc 28% · Ck 0.40 — CRITIC bắt đầu loạn
          ep 1308-1356: explVar 0.32→−0.06 · wc 44→23% — critic tụt hẳn
          ep 1404     : explVar 0.15 · wc 71% · Ck 1.43 — POWERMLP DEGENERATE CỰC ĐOAN ← SAU critic loạn
          ep 1500     : explVar 0.06 · wc 40% · Ck 0.60 — thoái hóa
        ⭐ KẾT LUẬN NHÂN-QUẢ: PowerMLP KHÔNG phải nguyên nhân GỐC của drift — nó là BỀ MẶT nơi drift hiện ra.
          BẰNG CHỨNG THỨ TỰ: critic destabilize TRƯỚC (explVar dip −0.21 @ep1068, tụt hẳn @ep1356) →
          RỒI wc spike cực đoan 71% @ep1404. Critic loạn → advantage nhiễu → PowerMLP trôi vào mode
          degenerate. KHÔNG phải PowerMLP tự gây loạn rồi critic theo sau.
        3 GỐC THẬT (PowerMLP KHÔNG trong số đó):
          1. ⭐ CRITIC NON-STATIONARITY RUN DÀI — đúng cảnh báo T3 "stable-capped 0.45 borderline".
             Qua ep1000+ critic không bền → explVar sụp → advantage nhiễu. PopArt giữ SCALE nhưng
             KHÔNG cứu non-stationarity policy-drift.
          2. QoS-ABANDONMENT (λ_D=1.5 thấp) — agent đổi QoS lấy Rtot (1.60→1.76). Reward GIẢM dù
             Rtot tăng → policy degrade, KHÔNG phải reward-hack hợp lý.
          3. ENTROPY ANNEALED (ent q 8→4.6, đã qua sàn) — hết explore để thoát degenerate mode.
        POWERMLP CÓ vai trò (2 cách — đừng nhầm là gốc):
          (a) ENABLER: wc/wp output KHÔNG bị ràng buộc → có sẵn "hướng degenerate" (dồn common-stream)
              cho drift trôi vào. Ràng buộc PowerMLP giảm BỀ MẶT drift, nhưng KHÔNG fix gốc.
          (b) VẤN ĐỀ RIÊNG (probe_power_qos): wp-concentration → trần QoS ~58%. Cái này có Ở ĐỈNH
              (lúc lành ep400-700) — TÁCH BIỆT với drift. PowerMLP suboptimal nhưng KHÔNG gây drift.
        HÀM Ý:
          • FIX PowerMLP ≠ FIX DRIFT. Sửa PowerMLP (dàn power / QoS-aware) trị TRẦN QoS, KHÔNG trị
            drift. Drift cần: early-stop + entropy-floor↑ + (λ_D / non-stationarity lever).
          • Drift = BẰNG CHỨNG KÉP: (i) T3-borderline critic không đủ bền run dài → cần lever
            non-stationarity (xem Common [B-2b] "T1+T2+T4+T5 PASS NHƯNG T3 WOBBLE" branch:
            target-net cho value / GAE-λ thấp hơn / freeze-then-fit critic). (ii) λ_D=1.5 thấp
            cho phép QoS-abandonment ở run dài.
  >>> RESUME ĐIỂM (2026-06-01, sau drift result_8) — CẬP NHẬT (override line trước) <<<
      Critic PopArt = stable-capped NGẮN HẠN (post-anneal ep400-700) NHƯNG KHÔNG bền dài hạn → drift.
      PowerMLP có 2 vai trò ĐỘC LẬP: (a) trần QoS ~58% — lever fix: dàn power / QoS-aware (chính đáng);
      (b) bề mặt drift — fix giảm rủi ro nhưng KHÔNG đủ.
      THỨ TỰ NEXT RUN (chốt khi user quay lại):
        1. EARLY-STOP-BY-QoS (luôn bật từ giờ — chặn drift consume model tốt; dừng nếu QoS rolling-50
           không vượt best sau N=200 ep)
        2. NON-STATIONARITY LEVER cho critic (per Common [B-2b] T3-wobble branch): target-net value
           HOẶC GAE-λ thấp hơn HOẶC freeze-then-fit
        3. POWERMLP FIX (lever QoS trần, độc lập drift)
        4. ENTROPY-FLOOR↑ + λ_D candidate (1.5→2.0) chống QoS-abandonment dài hạn
      Best ckpt (QoS): ep_00700 (QoS đỉnh 60% TRƯỚC drift). result_8 stop @ ep1500 (SIGINT). <<<
  L10 (result_10 smoke 60ep / result_9 fresh 4000ep → SIGINT @ep~270): POWERMLP FACTORED ARCH (option B)
        Implementation: 3-way softmax (split[2] + common[M+1] + private[K]) thay single softmax[M+1+K].
        Loss: per-axis entropy bonus, β_global cho split/common + (β_global + β_pwr_private) cho private.
        Lý do option B (vs option A slice-entropy): single-softmax cũ ép 2 quyết định ĐỘC LẬP (budget
        split common-vs-private + within-private allocation) vào 1 logit vector → gradient bleed. Tách
        ra cho phép entropy bonus trên π_private TRỰC GIAO với common-vs-private split.
        Code: RL/sub_actors.py PowerMLP rewrite. train.py: trans dict {power_a_split/common/private},
        4-tuple unpack từ compute_grads_batch, diag panel + dòng mới "pw axis (H/Hmax)" + π_split(common).
        params: beta_entropy_pwr_private (extra trên private axis).

        ITERATION HYPERPARAM (β + init bias):
          [smoke result_10] β_pwr_private=0.02 + bias=+1.0 (no zero-W trên split) → ⚠ENTROPY-DOMINATED
                pw (ent/pg=2.43, β_p 21× global β), wc=7-15% (split bias không đủ chống PG về private —
                bias yếu vì W không zero-out). FAIL. Delete result_10.
          [result_9] β_pwr_private=0.003 (6.7× nhỏ hơn) + bias=+1.0 + ZERO W[-1][:,0:2] (bias dominate
                init thực) → ent/pg pw=0.26-0.43 OK, NO entropy-dom; π_split(common) LOCKED ~70% (init
                73%, PG drift không nổi 5%); wc=67-72% locked. SO RESULT_8 ep200-280 theo priority:
                ✅ #1 CRITIC: explVar 0.34±0.06 (vs result_8 0.40±0.18) — STABLE HƠN, no wobble âm
                ❌ #2 PHASEMLP: ent ph 61%max (vs result_8 56%) — REGRESSION, idle hơn
                ✅ #3 NO-IDLE pw: ent pw 12-20 (vs result_8 2.0 settled-concentrated) — active spread
                ⚖️ #3 NO-IDLE λ: y/z=0.48/0.50 vs 0.50/0.50 — đều frozen [E], không thay đổi
                ❌ #4 QoS: 48-50% (vs 56-59%) — behind 8pts (LOW priority, đừng đọc khi #2 fail)
                ❌ #5 Rtot: 1.38-1.46 (vs 1.48-1.51) — slightly behind
                ROOT CAUSE PHASEMLP REGRESS: bias=+1.0 LOCK wc=70% → private 30%/K=10 = ~3% per user
                (vs result_8 wc~30% → 7% per user, 2.3×). PhaseMLP đói tín hiệu phân biệt user gain.
                Đổi 1 priority-3 issue lấy 1 priority-2 issue = NET NEGATIVE per Common Part5 #8.
                SIGINT @ep~270.

        VERDICT FACTORED ARCH (option B):
          ✅ ĐÚNG PRINCIPLE: critic stability + PowerMLP no-idle là gain THẬT (cải thiện 2/5 priorities).
          ❌ INIT BIAS +1.0 OVER-CORRECTION: lock split → starve private → PhaseMLP regress.
          GIẢI PHÁP: hạ init bias +1.0 → +0.3 (π_split init [0.57, 0.43] thay vì [0.73, 0.27]). PG
          có space drift split xuống ~30-40% (như result_8 healthy), per-user private ~6-7% P_S đủ cho
          PhaseMLP có signal. Giữ factored arch + zero W + β=0.003.
  >>> RESUME ĐIỂM (2026-06-01 sau L10) — OVERRIDE line trước <<<
      Trạng thái: PowerMLP factored arch ĐÚNG hướng (critic+no-idle pw gains). Bottleneck là INIT BIAS
      cường độ — sửa bias=+0.3 đã apply. Sẵn sàng fresh restart R_LoS=0.2 → result_10.
      KỲ VỌNG result_10: priority #1 critic giữ stable (đã pass), #2 PhaseMLP HỒI PHỤC ent ≤ 56% max
      (≈ result_8), #3 no-idle pw GIỮ active, #4 QoS có chance vượt 58% (lever wp-spread vẫn còn). <<<

  L11 (result_10 SIGINT @ep119, 2026-06-01): factored PowerMLP + bias=+0.3 + β_pwr_private=0.003.
        KẾT QUẢ THEO PRIORITY ORDER:
          #1 CRITIC: 🟡 MARGINAL. explVar mean 0.28, dip âm -0.19 @ep71 (recover), preclip‖∇‖V 0.8-3.7
             tight, clipfrac 0%. Borderline wobble nhưng không crash.
          #2 PHASEMLP: ❌ REGRESS XUYÊN TIẾP. ent ph ~40 (61% max) — GIỐNG result_9 với bias=+1.0
             → hạ init bias KHÔNG fix priority 2. ent/pg phase = 0.69-1.04 ⚠ ENTROPY-DOMINATED.
          #3 NO-IDLE λ: ❌ λ FROZEN [E] confirmed. y/z = 0.48/0.49-0.50 (init values).
             λgrad mag=0.24 net=0.015 r=0.04-0.09 STABLE ~0.06 → ZERO-MEAN NOISE pattern.
          #3 POWERMLP FACTORED: ✅ SUCCESS. π_split(common) 57% (drift từ init 57%, KHÔNG locked
             extreme như result_9 70% lock). wc=57% giữa result_9 (70%, bad) và result_8 (38%, healthy).
             ent/pg pw = 0.42 (no ENT-DOM). pw axis split=98% common=95% private=77% — healthy spread.
          #4 QOS: 🟡 plateau 52% (target 55%+, baseline result_8 ep50-100 ~57%).
          #5 RTOT: 🟡 1.38-1.43 (baseline result_8 1.48-1.51) — slightly behind.
          + Best reward POSITIVE +54.5 @ ep62 (sớm hơn baseline) — quỹ đạo OK.

        ⭐ ROOT CAUSE: Init bias factored PowerMLP ĂN ở priority 3 PowerMLP. NHƯNG priority 2 PhaseMLP
        regress KHÔNG do PowerMLP — gốc là λ FROZEN [E].
        Logic chain: λ frozen → VQC encoding không học → state representation cố định theo λ init →
        AE+VQC output cho PhaseMLP không có gradient signal differential → phase ent idle.
        → Cần fix λ frozen TRƯỚC để unblock PhaseMLP. SIGINT @ep119, KEEP factored arch, proceed G2.

  L12 (result_11 sẽ chạy, 2026-06-01): G2 ATTEMPT 1 — spsa_n_reps 8→16.
        Diagnosis: λgrad r=0.06 STABLE = zero-mean noise pattern. Per Training-Case-2 Ý TƯỞNG (B):
          - NOISE candidate: SPSA samples cancel after batch averaging → fix = ↑n_reps
          - STRUCTURAL candidate: g_λ ∝ mean(δ·z_enc), z_enc layer-norm zero-mean self-cancel
        First attempt: spsa_n_reps 8→16 (one-param change). Cost ~2× VQC compute/step (60s/ep
        vs 30s), 4000 ep ≈ 55-65h.
        Watchpoint ~ep 100-150 (Telegram monitor per-diag):
          ✅ NOISE confirmed: λgrad r jump > 0.2 + λ|max| moves off init within ep 200 + ent ph
                              drop < 56% max
          ❌ STRUCTURAL: r stays ~0.06 với spsa=16 → escalate G2 attempt 2 (normalization fix)
        Carry-over KEEP (don't touch): factored PowerMLP + bias=+0.3 + β_pwr_private=0.003,
        critic PopArt + GAE-target + grad_clip 1000, per-axis pw diag.
        Maps tới: C3 (VQC expressivity — encoding phải học để quantum có effect thật).

  >>> RESUME ĐIỂM (2026-06-01 sau L11/L12) — OVERRIDE prior <<<
      G1 (factored PowerMLP) verdict = PARTIAL pass (priority 3 ✅, priority 2 ❌ do λ frozen). Keep arch.
      G2 (frozen-λ fix) = NEXT FRESH RUN. spsa_n_reps 8→16, expect ~60s/ep, 4000 ep target.
      Monitor via Telegram (monitor_train.py updated 2026-06-01 — per-diag notify, not 30-min interval).
      Verdict gate ~ep 100-150: λgrad r > 0.2 + λ|max| moves → success → proceed G3 (R1 readout +
      SoftmaxPQC bundle). r stays ~0.06 → escalate G2 attempt 2 (normalization fix). <<<

  L13 (result_11 SIGINT @ep~1200, 2026-06-01): G2 attempt 1 = spsa_n_reps 8→16.
        VERDICT G2-att1 ❌ FAIL (λ unfreeze) — λgrad r STAYS 0.05-0.08 (≈ result_10 0.06) ở cả
        ep12 và ep1100+. spsa 2× samples KHÔNG đổi r → BÁC noise hypothesis → STRUCTURAL confirmed
        (z_enc per-sample LN zero-mean self-cancels). λ|max| y/z=0.49-0.50/0.51 (vẫn frozen).
        SO RESULT_8 (gold baseline): result_11 KHÔNG vượt priority nào. best +158 (vs r8 +233),
        QoS 55-56% (vs 57-59%), Rtot 1.62-1.65 (vs 1.68-1.76). explVar dip -0.47 @ep1103 (drift onset
        ~ep1000 giống r8). spsa=16 GIỮ (task signal hơi tốt hơn) nhưng KHÔNG fix λ.
        → result_8 vẫn gold baseline. result_11 = architectural-only, không production-better.
        → λ STRUCTURAL → cần Δ1 normalization fix (không phải spsa). Đi G3 bundle.

  L14 (ARCHITECTURE-2 IMPLEMENTED 2026-06-01, branch vqc-arch2-softmaxpqc) — Core 4Δ:
        Δ1 (λ-unfreeze): encoding norm = LayerNorm + learnable affine (gamma_enc, beta_enc).
            Per-sample LN GIỮ (drift-stability, user nhấn mạnh); affine β adds DC offset phá
            zero-mean cancellation. dL/dλ_i = γ·mean(g·ln) + β·mean(g) — β recovers DC gradient
            mà pure LN annihilate. init γ=1, β=tiny noise.
        Δ2 (R1 readout): structured observables (per-user Z, per-IRS Z·Z_IRS, cluster mean-ZZ,
            Z_IRS) thay generic Z+NN-ZZ. N_QUANTUM=(nq-M)(M+2)+M=42 (Case2). Jacobian
            observable-agnostic (auto-adapt — không cần re-derive). CAVEAT: R1-b/c full value
            cần E1 entanglement (Δ3, follow-up) — core test với entanglement HIỆN CÓ.
        Δ4 (SoftmaxPQC head): thay post-NN MLP (~50K) bằng linear + trainable β_temp
            (Jerbi et al.): logits=β·(W_sm@[z_t‖o_hat]+b_sm). 2011 params (Case2) = 25× ít hơn.
            Clean quantum-vs-classical attribution.
        Δ5 (bypass): z_t giữ trong head input = per-user resolution.
        VERIFY (analysis/verify/run_all.py — ALL PASS): R1 obs correctness 1e-7, head gradcheck
            relerr 1e-8, λ/affine gradcheck qua SoftmaxPQC 6e-8, PPO single/batch logprob 4e-15,
            save/load+greedy parity. Existing B1/B2 tests pass (default generic+MLP unchanged).
        ⭐⭐ KEY FINDING (train smoke 2ep): λgrad r=0.65 "consistent→should move" (vs result_10/11
            r=0.06 "frozen")! SoftmaxPQC linear head (no MLP dilution) + R1 + Δ1 affine → λ gradient
            O(0.4) (vs 2.8e-4 cũ). → frozen-λ ROOT CAUSE addressed ở architecture level.
        Config: params.py vqc_readout_mode='r1' + vqc_softmax_head=True + vqc_softmax_beta_init=1.0
            (A1 ablation toggle về 'generic'+False = baseline). spsa_n_reps=16 + factored PowerMLP
            + bias=+0.3 + β_pwr_private=0.003 carry-over. Critic PopArt unchanged.

  >>> RESUME ĐIỂM (2026-06-01 sau L14) — OVERRIDE prior <<<
      Architecture-2 (Δ1+Δ2+Δ4+Δ5) IMPLEMENTED + verified (all V1-V5 pass), committed branch
      vqc-arch2-softmaxpqc. λgrad r 0.06→0.65 ở smoke = frozen-λ addressed.
      NEXT: launch FRESH result run R_LoS=0.2 trên architecture-2 (~60-65s/ep spsa16+42obs, 4000ep).
      WATCHPOINTS (priority order):
        #1 critic: explVar giữ stable (PopArt unchanged — kỳ vọng OK)
        #3 λ UNFREEZE: λ|max| y/z MOVES off 0.50/0.45 trong ep 100-300 (r đã 0.65 → λ sẽ di chuyển);
           λgrad r giữ > 0.2
        #2 PhaseMLP: ent ph GIẢM < 56% max (λ học → encoding informative → phase có signal)
        #4 QoS: chance vượt result_8 60% nếu λ+phase unblock
        β_temp: theo dõi giá trị (inverse-temp tự học — kỳ vọng tăng dần = policy sharpen)
      Monitor Telegram per-diag. Gate ~ep 300: λ moved + phase ent down → SUCCESS → proceed G4/G5
      (nq=12 unification, A1 ablation). λ stuck dù r>0.2 → diagnose deeper. <<<
  result___  ...
================================================================================
